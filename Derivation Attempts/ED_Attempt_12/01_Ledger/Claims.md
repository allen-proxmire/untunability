# Claims (ED_Attempt_12)

*Statuses and rules as in `../../ED_Attempt_01/01_Ledger/README.md`. References to earlier attempts are written A1- through A11-ledger.*

| ID | claim | status | evidence | inputs | outside check |
|---|---|---|---|---|---|
| C1 | Reasoning: carried into attempt 12 from A11-ledger C24, C25 and RD20 - the attempts 7-11 thread; A11's decisions (D3-D6, D11-D13) and the standing results: ED's budgets fix the three totals CDT tunes, with nothing tuned and no new free numbers (A11 C5, exact algebra, the project's strongest claim); ED sits inside 3D CDT's extended phase at tau = 1/3 (A11 C8) and never collapses in the 2+1 runs (A11 C20); A6's rate-matching floor measured on four known shapes (A11 C21); ED's own patterns hold their clocks together on 3.5-3.8 relations per event, below ED's link budget of 6.7 (A11 C22); and the finding of record, reached independently by two routes - A10 C33 (counting whole histories on borrowed triangles) and A11 C23 (ED's clocks with no geometry) - that **a shape is carried, not chosen**. Not established: three dimensions from ED's rules, where a new event goes, space derived rather than kept. Inputs supplied: 3; nothing derived. Tools: `n1_sync.py` (the locking test, calibrated on ring, square torus, cubic torus and 6-regular web), `n2_grow.py` (growth with the strain-driven relation rule and the full-dynamics arbiter), `n2b_descent.py` (descent attachment), `st3f.py` (the compiled 2+1 sampler, validated against published CDT). Opening road P, persistence: does ED keep a shape and keep it sync-able, and what does that cost under the census guard. | CONDITIONAL | `Carry_Forward.md`; A11-ledger C24, C25. | A10 C33; A11 C5, C8, C20-C24 | — |
| C2 | Road P on paper (note 2 `P_Persistence.md`), reasoning only. The trap named first: if relations never break (A11 D12), "a shape persists" is close to a tautology, so the road sharpens it into three questions that could fail - P1 does ED DESTROY what cannot hold its clocks together (a line and a flat sheet provably cannot at large size, A11 C21, so a pattern that starts as a line should not be able to stay one as it grows; A11's probe 2 found a ring stays a ring, but that probe had no sync filter running); P2 does a shape stay sync-able as it grows (the pull needed could start rising with size); P3 does persistence beat sheer number (A10 C33 probed it; carried, not opened). The reframe: **ED may not be a generator of space but a filter on it** - it does not make three dimensions (A11 C22, C23) but its own content forbids a pattern that cannot hold its clocks together from persisting, which rules out one and two dimensions and nothing further; three and small worlds both survive and ED does not choose between them. Census guard stated: that is a CONSTRAINT ED derives, not a reduction - the input list does not get shorter, and it will not be written up as a derivation of space. First test: the configuration road N never ran - spent events, generation-wise growth with children of neighbouring parents linked, and the sync filter running throughout - started from four shapes (line, flat grid, 3D grid, small-world web), reading what each becomes. Outcomes: line and flat grid destroyed while the 3D grid survives = the filter bites, road P's positive result; everything forced into a small world = the filter overshoots, a clean negative closing the line of attack; everything keeps its shape including the line = persistence is a restatement, road P closes; the 3D grid stops holding together = worse than any of them. Meaning questions with proposed defaults: P-Q1 "keeps its shape" = the readings hold within +/- 0.3 over a four-fold growth in size, fixed before running; **P-Q2 (the hinge) what happens to a pattern whose clocks cannot hold together - default: it is not allowed, A6's own reading of Synced Now; alternatives are that it breaks into pieces that each hold, or that nothing happens**; P-Q3 who may pass on and when an event is spent - default: an event passes on once and is then spent (Claude's reading of A9 D3, and the one Allen should rule on himself); P-Q4 space = the frontier (the accumulated pattern reported alongside); P-Q5 relations cannot break (D12 stands), with the consequence stated that a shape can only change through new events arriving; P-Q6 the 6.7 link budget reported, not enforced (borrowed from flat geometry, C16; enforcing it would put the answer in by hand). Expected results to be fixed before code: P0 shapes that can hold still hold (high); P1 the line and the flat grid do not survive as themselves (about 55%); P2 the 3D grid survives within +/- 0.3 (about 40%); P3 reported. Honest points: the claim is weaker than the project set out to prove and should be said in those words; shape stays a supplied input; the probes this road is built on are single-seed and one leaks relations, so the first test must be done properly; and P-Q2 is the hinge - without a real consequence for failing to sync there is no filter and nothing to test. Options: (a) answer P-Q1 to P-Q6, then the model on paper, then build [proposed]; (b) take P-Q2 alone first; (c) redo A11's probes properly first. | CONDITIONAL | `P_Persistence.md`. | C1; A6 C28, C29; A9 D3; A10 C33; A11 C21-C24, D12 | — |
| C3 | Specification (paper), road P's model (D2, note 3 `P_Model_Spec.md`). Each generation: every event passes on once and is then spent (D2 P-Q3); each parent has one child, a share have two (growth about 1.1 per generation, Claude's); spent events keep every relation and their child links (P-Q5), forming the accumulated pattern; the new generation's relations are INHERITED - siblings mutually linked, and for each relation between parents every child of one linked to a child of the other, which is **the fix for A11's leaky probe** (that version gave each parent relation a single cousin link, so relations bled away, mean degree 4.0 -> 2.2; this version hands each child its parent's full neighbourhood and preserves degree); then **the filter with teeth** (P-Q2): the generation's clocks are tested at a fixed pull K0 = 1, and while they fail relations are added where the strain is worst up to the ceiling, giving three outcomes - holds as inherited, holds after repair (how many recorded), or cannot be made to hold at all (DESTROYED, run ends). Readings on the frontier (P-Q4) and on the accumulated pattern: ball growth, patch-edge exponent, mean distance, small-world flag, relations per event. Four starts (ring, square torus, cubic torus, 6-regular web - the objects calibrated in A11 C21) at about 2,000 events grown to about 8,000, three seeds, TWO ARMS: filter on and filter off. Pass rule: "survives as itself" = ball growth within +/- 0.3 of its starting value with the clocks holding at every generation. Expected results fixed before code: P0 the 3D grid and the web hold at every generation (high); **P1 the main one, the line and the flat grid do NOT survive as themselves, being forced above two dimensions by the repairs or failing to hold (about 55%)**; P2 the 3D grid survives within +/- 0.3 (about 40%); P3 reported - what the web becomes, relations per event against 6.7, and how many relations the filter adds per generation and whether that grows with size; **P4 the control - with the filter off all four starts keep their shapes (A11's probe 2 done properly); if the line dies in this arm too, P1 is VOID**. What each result licenses: P1 and P2 both holding = ED forbids sub-three-dimensional space from persisting and keeps 3D space once there, a constraint derived from ED's own content and explicitly NOT a derivation of space nor a reduction in inputs; P1 holding and P2 failing = the filter is real but indiscriminate and the line of attack closes; P1 failing = persistence is a restatement, road P closes, leaving A11 C5 (the budget result) and A11 C21 (the measured floor). Claude's, labelled: growth factor, start sizes, three seeds, the +/- 0.3 tolerance, the bell-curve rate spread, and "where the strain is worst" as the repair rule (carried unchanged from A11 C22 so the attempts compare). ED's: events and relations, the ceiling, relations sticking, passing on once and being spent, passing on reaching a neighbourhood, clocks having to be able to hold together. Honest points: the filter can only ADD relations, never move them, so "destroyed" means changed beyond recognition rather than annihilated, and that limit follows from P-Q5; a line repaired into something three-dimensional was still a line to begin with, so the result would be that ED cannot KEEP sub-3D space, not that ED makes 3D space; all four starts are supplied, and the fourth input does not go away in this attempt. Cost: laptop-sized, hours. | CONDITIONAL | `P_Model_Spec.md`. | C2; D2; A6 C28, C29; A9 D3; A11 C21, C22, D12 | — |
| C4 | Road P run 1 INVALID, kept in `model/p_runs_invalid1/` (Claude's error, found while reading the numbers, before any write-up). Two faults. (i) **The inheritance rule inflated relations.** The cousin rule was applied from both parents' sides, so relations multiplied as the pattern grew: a ring finished carrying 12.65-13.2 relations per event against its starting 2, the flat grid about 20, the cubic grid about 28 - all far above ED's link budget of 6.7, and the inflation is Claude's, not ED's. Because more relations make locking easier, this flatters P2 (the 3D grid surviving) while making P1 (the line failing) only harder to achieve, so P1's direction survived it but nothing could be written up. (ii) **The pass rule charged the instrument's size bias to the pattern**, comparing a grown pattern's reading against its own small starting value when the ball-growth reading drifts with size on a fixed shape (a cubic torus reads 2.36 at 2,200 events and 2.67 at 8,000, A11 C22's controls). Recorded numbers from the invalid run, direction only: with the filter ON the ring lost its dimension entirely at all three seeds (about 2,400 relations added each), the flat grid went to 2.46, 2.91 and no dimension, the 3D grid to 2.30, 2.79, 2.35 (two with no repairs at all), the web unchanged with no repairs; with the filter OFF the ring held at 0.993 exactly. Revisions (RD5, both recorded before the rerun): inheritance now uses **ED's own conserved link budget** (A7 C4) - every parent relation is inherited exactly once, plus sibling relations, and relations per event is then topped up to its starting value, so the count is conserved exactly (verified: 2.00 -> 2.00 for a ring, 6.00 -> 6.00 for a cubic grid); and "kept its shape" is judged against **a fresh object of the same kind at the size the run ended at**, same tolerance 0.3. First result of the revised build, recorded before the rerun: **with the filter OFF the lattices drift upward anyway** - the 3D grid reads 3.20 where a real one of that size reads 2.69, the flat grid 2.28 against 1.93 - because ED does not say WHICH child of a neighbour a child attaches to, so random re-pairing slowly randomises a lattice. That is the pre-registered control (P4) doing its job, and it means the arm-to-arm comparison, not the absolute pass rule, is what the rerun can settle. | CHECKED (failure, fix) | `model/p_runs_invalid1/`. | C3; A7 C4; A11 C22 | — |
| C5 | **Road P run 2: the filter erases every shape, including three-dimensional ones (P0 as expected, P1 True but VOID by its own rule, P2 fails, P4 fails)** (`model/p_persist.py` with RD5's two fixes, expectations P0-P4 fixed in note 3 before any code; output `model/p_persist.txt`, data `model/p_runs/roadP.json`, note 4 `P_Results.md`). Four starts x three seeds x two arms, about 2,000 events grown four-fold, relations per event conserved exactly by inheritance (2.00 -> 2.00, 4.00 -> 4.00, 6.00 -> 6.00 in the control arm). **Filter on: all four starts converge** - ring 1.0 -> 4.21/4.15/4.20, flat grid 1.85 -> 4.01/4.03/4.09, cubic grid 2.36 -> 3.82/3.76/3.83, web (no dimension) -> 4.23/4.34/4.23; mean distance 5.7-6.4 in every case (from 500, 22.5, 9.7, 4.6); small-world flag set everywhere; relations per event 6.2, 6.3, 6.9, 6.8; relations added 7,019 / 4,681 / 2,125 / 1,884. **Filter off (control): ring 1.01 against 1.00 for a real ring that size (keeps its shape), flat grid 2.28 against 1.93, cubic grid 3.20 against 2.69, web 3.91** - so the lattices drift upward on their own, because ED does not say WHICH child of a neighbour a child attaches to and random re-pairing slowly randomises a lattice. Verdicts: P0 as expected; **P1 True but voided by its own pre-registered rule** (the flat grid fails in the control arm too), with the ring case nonetheless clean and attributable (kept at 1.01 with the filter off, destroyed to 4.2 with it on); P2 fails (the 3D grid reaches 3.82 with the filter and 3.20 without, against 2.69); P4 fails (only the ring keeps its shape). By note 3's table this is outcome 2: the filter bites but overshoots, destroying everything including the shape we would want kept. **Consequence for A11 C23/C24 (recorded plainly): the "a shape is carried, not chosen" finding was measured with sync switched OFF; with ED's own sync requirement enforced it does not hold - shape is ERASED and replaced by a common end state. The A10-A11 two-route agreement stands as a statement about counting and inheritance without sync, not as a statement about ED.** What ED does instead is CONVERGE: four very different starts reach the same end state, which is a stronger property than persistence and is ED's own doing, but the end state is a small world with no dimension. Also recorded: the repair rule was restricted to partners within three relations (the least shape-destroying available), and 7,000 local repairs still destroyed a line's shape entirely. **Diagnostic, labelled, not pre-registered (`model/p_k0check.py`, `p_k0check.txt`): relations per event settled at 6.2-6.9 against ED's link budget of 6.699, which the model was never given - so it was tested at pulls 0.5, 1 and 2, giving 9.60, 6.21, 5.94. It moves with the pull, so the match is a consequence of Claude's setting K0 = 1 and is NOT claimed** (recorded, not leaned on: it flattens above a pull of 1, doubling the pull moving it only 6.21 -> 5.94; one seed, three points). Live lead, from Allen's reading of ED's inflation language ("event-structure complexity outpacing event-production capacity", ED-Orientation's ED-08/00.1 cosmology summary): the knob that took A11's readings from a chain through about three to a small world is the ratio of event production to relation production, and the sharp question is whether there is a ratio at which the reading stops drifting with size; its honest cost is that unless ED supplies the ratio, tuning it to land on three is a dial and not a derivation. Options: (a) take stock of road P; (b) test the balance point; (c) ask what could stop the filter overshooting - the filter can only ADD relations because relations never break (D2 P-Q5), so a rule letting a relation lapse is the obvious missing piece and is a meaning question for Allen. No default taken. | CHECKED (result) / CONDITIONAL (reading) | `model/p_persist.txt`, `model/p_runs/roadP.json`, `model/p_k0check.txt`, `P_Results.md`. | C3, C4; D2; A7 C4; A10 C33; A11 C21-C24 | ED-Orientation (Event Density Dead repo), ED-08 / 00.1 cosmology summary |
| C6 | Specification (paper), the balance point (note 5 `B_Balance_Spec.md`), from Allen's reading of ED's inflation language - "event-structure complexity outpacing event-production capacity" (ED-Orientation, the ED-08 / 00.1 cosmology summary; Allen's recall had the two terms swapped). The question asked is not "what rate gives three?" but "is there a rate at which the reading STOPS MOVING as the pattern grows?" - a shape that holds still as it grows is a shape, one that drifts is on its way somewhere else. Test: road P's generation growth with the filter OFF and one knob, RHO = the relations per event the growth holds to, scanned over 2 to 16; starts a ring and a cubic torus at about 2,000 events, seeds, grown four-fold, the reading taken at two sizes and the drift judged against a real shape of the same kind at the same size. Expectations fixed before code: B1 a balance point exists, the drift changing sign (about 50%); B2 if it exists the reading there is 2.5-3.5 (about 25%); B3 reported. Honest cost recorded before the run: RHO is a dial, so a balance point reading three would NOT be a derivation unless ED supplies that rate - unless stability itself picks it out, which is why the test reads drift rather than dimension. | CONDITIONAL | `B_Balance_Spec.md`. | C5; A11 C23 | ED-Orientation (Event Density Dead repo), ED-08 / 00.1 cosmology summary |
| C7 | **The balance point: there is none, because the rate does not move the dimension at all (B1 fails on both starts)** (`model/b_balance.py`, expectations B1-B3 in the file header before it was written; output `model/b_balance.txt`, data `model/p_runs/balance.json`, note 6 `B_Balance_Results.md`). Eight rates from 2 to 16 relations per event, two starts, three seeds, grown four-fold from about 2,000 events, filter off. **Ring: the reading is 0.98-1.02 at EVERY rate** (a real ring reads 1.00); only the thickness changes, mean distance falling 1,045 -> 570 -> 513 -> 502 -> 500 and then stopping. **Cubic torus: below about 5 relations per event the pattern FRAGMENTS** (largest piece 47%, 65%, 82% at rates 2, 3, 4 - the genuine "events outpacing relations" side, with a threshold); at and above 5 it holds together and copies itself more faithfully as the rate rises - end readings 3.21, 3.25, 3.01, **2.71 at rate 12 against a real grid's 2.69**, and 2.50 at 16. **At no rate does either start become something other than what it was.** B1 fails (the gap to a real shape grows slowly at every rate and never shrinks, so there is no sign change to find); B2 does not apply; B3 as above, and the two starts agree. **Reading (Claude's, labelled):** Allen's inflation idea is half right - the rate genuinely controls whether a pattern HOLDS TOGETHER, with a threshold near 5 relations per event, but it does not control dimension. So A11's 1D -> about 3 -> small-world sweep was never about this rate; it was about WHERE new relations may reach. In this model relations only ever form between children of NEIGHBOURING parents - one step - and that alone preserves dimension at any rate. **Which points at road P's filter: it was allowed to repair within THREE steps and destroyed every shape, while inheritance reaching ONE step preserves shape perfectly. The destruction may be about the filter's reach rather than the amount of repair** - a cheap and decisive test. Options: (a) test the reach - rerun road P's filter at two steps and at three on the same starts [proposed]; (b) take stock of attempt 12; (c) settle whether a relation may ever lapse (D2 P-Q5 says no, so the filter can only add). | CHECKED (result) / CONDITIONAL (reading) | `model/b_balance.txt`, `model/p_runs/balance.json`, `B_Balance_Results.md`. | C5, C6; A11 C22, C23 | — |
| C8 | **Self-correction (D4, D5), reasoning only: road P's filter tested the wrong condition.** Two meanings Allen settled after seeing road P's results. (i) D4, a relation can dissolve - so the filter's add-only behaviour was an artefact of A11 D12 and this attempt's D2 P-Q5, not of ED; every generation inherited every repair the last one needed, which is the ratchet behind C5's overshoot. The replacement rule needs no new parameter: D13 (fewest relations that still hold) applied in both directions, adding where the strain is worst and letting go where there is none. (ii) D5, sync is local and there is no shared now - the filter as built in C3 demanded that the whole frontier frequency-lock as one, which is a shared now; ED has said since A6 that rates matching does not imply one. The condition should be A6's original: every patch holding its own rates against its own edge. **Status of the earlier results under this correction:** C5 stands as a record of what a globally-locking, add-only filter does to a pattern - all shapes destroyed, all starts converging on one small-world end state - but NOT as a test of ED's sync requirement; C7's balance scan is unaffected, since it ran with the filter off and its finding (the rate does not move the dimension; below about 5 relations per event patterns fragment) is about inheritance alone; the reach scan running at the time (B_Reach) inherits the same defect and can only say whether reach matters to a global add-only filter. **What this leaves open, and it is now the live question:** whether a local sync condition with relations free to dissolve keeps a shape, destroys it, or selects one - none of which the runs so far can answer. Claude's reading, labelled: this is the first time a negative result in this project has turned out to rest on a condition Claude imposed rather than on something ED says, and it was Allen who caught it, from the meaning rather than from the numbers. | CONDITIONAL (correction) | this ledger; conversation 2026-09-23. | C3, C5, C7; D4, D5; A6 C28, C29; A9 D3; A11 D12 | — |
| C9 | Specification (paper), the local sync model (D4, D5; note 7 `L_Local_Sync_Spec.md`). **The condition, in the form A6 actually argued for and never implemented:** a patch holds if the relations crossing its edge can carry the surplus of its clocks, and the pattern holds if every patch does - a local condition with no shared now, nothing happening at the same time anywhere. **Exact and free of any patch-size dial:** by max-flow/min-cut, such a pattern-wide carrying assignment exists precisely when every patch S satisfies |sum of omega over S| <= K e(S) with e(S) the relations crossing S's edge, so one max-flow computation settles every scale at once, and when it fails the minimum cut NAMES the starved patch. Model: road P's growth (pass on once and be spent, siblings and cousins linked, relations per event carried forward) with two steps each generation instead of one - relations ADDED across the returned min cut where a patch is starved, and relations that carry nothing DISSOLVED (D4), greedily, each removal kept only while the condition still holds. That is D13 applied in both directions, which is what ED has said all along and what every earlier model did only half of. Expectations fixed before code: L1 patterns satisfying the condition exist and growth reaches them without fragmenting (high); **L2 shapes are kept - the ring and the 3D grid each still read as themselves after four-fold growth (about 50%)**; **L3 the one most worth seeing - a small-world start SHEDS relations and reads lower than it started (about 25%)**; L4 reported - relations per event each start settles on, against ED's budget of 6.7 and C7's fragmentation threshold of about 5. What each outcome means: shapes kept -> road P's overshoot was Claude's global filter, not ED; everything still converging on a small world -> the overshoot is ED's and survives both corrections, closing the line honestly; a small world thinning toward a dimension -> ED's rules SELECT shape rather than merely permitting it, the thing sought since attempt 5; patterns fragmenting -> the two halves of D13 do not balance and ED needs more to say about when a relation goes. ED's parts: events and relations, the ceiling, passing on once and being spent, passing on reaching a neighbourhood (A11 D4), a patch holding its rates against its own edge (A6), relations able to dissolve (D4), no shared now (D5), fewest relations that still hold both ways (D13). Claude's, labelled: growth rate, start sizes, seeds, the bell-curve rate spread, and **the pull K - still the one number ED does not supply**, held at 1 in units of the rate spread and to be varied over 0.5, 1, 2 afterwards. Honest points: this is the first model in which ED's sync condition appears in A6's own form (everything since attempt 7 used geometry or a global lock); the starts are still supplied, so this can settle whether ED KEEPS or SELECTS a shape, not where the first one came from; shapes being kept is persistence, a constraint and not a reduction; a small world thinning would be a selection and is written at 25% for a reason; and if the answers move with the pull they are its answers as much as ED's. | CONDITIONAL | `L_Local_Sync_Spec.md`. | C8; D4, D5; A6 C28, C29; A11 D4, D13 | max-flow/min-cut (Ford-Fulkerson); A6's literature via C18 |
| C10 | **The reach is not the story (R1 fails, R2 holds)** (`model/b_reach.py`, expectations R1-R3 in the file header before it was run; output `model/b_reach.txt`, data `model/p_runs/reach.json`). Road P's filter rerun with its repair reach set to 2, 3 and 4 relations, on a ring and a cubic torus, two seeds, about 2,000 events grown four-fold. A defect was fixed first and recorded: A11's ball includes the events already related to the strained one, so at a reach of 2 most of the candidates were relations that already existed - the reach now means "between two and h relations away". Results: **no shape is kept at any reach** - ring readings 4.02, 4.10 (reach 3) and 3.96, 4.15 (reach 4) against 1.00 for a real ring; cubic torus 3.95, 3.71 (reach 2), 3.79 (reach 3) against 2.69; relations per event 6.05-6.17 (ring) and 6.81-6.92 (grid) at every reach; repairs 6,792-7,182 and 2,209-2,506. R1 fails, R2 holds (road P reproduced exactly). **Reading, written before the run and confirmed by it: a fixed reach COMPOUNDS** - the filter measures its steps in the graph it has already modified, so a two-step link this generation makes next generation's two steps span four of the original positions, and a short reach is not short for long. The ratchet, not the reach, is what destroyed the shapes - which is what D4 then addressed. Under C8 this run can only speak about a globally-locking add-only filter. | CHECKED (result) | `model/b_reach.txt`, `model/p_runs/reach.json`. | C5, C7, C8 | — |
| C11 | **Local sync (D4, D5): every start still converges, but on a SPARSE, spread-out end state (L1 as expected, L2 fails, L3 passes as written, L4 reported)** (`model/l_local.py`, expectations L1-L4 in the file header before it was written; output `model/l_local.txt`, data `model/p_runs/local.json`, note 8 `L_Local_Sync_Results.md`). A6's condition implemented exactly as a max-flow feasibility - every patch able to shed its surplus through its own edge, at every scale at once, no shared now, no patch-size dial - with relations added across the returned minimum cut and relations carrying nothing dissolved (D13 both ways). Sanity check recorded before the runs: a bare ring FAILS the condition at pull 1 and a cubic torus PASSES it, which is A6's argument reproduced by a test that knows nothing about dimension. Results at about 8,000 events, three seeds: ring 1.0 -> 4.67/4.73/4.95, cubic torus 2.36 -> 4.83/4.65/5.02, web (no dimension) -> 4.63/4.81/4.77, against 1.00 and 2.69 for real ones; **mean distance 7.6-7.9 and relations per event 3.01/3.04/3.06 in EVERY run, from starts as different as a line, a lattice and a random web**, with nothing tuned; every run in one piece. L1 as expected; L2 fails; **L3 passes as written** - the web shed half its relations (6.0 -> 3.06) and its mean distance nearly doubled (4.6 -> 7.8) while growing four-fold - **but its spirit only half happened: the pattern spread out without arriving at a definite dimension**. L4: 3.0 relations per event, below ED's budget of 6.7 and below C7's fragmentation threshold of about 5, yet still connected. **Pull check in the same run** (the one number ED does not supply): at pulls 0.5, 1, 2 the cubic torus gives readings 4.70, 4.83, 4.63 with mean distances 5.9, 7.8, 11.2 and relations per event 4.50, 3.02, 2.36 - **the density and the distances depend on the pull, the reading does not**, so the end state's shape is not an artefact of Claude's setting. **Verdict: the overshoot is ED's, not Claude's** - it survives both of Allen's corrections (no shared now; relations free to dissolve), which is what note 7 said in advance would close this line honestly. What ED's rules do is CONVERGE, not select: three very different starts reach one common end state, tightly, which is a stronger property than the persistence A11 thought it had found - but the end state is a sparse small world, not space. Worth keeping: with relations free to dissolve, patterns get sparser and MORE spread out rather than denser and more crammed, the reverse of every earlier model, so D4 was the right correction even though it does not rescue the dimension. **Claude's remaining assumption, flagged: a relation dissolves when it carries nothing THIS generation, with rates redrawn each generation, so the pattern churns - about 13,000 relations added and 13,000-17,000 let go in a run ending with about 25,000 - and that churn is a plausible cause of the randomising.** The meaning question for Allen: does a commitment lapse the moment it is not carrying anything, or persist while it is part of a commitment and lapse only after being idle a while? Options: (a) settle the lapse question and rerun, removing the last of Claude's assumptions [proposed, then (b)]; (b) take stock of attempt 12; (c) accept the convergence as the result and ask what the common end state IS. | CHECKED (result) / CONDITIONAL (reading) | `model/l_local.txt`, `model/p_runs/local.json`, `L_Local_Sync_Results.md`. | C8, C9, C10; D4, D5; A6 C28, C29; A11 D13 | max-flow/min-cut |
| C12 | **With D6 (a relation persists while it is part of a commitment): the churn stops and the readings move toward three, but shapes are still not kept (M1 as expected, M2 fails, M3 as expected, M4 reported)** (`model/l_local2.py`, expectations M1-M4 in the file header before the run; output `model/l_local2.txt`, data `model/p_runs/local2.json`, note 9 `L_Commitment_Results.md`). C11's model with one change: a relation that has EVER carried something persists, one that never has lapses; repair relations start uncommitted; an inherited relation carries its parent relation's standing forward. Results at about 8,000 events, three seeds: ring 1.0 -> 3.93/3.98/4.04, cubic torus 2.36 -> 3.75/4.86/3.86, web -> 3.76/3.92/3.92, against 1.00 and 2.69 for real ones; mean distance 6.8-7.2; relations per event 4.20/4.17/4.15 - still one common end state from a line, a lattice and a random web. M1 as expected; M2 fails; **M3 as expected and it is the check that the rule bites: the ring let go 2,143 relations where C11 let go 12,800, the others 5,700-6,000 against 15,400-17,300**; M4 as above. Pull check: at 0.5, 1, 2 the cubic torus gives relations per event 6.60, 4.16, 2.98 with readings undefined, 3.75, 3.92 - density and distances follow the pull, the reading does not move much between 1 and 2. **Observation, labelled, not a claim: each time an assumption of Claude's was replaced by something ED says, the end state moved - global lock with permanent relations 6.2 per event reading 4.2; local patches with instant lapse 3.0 and 4.8; local patches with commitment persisting 4.2 and 3.9. ED's own rule gives the lowest reading of the three. It is still flagged a small world, so 3.9 is not a dimension but what the reading returns when there is none; three points with no error bars are a direction, not a result.** Standing conclusion for road P: every assumption of Claude's that could be replaced has been (D4 dissolution, D5 no shared now, D6 commitment) and the answer has not changed in kind - **ED's rules CONVERGE, they do not SELECT**; the common end state is sparse, spread out, holds its rates patch by patch, and has no dimension. What did change: every correction moved the end state toward the sparser, more spread-out side and toward a lower reading, and Claude has no fourth candidate correction and says so rather than inventing one. Options: (a) take stock of attempt 12 [proposed]; (b) ask what the common end state IS, as a describable object nobody has looked at directly; (c) keep hunting for a fourth assumption to replace - not proposed, since looking without a candidate is how fitting starts. | CHECKED (result) / CONDITIONAL (observation) | `model/l_local2.txt`, `model/p_runs/local2.json`, `L_Commitment_Results.md`. | C11; D4, D5, D6 | — |
| C13 | **Attempt 12 stock-taken** (note 10 `Taking_Stock.md`), reasoning only. Road P opened on persistence - the one statement A10 and A11 had both landed on - and found it wrong: with ED's sync requirement switched ON every shape is destroyed (C5), overturning A11 C23/C24, which had been measured with sync off. Then three assumptions of Claude's were replaced by things ED says (D5 no shared now, D4 relations can dissolve, D6 commitment persists) and each time the answer improved in character and stayed the same in kind. **Standing statement: ED's rules CONVERGE, they do not SELECT** - three very different starts reach one common end state, tightly, with nothing tuned, and that end state is sparse, spreads out, holds its rates patch by patch, and has no dimension. Findings in order: C5 every shape destroyed; C7 the rate of relations per event does NOT move dimension (a ring at 16 per event is still a line) but DOES control holding together, with a fragmentation threshold near 5 - new; C10 reach is not the story and a fixed reach compounds; C11 A6's condition in its own form (a bare ring fails it, a cubic torus passes it) still converges but on a sparse, spread-out end state; C12 D6 stops the churn (2,143 relations let go against 12,800) and the readings drop to about 3.9, the closest to three, with shapes still not kept. What ED has after twelve attempts: the budgets fixing what CDT tunes (A11 C5, the strongest and still standing, the only genuine reduction); the sync floor measured (A11 C21); patterns holding cheaply at about 4 relations per event against the 6.7 budget; the fragmentation threshold (C7, new); convergence (C11, C12, new); three dimensions from ED NOT established, with attempt 12 having closed the most promising route to it. Census guard: inputs supplied still three, nothing derived, and the starting shape is supplied on top of that in all these models. **Recorded because Allen asked whether his inputs had derailed the work: three of the four largest corrections in this attempt came from him and none came from the numbers** - the audit challenge (A11 D9) exposed four attempts testing a borrowed picture with none of ED's clocks in it; D5 caught Claude having built a global lock into the filter and published the result as a negative about ED; D4 and D6 each changed the model's behaviour measurably, the second by a factor of six in churn. Cost: ten notes, C1-C13, D1-D6, RD1-RD13, five programs, about four hours of computing. Claude's errors, all recorded before their results and all caught before any claim was written up: the inheritance rule that inflated relations (C4), the pass rule charging the reading's size drift to the pattern (C4), the ball counting already-related events so "reach 2" did not mean reach 2 (C10), the instant-lapse dissolution rule (C11, corrected by D6), and the global lock (C8, corrected by D5 - the serious one); plus one result withdrawn on its own check within the hour (the 6.699 match, C5). Open: no fourth candidate correction; what the common end state IS as an object in its own right; A11's two 2+1 options; roads R, I, D; amplitudes; A9 H3-Q2; A10 D15's flip-pairing reading. **The question attempt 12 raises and does not answer: ED's rules take every pattern to one place, and nobody has looked at that place directly - everything so far asked whether it was three-dimensional space and answered no.** Options: (a) conclude A12 and open A13 on the end state - describe what ED's rules actually make [proposed; it needs no new meanings and the object is already in the run data]; (b) conclude and press the budget result further, the one thing that has survived everything; (c) conclude and pause. | CONDITIONAL (stock-take) | `Taking_Stock.md`. | C4, C5, C7, C8, C10, C11, C12; A11 C5, C21, C22, C23, C24 | — |
| C14 | Attempt 12 concluded (D7, RD14) and attempt 13 opened. What attempt 13 inherits, summarised in A13 note 1: the attempts 7-12 thread on one page; the standing results (the budgets fixing what CDT tunes - the only genuine reduction; the sync floor measured; patterns holding on about 4 relations per event below the 6.7 budget; the fragmentation threshold near 5; and convergence itself); the correction that "a shape is carried, not chosen" is NOT carried, having been measured with sync switched off; the meanings settled here and carried (D4 a relation can dissolve, D5 no shared now, D6 a relation persists while it is part of a commitment); what is not established (three dimensions from ED, space derived rather than supplied, where a first shape comes from); inputs supplied 3 plus the starting shape, nothing derived; and the tools. Opening road E, the end state: every test so far asked whether it was three-dimensional space, got no, and moved on, so nobody has looked at what ED's rules actually make - an object reached from a line, a lattice and a random web alike, sparse at about 4 relations per event, spread out, holding its rates patch by patch with no shared now, and chosen by nothing. Road E asks what it is, needs no new meanings, and may be the honest form of the project's result. | CONDITIONAL | `../ED_Attempt_13/Carry_Forward.md`, `../ED_Attempt_13/README.md`. | C13 | — |
