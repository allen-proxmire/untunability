# Claims (ED_Attempt_07)

*Statuses and rules as in `../../ED_Attempt_01/01_Ledger/README.md`. References to earlier attempts are written A1- through A6-ledger.*

| ID | claim | status | evidence | inputs | outside check |
|---|---|---|---|---|---|
| C1 | Reasoning: carried into attempt 7 from A6-ledger C90 and RD30: the meanings of attempts 4–5 (A6 note 1) and those decided in attempt 6 (waves D3, D4, D11; growth D5, D8; three dimensions D6, D7; smoothness D10; time order D22: the pattern is the order itself with space as a slice, growth only adds to the future, a slice does not split or merge, labelled); inputs supplied 3 (Born rule in form, A2 C11; horizon area law, A5 C17; one time dimension, A5 C37); walls: 1 rest frame (risk), 2 dark sector (leakage open), 3 growth as the open problem of a finite-neighbour causal growth rule without branching of space with slice dimension three from sync and commitment, 4 numbers inherited, 5 handle-free rule; also open: spin, charge, particle identification, PF-Q2, discrete-to-continuum step, time-ordered smoothness theory, short waves in the moving sector. Reusable: calibrated readings v2 (6 of 6 test patterns, A6 C70) and the resumable runner; baseline: space grown without time gives rings, rough tangles, stalls or small worlds (A6 C89); known answer: 2D causal growth without branching is a critical random tree of dimension 2 once slices are lines (A6 C87). Cost lessons: time every reading, cost rises steeply with neighbour cap, project from a spread of settings, overnight reboots. Opening road C (causal growth): C1 the causal half checked against 2D CDT's known answer, C2 slice dimension from sync and commitment; suggested order C1 on paper, a small model, then C2. | CONDITIONAL | `Carry_Forward.md`. | A6-ledger C90, RD30 | — |
| C2 | Literature: two-dimensional causal triangulations are in bijection with Galton–Watson trees (Durhuus, Jonsson, Wheater; Krikun and Yambartsev); the uniform infinite causal triangulation corresponds to the critical offspring distribution p(c) = (1/2)^(c+1) conditioned on non-extinction, i.e. Kesten's tree (a single infinite spine with size-biased offspring and independent finite trees grafted on); its fractal dimension is exactly 2 (Sisko, Yambartsev, Zohren, arXiv:1203.2869); it is almost surely recurrent with spectral dimension at most 2, exactly 2 for reduced versions (Durhuus, Jonsson, Wheater, arXiv:0908.3643). | CITED | arXiv:0908.3643, 1203.2869, 1304.4035 (listings); Krikun–Yambartsev and conditioned Galton–Watson tree listings; 2026-09-16. | A6-ledger C87 | — |
| C3 | Reasoning (paper), ED's causal rule for C1: slice t is a circle of L_t events linked to their two slice neighbours (given as a circle for C1, A6 D22 with slice dimension given); each event x links forward to a run of c_x + 1 consecutive events in slice t+1, neighbouring events sharing exactly one future event, so slice t+1 is again one circle in the same cyclic order (space does not split, merge or reorder, A6 D22); c_x independent with p(c) = (1/2)^(c+1) (critical, put in); one spine event per slice has size-biased offspring with one child continuing the spine so the pattern never ends. By construction (firm): parent links form one tree; L_{t+1} = Σ c_x; forward links out of slice t number L_{t+1} + L_t; every link points from slice t to t+1, so the order is strict. | CONDITIONAL | `C1_Causal_Half.md`. | C2, A6-ledger D22 | — |
| C4 | Reasoning (paper), expected results and what C1 puts in: for Kesten's tree with mean-1 offspring of variance σ², expected generation size is 1 + σ²·t, and for p(c) = (1/2)^(c+1) the variance is 2 (arithmetic), so mean slice length ≈ 1 + 2t with fitted slope in [1.8, 2.2]; mass dimension of the spacetime pattern d_H in [1.7, 2.3] (exactly 2, C2; the A6 2D calibration class); walk-return dimension d_s ≤ 2.3 (at most 2, C2); walk dimension reported, expected ≥ 1.8 by d_w = 2 d_H / d_s; structural checks exact (one circle per slice, strict order, forward links = L_{t+1} + L_t, parent links one tree). Put in and named: slice dimension 1 by design (C2 is where it must come out); criticality, the balance CDT gets by tuning its cosmological constant, put in because the known answer needs it, with what balances space's growth in ED a question for C2 (candidate: commitment's cost against sync's pull, A6); no fixed cap on offspring, since a cap lowers the mean below 1 and space dies out, with every count finite (A4 D32 holds, finite not bounded; flagged). Meaning: C1 is a machinery check, not new physics; a pass shows ED's time order plus no splitting gives honest 2D geometry once slices are given, as 2D CDT. | CONDITIONAL | `C1_Causal_Half.md`. | C2, C3, A4-ledger D32, A6-ledger C87 | — |
| C5 | Meaning questions with proposed defaults: C1-Q1 each slice given as a circle of events for C1 (yes); C1-Q2 each event links forward to a run of consecutive next-slice events, neighbours sharing exactly one (yes; the concrete form of no splitting with slice order kept); C1-Q3 new-link count random with average exactly 1 and one surviving spine (yes, known case, flagged as put in); C1-Q4 no fixed cap on forward links in C1, every count finite (yes, flagged). Draft exit rule for the C1 model: pass if every structural check is exact and slice-length slope in [1.8, 2.2], d_H in [1.7, 2.3] and d_s ≤ 2.3, recording 'C1: ED's causal growth with no splitting reproduces 2D CDT: the causal half checked', then C2; a structural failure is a code bug, fixed and rerun with the fix recorded; if readings miss with structure exact, first run a flat causal calibration (every event exactly one offspring, dimension 2) through the same readings: if it passes and the tree does not, record 'C1 not reproduced' and examine why; if it also misses, fix the readings for this pattern type (recorded) and rerun; the growth rule is not changed after seeing results. Options: (a) decide C1-Q1–C1-Q4 and the exit rule; (b) then specify the C1 model (sizes, seeds, readings, timing first) before code. Proposal (a), then (b). Awaiting Allen. | CONDITIONAL | `C1_Causal_Half.md`. | C3, C4 | — |
| C6 | C1-Q1–C1-Q4 accepted and note 2's exit rule confirmed (D3): slices given as circles; forward links to a run of consecutive next-slice events, neighbours sharing exactly one; offspring average exactly 1 with a surviving spine; no fixed cap. The C1 rule and expected results (C3, C4) stand as written. | CONDITIONAL | `C1_Model_Spec.md`. | C3–C5, D3 | — |
| C7 | Specification (paper), the C1 model: build Kesten's tree slice by slice from one root on the spine, ordinary events with offspring (1/2)^(c+1), the spine event size-biased c·(1/2)^(c+1) with one child uniformly continuing the spine; children placed in slice t+1 in parent order around slice t and birth order within a parent (one circle, same cyclic order); in-slice links to the two circle neighbours (one link for a slice of 2, none for 1); the event at position i with children starting at s_i links forward to positions s_i…s_i+c_i (cyclic). Spacetime pattern for readings: all events and links undirected, duplicates merged. Sizes: slice-length check 1,000 tree-only seeds × 200 slices (standard deviation of L_t about 1.4t, so the mean pins the slope to about ±0.1); readings on 10 seeds × 200 slices (about 40,000 events, varying by seed); flat calibration, every event one child, 200 × 200 = 40,000 events. Readings: S1 slope of mean L_t against t for t = 10…200; S2 structure per run (each slice of 3 or more a single cycle, forward links only slice t to t+1, forward links before merging = L_{t+1} + L_t, parent links events − 1 and connected); R1–R5 A6 readings v2 copied unchanged. Expected results written before code: F0 flat d_H and d_s in [1.7, 2.3], d_w in [1.8, 2.2]; E1 structure exact; E2 slope in [1.8, 2.2]; E3 median d_H over 10 seeds in [1.7, 2.3]; E4 median d_s ≤ 2.3; E5 d_w reported, median ≥ 1.8. Exit rule as C5: pass records 'C1: ED's causal growth with no splitting reproduces 2D CDT: the causal half checked', then C2; E1 failure a code bug; F0 failure fixes readings for this pattern type then reruns; F0 pass with E2–E4 miss records 'C1 not reproduced' and examines why (for example edge effects of a growing cone); the growth rule is not changed after results. Running order: readings timing (flat and one tree seed, plan estimate including every reading), flat calibration, slice-length runs, ten seeds with readings; code in `model/`, one result per run. Implementation: readings copied unchanged with hashes; tree seed s own stream, centre seeds 1,000 + s, flat centre seed 999; duplicates merged for readings, S2 counted before merging. No code; nothing run. Awaiting Allen's run. | CONDITIONAL | `C1_Model_Spec.md`. | C2–C6, A6-ledger C70 | — |
| C8 | C1 model run 1 (`model/c1_run.py`, expected results written in note 3 and the script header before running; 1,529 s): F0 flat strip (40,000 events) d_H 1.897, d_s 1.884, d_w 2.065, AS EXPECTED; E2 slice length over 1,000 seeds slope 2.052 (intercept 0.24; mean L 102.6, 203.2, 413.9 at t = 50, 100, 199 against 1 + 2t = 101, 201, 399), AS EXPECTED; E1 structure exact in all 10 seeds (15,819–65,546 events), AS EXPECTED; E3 median d_H 2.166 (2.108–2.203), AS EXPECTED; E4 median d_s 1.893 (1.740–1.954), AS EXPECTED; E5 median d_w 2.412 (2.378–2.648), reported, AS EXPECTED. Exit (note 2's rule, D3): PASS, 'C1: ED's causal growth with no splitting reproduces 2D CDT: the causal half checked'. Meaning: ED's time order with no splitting of space gives honest two-dimensional geometry as 2D CDT does, and the calibrated readings handle this pattern type; slice dimension and the critical balance were put in as planned; a machinery check, not new physics; inputs unchanged (3). Next: C2, the balance (what keeps space from growing or shrinking in ED; candidate commitment's cost against sync's pull) and slice dimension from sync and commitment, on paper first. | CHECKED (all as expected; PASS) | `model/c1_run1.txt`, `.json`, `.log`; run 2026-09-16. | C3–C7, D3, D4 | — |
| C9 | Literature: in 2D CDT the only coupling is the cosmological constant, tuned to its critical value ln 2 for the infinite-volume continuum limit (Ambjørn and Loll, Nucl. Phys. B 536, 407, 1998; CDT reviews); causal maps built from supercritical Galton–Watson trees are hyperbolic, with bi-infinite geodesics, weak Gromov hyperbolicity and positive-speed random walk (Budzinski, EJP 24, 2019, arXiv:1806.10588); subcritical branching dies out; in the self-organized branching process the offspring mean increases after subcritical and decreases after supercritical realizations, driving it to 1 (Zapperi, Lauritsen, Stanley, PRL 75, 4071, 1995); for population-size-dependent branching with offspring means approaching 1 at rate 1/n, Z_n/n converges to a gamma distribution, while m = 1 with convergence no slower than n^-2 dies out with probability 1 (Klebaner, Adv. Appl. Probab. 16, 30, 1984). | CITED | Listings; Klebaner abstract via Cambridge Core; 2026-09-16. | C2 | — |
| C10 | Reasoning (paper) and arithmetic: offspring below average 1 dies out, exactly 1 gives slow growth with smooth finite-dimensional geometry (C1 passed), above 1 gives exponential growth and hyperbolic geometry with no finite dimension (C9), so the balance is a condition for smooth geometry, which is why CDT tunes its cosmological constant. The universe's slice count grows by a fraction of about 3H per unit time; per Planck tick 3·H₀·t_P = 3.53 × 10⁻⁶¹ (H₀ = 67.4 km/s/Mpc = 2.184 × 10⁻¹⁸ s⁻¹, t_P = 5.391 × 10⁻⁴⁴ s), or 2.92 × 10⁻⁶¹ at late times with Ω_Λ = 0.685; so the universe sits slightly above the balance by about one part in 10⁶¹, the excess playing the cosmological constant's role; explaining its size is the cosmological constant problem, which ED does not solve. | CONDITIONAL | `C2_Balance.md`. | C8, C9 | — |
| C11 | Reasoning (paper): two known ways to reach the balance without tuning: (a) feedback, more offspring where sparse and fewer where crowded, self-organizing the mean to 1 (Zapperi et al.); (b) size dependence, a fixed total excess per tick shared over the slice so the per-event excess is c/L, giving linear growth (Klebaner). ED's budget is already feedback (a): commitments use up budget, so crowded regions have fewer offspring and sparse regions more, and it acts locally, fitting ED's lack of a global view better than (b); 'clocks want to sync' pulls the same way (crowded regions' clocks run slow relative to sparse neighbours). The tiny leftover excess would be the cosmological expansion with its value inherited, an association with ED's loci born at a rate (What_ED_Needs row 23). Limits: feedback needs a response strength, a knob counted by the census guard, though the balance itself would then come out rather than be tuned; feedback reaches exact criticality only in certain limits (e.g. slow feedback against fast growth); getting the mean right is not the same as keeping geometry smooth (C1's readings test it); the 10⁻⁶¹ excess is not explained. | CONDITIONAL | `C2_Balance.md`. | C9, C10 | — |
| C12 | Meaning, questions and exit rule: the balance is a condition for smooth geometry; ED's budget meaning is a natural local feedback toward it, which the literature shows can self-organize branching to criticality; if a model confirms it, ED supplies the balance CDT tunes, with the cosmic excess inherited; consistent, not derived, until modelled; inputs unchanged (3). Meaning questions with defaults: C2-Q1 ED must explain the balance rather than put it in (yes); C2-Q2 the balance comes from commitment's budget, fewer offspring where crowded and more where sparse (yes, labelled reading); C2-Q3 the feedback is local to each event's neighbourhood (yes); C2-Q4 the tiny leftover excess is read as the cosmological expansion with its value inherited (yes, labelled association). Draft exit rule: model C2a, C1 with the fixed mean replaced by local budget feedback; pass if without tuning the mean settles at 1, slice length grows slowly, and C1's readings hold (d_H in [1.7, 2.3], d_s ≤ 2.3) across a range of response strengths, recording 'the balance self-organizes from ED's budget: consistent, not derived; the response strength is a knob'; if it dies out, runs away or roughens across the range, record 'balance put in' and go on to slice dimension with the balance as an input; exact ranges fixed in the C2a spec before code; revise-and-retest allowed with reasons recorded. Options: (a) decide C2-Q1–C2-Q4 and the exit rule; (b) spec C2a on paper; (c) go straight to slice dimension with the balance put in. Proposal (a), then (b). Awaiting Allen. | CONDITIONAL | `C2_Balance.md`. | C10, C11 | — |
| C13 | C2-Q1–C2-Q4 accepted (D6): ED must explain the balance; it comes from commitment's budget, fewer offspring where crowded (labelled reading); the feedback is local; the tiny leftover excess is read as the cosmic expansion with its value inherited (labelled association). | CONDITIONAL | `C2a_Model_Spec.md`. | C12, D6 | — |
| C14 | Reasoning (paper): reading A, 'links held use up budget': forward links out of slice t number L_t + L_{t+1} (C1's construction), so the mean number of past links per new event is 1 + L_t/L_{t+1}; shrinking space gives more past links, less budget and fewer offspring, shrinking further, and growth likewise feeds growth, so reading A pushes away from the balance. Reading B, 'budget passed forward, conserved': each event splits its budget equally across its forward links, a new event's budget is the sum received, the slice total B is conserved, average budget per event is B/L_t, and with μ = 1 + k(b/b_ref − 1), b_ref = B/L*, the slice-average mean is 1 + k(L*/L_t − 1), so on average L_{t+1} = L_t + k(L* − L_t): crowded slices get fewer offspring, sparse more; arithmetic stability window 0 < k < 2 (gap shrinks by 1 − k per slice), neutral oscillation at k = 2, growing oscillation for k > 2. Reading B is the precise form of C2-Q2 and reading A its wrong-signed cousin; reading B was chosen after this sign check, recorded as revise-and-retest. Reading B gives the balance without tuning the mean but slices hover at a fixed size L*, a closed tube rather than expansion; growth would need budget creation (the C2-Q4 excess); k and L* are knobs. | CONDITIONAL | `C2a_Model_Spec.md`. | C3, C12, C13 | — |
| C15 | Specification (paper), model C2a: C1's structure (slices as circles, forward runs of c+1, neighbours sharing one, no splitting) with slice 0 of L* events, no spine; reading B offspring geometric with mean max(0, 1 + k(b_x/b_ref − 1)), budget split equally over c_x + 1 forward links and summed on arrival; reading A contrast offspring mean max(0, 1 − k(p_x − 2)) with p_x past links. Sizes: balance runs (no readings) reading B k ∈ {0.5, 1, 1.5, 2.5} × L* ∈ {100, 200} and reading A k ∈ {0.5, 1} × L* = 200, 20 seeds each, T = 400; geometry runs reading B k ∈ {0.5, 1, 1.5}, L* = 200, 3 seeds, T = 200 (about 40,000 events, the size of C1's calibrated flat strip, C8). Readings: B1 survival (no extinction, no slice over 10·L*); B2 mean offspring and mean slice length over slices T/2…T; B3 budget conserved per slice to 10⁻⁹ relative; S2 structure as C1; R1–R5 readings v2. Expected results written before code: E1 structure exact and budget conserved in every run; E2 reading B at k = 0.5, 1, 1.5 and both L*: at least 19 of 20 seeds survive, median mean offspring in [0.98, 1.02], median mean slice length within ±10% of L*; E3 reading B at k = 2.5 fails E2 in a majority of seeds at both L*; E4 reading A majority of seeds die or run away before T; E5 reading B geometry at L* = 200, median over 3 seeds d_H in [1.7, 2.3], d_s ≤ 2.3, structure exact. Exit rule: pass (E1, E2, E5) records 'the balance self-organizes from ED's budget passed forward and conserved: consistent, not derived; k and L* are knobs; space settles as a fixed-size tube, and growth would need budget creation (the cosmic excess, inherited)', then C2 part 2; E3 or E4 not as expected recorded as a stability finding without blocking a pass; E2 or E5 failing records 'balance put in' and goes on to slice dimension with the balance as input, revise-and-retest allowed with reasons; E1 failure a code bug. Running order: timing, balance runs, geometry runs; about 30–40 minutes. Meaning questions with defaults: C2a-Q1 budget passed forward along links and conserved (yes, labelled reading); C2a-Q2 more budget received means more offspring with strength k (yes); C2a-Q3 total budget fixed for C2a, budget creation left for later (yes). No code; nothing run. Awaiting Allen. | CONDITIONAL | `C2a_Model_Spec.md`. | C8, C13, C14 | — |
| C16 | C2a run 1 (`model/c2a_run.py`, expected results written in note 6 and the script header before running; 1,007 s): E1 structure exact and budget conserved in every run, AS EXPECTED; E2 reading B at k = 0.5, 1, 1.5 and L* = 100, 200: 20/20 survived in every setting, median mean slice length 99.8–105.6% of L*, but median mean offspring (average of L_(t+1)/L_t over slices T/2…T) 1.0174, 1.0089, 1.0395, 1.0196, 1.1157, 1.0593, above 1.02 in three settings, NOT AS EXPECTED; E3 reading B k = 2.5: all 20 survived at both L* but slices hovered about 39–40% above L*, failing E2 in every seed, AS EXPECTED; E4 reading A: 40 of 40 seeds ran away, AS EXPECTED; E5 geometry (k = 0.5, 1, 1.5, L* = 200, 3 seeds each): structure exact, medians d_H 2.114, d_s 1.998, d_w 2.199, AS EXPECTED. Exit by the rule as written: 'Balance put in (E2 or E5 not met)'. This verdict stands as the pre-registered outcome. | CHECKED (E2 miss recorded) | `model/c2a_run1.txt`, `.json`, `.log`; run 2026-09-16. | C14, C15, D7 | — |
| C17 | Follow-up diagnostic (not pre-registered, `model/c2a_telescoped_diagnostic.py`), prompted by Allen's observation that his Prime-Triangle angle changes telescope and cancel at any window (Primes repo `PG_Angle_Wobble.md` §3: the angle is to first order 45° − (90/π)ln(p_(n+1)/p_n), a log-ratio, whose signed changes sum to last minus first): the average of ratios used in E2 is biased upward by fluctuations (Jensen; 1.1 × 0.9 = 0.99 while the ratios average 1), whereas log-ratios telescope. On the same settings and seeds, median telescoped offspring exp[(ln L_(T−1) − ln L_(T/2))/(T/2 − 1)] = 0.9993, 0.9999, 0.9999, 1.0001, 1.0003, 1.0002 for reading B at k = 0.5, 1, 1.5 and L* = 100, 200 (ratio of sums agreeing to 10⁻⁴), and 0.9991, 1.0010 at k = 2.5 (which still fails by slice size, about 1.4 L*). So measured without the ratio bias every E2 condition is met for k = 0.5–1.5; the miss came from the statistic, not the balance. C16's verdict stands. | CHECKED (diagnostic) | `model/c2a_telescoped_diagnostic.py`, `model/c2a_telescoped_diagnostic.txt`; run 2026-09-16. | C16 | — |
| C18 | Reasoning (paper), what C2a means: reading B (budget passed forward and conserved) holds the balance with no tuning of the mean: every seed survived, slice size stayed at L* within 6% for k up to 1.5, the telescoped offspring average is 1.000, and the geometry still reads two-dimensional (d_H 2.11, d_s 2.00); the naive reading A ran away every time, confirming the sign check (C14). The pre-registered verdict is 'balance put in' because E2's offspring check used a biased average; with the telescoped measure every E2 condition is met. Adopting the telescoped measure is Allen's revise-and-retest decision, with the prime-triangle reason recorded; if adopted, the recorded result becomes 'the balance self-organizes from ED's budget passed forward and conserved: consistent, not derived; k and L* are knobs; space settles as a fixed-size tube, and growth would need budget creation (the cosmic excess, inherited)'. Options: (a) adopt the telescoped measure and record the balance as self-organizing, then C2 part 2 (slice dimension); (b) keep 'balance put in' and go on with the balance as an input. Inputs unchanged (3). Awaiting Allen. | CONDITIONAL | `C2a_Results.md`. | C14, C16, C17 | — |
| C19 | Revision verdict for C2a (D9, option (a) of C18): E2's offspring check is revised to the telescoped measure (revise-and-retest, prompted by Allen's prime-triangle point, D8; adopted after seeing run 1, recorded), under which every E2 condition is met (C17); C2a is recorded as: 'The balance self-organizes from ED's budget passed forward and conserved: consistent, not derived; k and L* are knobs; space settles as a fixed-size tube, and growth would need budget creation (the cosmic excess, inherited).' C16's pre-registered verdict remains on record. A fresh-seed retest is offered, not run. | CONDITIONAL | `C2_Slice_Dimension.md`. | C16–C18, D8, D9 | — |
| C20 | Literature: CDT builds slice dimension into its simplices; in 4D CDT's de Sitter phase single spatial slices have Hausdorff dimension about 3 but spectral dimension about 1.5, i.e. fractal slices (Ambjørn, Görlich, Jurkiewicz, Loll, arXiv:1111.6938; review arXiv:1905.08669); 2D Euclidean dynamical triangulations have Hausdorff dimension 4 (Ambjørn and Watabiki, NPB 445, 1995); CDT without a preferred foliation, with only local light-cone causality, still gives an extended de Sitter universe in 2+1 dimensions (Jordan and Loll, PLB 724, 155, 2013; PRD 88, 044055, 2013); locally coupled oscillators with random frequencies on d-dimensional lattices have lower critical dimension 4 for phase synchronization and 2 for frequency entrainment (Hong, Park, Choi, PRE 72, 036217, 2005). | CITED | Listings and abstracts as named; 2026-09-16. | A6 C28 | — |
| C21 | Reasoning (paper): C1 and C2a put sync in twice: slices given as circles, and every link joining slice t to t+1 so every chain from slice t to t+s has exactly s steps, i.e. all clocks tick in step by construction (a perfect global sync, as CDT's foliation); Jordan–Loll show the global foliation can be dropped keeping local causality, so in ED a common time across space should be produced by sync rather than assumed. | CONDITIONAL | `C2_Slice_Dimension.md`. | C3, C15, C20 | — |
| C22 | Reasoning and scaling arithmetic (paper): with persistent random clock rates (spread σ, A6 G-Q9) and bounded pull K (G-Q8), a slice is the set of events with equal tick count; influence moves at most one hop per tick, so the slice is space-like (a real 'now') only if tick counts L hops apart differ by less than L. In the strong-coupling steady state K∇²φ = −(ω − ω̄), the wobble W(L) ∝ (σ/K)·L^((4−d)/2) and the tilt W/L ∝ (σ/K)·L^((2−d)/2): d = 1 tilt grows (no 'now' at large scales), d = 2 tilt constant (a 'now' only for small enough σ/K, a ratio), d = 3 tilt shrinks for any σ/K, d ≥ 4 wobble bounded. So a common 'now' without a ratio needs slice dimension ≥ 3; this is A6's rate floor (C29) as geometry in ED's time order. With rates fresh each tick, W² ∝ L^(2−d) and every dimension passes. | CONDITIONAL | `C2_Slice_Dimension.md`. | A6 C29, C20, C21 | — |
| C23 | Reasoning (paper): more slice directions mean more neighbours and links per event and more budget split per link (C2a reading B); commitment favours the fewest directions (A6 G-Q7, labelled lead); the fewest whole-number slice dimension where a common 'now' holds without a ratio is 3, so spacetime is 3 + 1 = 4. | CONDITIONAL | `C2_Slice_Dimension.md`. | C22, A6 C25, C31 | — |
| C24 | Reasoning (paper), limits: the floor needs rate differences persistent along worldlines (fresh-each-tick noise removes it; C2-Q5); d = 2.1 also passes, so three still needs whole-number dimension (A6 G-Q10, assumption Q), and 4D CDT's fractal slices show causality alone does not supply smooth slices; the wobble calculation is the linear strong-coupling regime; nothing here lets a slice change dimension as it grows, so it is a selection among slice dimensions and the growth rule for d-dimensional slices without splitting remains wall 3; the exponents are known on grids, what is new is checking them inside ED's causal budget-balanced pattern. | CONDITIONAL | `C2_Slice_Dimension.md`. | C20, C22, C23 | — |
| C25 | Meaning, questions and exit rule: with time order, 'clocks want to sync' keeps 'now' space-like; with persistent random rates that holds without a ratio only for slice dimension ≥ 3, and commitment's fewest directions with whole numbers gives three, spacetime four; A6's reason for three tied to the causal pattern; consistent, not derived (a selection, whole numbers assumed); inputs unchanged (3), nothing added if the questions hold. Meaning questions with defaults: C2-Q5 rate differences persist along a worldline (yes; A4 R4, G-Q9; if no, sync gives no reason for three); C2-Q6 in a time-ordered pattern sync means a common 'now' holds, tick counts across space never differing by the hop distance at large scales (yes, causal form of G-Q6); C2-Q7 check the floor first with slices given as 1-, 2-, 3-dimensional (yes; known answer first). Draft exit rule: spec model C2b on paper (C2a's budget-balanced causal growth with slices given as d-dimensional tori d = 1, 2, 3, no global clock, persistent random rates, bounded pull, tick count carried forward; reading wobble W(L) and tilt W/L); pass if wobble exponents read near 1.5, 1, 0.5 (ranges fixed in the spec) with tilt growing, constant, shrinking, recording 'A common now needs slice dimension ≥ 3 without a ratio in ED's causal pattern; with commitment's fewest directions and whole numbers, three: consistent, not derived'; if the exponents don't separate, record 'the floor isn't seen in ED's causal pattern' and take stock of road C; expected results fixed in the spec first. Options: (a) decide and spec C2b; (b) paper work on a growth rule for d-dimensional slices without splitting; (c) fresh-seed retest of C2a's revised check. Proposal (a), with (c) if Allen says run. Awaiting Allen. | CONDITIONAL | `C2_Slice_Dimension.md`. | C19–C24 | — |
| C26 | C2-Q5–C2-Q7 accepted and note 8's exit rule confirmed (D10): clock-rate differences persist along a worldline; in a time-ordered pattern sync means a common 'now' holds (tick counts across space never differing by the hop distance at large scales); the floor is checked first with slices given as 1-, 2-, 3-dimensional. C20–C25 stand as written. | CONDITIONAL | `C2b_Model_Spec.md`. | C25, D10 | — |
| C27 | Specification (paper), model C2b: every slice the same given d-dimensional slice (C2a's fixed tube with offspring randomness off, one successor per event), either G a periodic grid (d = 1: n = 64, 128, 256, 512; d = 2: 16, 32, 64, 128; d = 3: 8, 16, 24, 32) or R N = n^d uniform points in the unit d-torus linked within the radius for mean degree 10, connected (redraws counted); event x in slice t+1 has past links to x and its slice neighbours in slice t, no global clock; tick count φ(x,t+1) = φ(x,t) + ω(x) + (K/deg x)·Σ_y tanh(φ(y,t) − φ(x,t)), ω = 1 + σ g persistent, σ = 0.002, K = 0.5, φ = 0 start, stored as φ − t; T = ⌈3 ln(1000)·2d·n²/(4π²K)⌉; control with ω fresh each tick (W² time-averaged over the last third, 300 samples). Readings: wobble W(n) RMS of φ − mean at T; exponent β slope of ln W vs ln n per seed, median of 3 seeds; tilt W/n and tilt ratio largest/smallest n; linearity (largest neighbour difference); settling (rate spread over last tenth, W change 2T/3 to T). Expected results before code: E1 random slices connected with mean degree in [9, 11], grids exact, persistent runs with largest neighbour difference < 0.3, rate spread < 0.01σ, W change < 2%; E2 grids persistent β in [1.35, 1.65], [0.85, 1.15], [0.35, 0.65] for d = 1, 2, 3; E3 random slices persistent β in [1.3, 1.7], [0.8, 1.2], [0.3, 0.7]; E4 persistent tilt ratio > 1.5 (d = 1), in [0.8, 1.25] (d = 2), < 0.67 (d = 3) for G and R; E5 control β in [0.35, 0.65], [−0.1, 0.3], [−0.2, 0.15] with tilt ratio < 0.67 in every d. Exit rule: E1–E4 records 'A common now needs slice dimension ≥ 3 without a ratio in ED's causal pattern; with commitment's fewest directions and whole numbers, three: consistent, not derived'; E2 met and E3 not records 'the floor is seen on grids but not on ED's random slices' and road C takes stock; E2 not met is a calibration or regime problem, revise and retest recorded; exponents not separating (gaps < 0.3 in both G and R) records 'the floor isn't seen in ED's causal pattern' and road C takes stock; E5 not as expected is recorded, not blocking; E1 failing is a code bug. Running order: code with implementation notes, timing trial, then 144 runs. Not tested: slices growing, weak coupling, whole numbers, a rule picking dimension. Implementation questions: C2b-Q1 pull (K/deg)·tanh (yes); C2b-Q2 grids and random slices (yes); C2b-Q3 fresh-each-tick control (yes). No code; nothing run. | CONDITIONAL | `C2b_Model_Spec.md`. | C22, C26 | — |
| C28 | Reasoning (paper), Allen's question whether the wobble bears on the rest frame (wall 1, PF-Q2): a tilt of 'now' by s hops per tick is the simultaneity of a frame moving at speed s in units of the one-hop-per-tick limit, so the wobble is a spread of local frames. With persistent rates the tilt at scale L goes like (σ/K)·L^((2−d)/2): in d = 3 the local frames scatter at short range and converge at long range to one frame, the one in which the clocks' rates match. So sync does not remove the rest frame; it produces it (rather than the slicing putting it in, C21) and makes it the pattern's own clock-matching frame, which bears on PF-Q2 (is the rest frame the pattern's own?) with a 'yes' reading, and it resembles the one physically picked-out large-scale frame, the cosmic rest frame (labelled association). In d = 2 the frame spread stays the same at every scale, in d = 1 no frame survives at large scales. What stays: local Lorentz invariance is wall 1's actual risk and is unchanged; the wobble adds a scale-dependent local frame jitter whose size is set by σ/K, a number ED does not supply (inherited), and which would have to be compared with Lorentz-violation bounds before it counts either way. C2b's tilt readings at several sizes already read the frame spread against scale; a within-slice tilt against distance reading could be added before code, reported and outside the exit rule, if Allen wants. Inputs unchanged (3). | CONDITIONAL | `C2b_Model_Spec.md`. | A6 C90 wall 1, PF-Q2, C21, C22, C27 | — |
| C29 | Specification addendum before code (D11): C2b-Q1–C2b-Q3 accepted (pull (K/deg)·tanh; grids and random slices; fresh-each-tick control) and a frame-jitter reading added, reported only and outside the exit rule: G(r) the RMS tick-count difference at hop distance r within a slice, tilt(r) = G(r)/r (grids: axis offsets r = 1, 2, 4, … ≤ n/2; random slices: hop distances from 20 sources, r = 1, 2, 4, … with at least 100 pairs; persistent at T, control averaged over the 300 samples), summarised as the slope of ln tilt against ln r at the largest size; expected shapes from C22: persistent d = 1 flat, d = 2 slowly falling, d = 3 about r^(−0.5); control d = 1 about r^(−0.5), d = 2 and 3 about r^(−1). | CONDITIONAL | `C2b_Model_Spec.md` (addendum). | C27, C28, D11 | — |
| C30 | C2b code and timing (not results): `model/c2b.py` (slices, causal update, readings), `model/c2b_timing.py`, implementation notes C2b section, all written before any run; `model/c2b_run.py` with expected results and exit rule in its header, resumable (one JSON per run in `model/c2b_runs/`), 5 worker processes, written before running. Timing trial (500 steps per size, kind and noise type, run while the C2a retest shared the CPU): projected 75 min single-process for 144 runs, the largest items being d = 2 n = 128 random slices (about 6 min per run) and d = 1 n = 512 (up to about 1 min); random slices needed up to 9 redraws for connectedness (d = 1, n = 512). Smoke test completed the code path for d = 3 n = 8 R fresh and d = 1 n = 64 G persistent; only output keys were printed. | CHECKED (harness) | `model/c2b_timing.txt`, `model/c2b_timing.log`; 2026-09-16. | C27, C29 | — |
| C31 | C2a run 2, fresh-seed retest of the revised balance check (D9, D10; `model/c2a_run2.py`, expected results in its header before running; 2,031 s): balance seeds 20–39, geometry seeds 103–105, E2's offspring measured telescoped. E1 structure exact and budget conserved, AS EXPECTED; E2 reading B k = 0.5, 1, 1.5 at L* = 100, 200: 20/20 survived in every setting, median telescoped offspring 1.0006, 0.9998, 0.9998, 1.0001, 0.9998, 0.9998, median mean length 0.985, 1.008, 0.996, 1.000, 1.041, 1.050 of L*, AS EXPECTED; E3 k = 2.5: slices 1.397 and 1.392 of L* (18/20 and 20/20 survived), AS EXPECTED; E4 reading A: 40 of 40 ran away, AS EXPECTED; E5 geometry: structure exact, medians d_H 2.105, d_s 1.987, d_w 2.199, AS EXPECTED. Exit: PASS, revised check confirmed on fresh seeds; C19's record stands as a retested result: 'The balance self-organizes from ED's budget passed forward and conserved: consistent, not derived; k and L* are knobs; space settles as a fixed-size tube, and growth would need budget creation (the cosmic excess, inherited).' Inputs unchanged (3). | CHECKED | `model/c2a_run2.txt`, `.json`, `.log`; `C2a_Results.md` (retest section); run 2026-09-16. | C16, C17, C19, D9, D10 | — |
| C32 | C2b run 1 (`model/c2b_run.py`, expected results in note 9 and the runner's header before running; 144 runs, 5 workers, 1,954 s): median wobble exponents β persistent grids 1.398, 0.951, 0.523 (d = 1, 2, 3; per-seed 1.297/2.255/1.398, 0.709/1.153/0.951, 0.355/0.523/0.544), persistent random slices 1.957, 1.198, 0.639, fresh grids 0.421, 0.104, 0.022, fresh random 0.660, 0.124, 0.050; tilt ratios persistent grids 2.493, 0.888, 0.502, persistent random 8.820, 1.394, 0.569, fresh grids 0.309, 0.154, 0.259, fresh random 0.519, 0.160, 0.267; separation ≥ 0.3 in both grids and random slices. E1 NOT AS EXPECTED (grids exact, random slices connected with degree in range, all runs settled with rate spread ≤ 2.0e-5 σ and W change ≤ 0.0013, but 1D random n = 512 seeds 1 and 2 had largest neighbour differences 0.808 and 0.884 > 0.3); E2 AS EXPECTED; E3 NOT AS EXPECTED (d = 1 at 1.957); E4 NOT AS EXPECTED (random d = 2 tilt ratio 1.394 > 1.25); E5 NOT AS EXPECTED (random d = 1 β 0.660 > 0.65; not blocking). Exit by the rule as written: 'E1 failed: fix and rerun (recorded).' Reported only (D11), slope of within-slice tilt against r at the largest size: persistent grids −0.044, −0.233, −0.576; persistent random +0.193, −0.138, −0.430; fresh grids −0.547, −0.864, −0.934; fresh random −0.550, −0.846, −0.915, all matching the expected shapes. Random-slice redraws: 33 (d = 1), 6 (d = 2), 9 (d = 3) over all runs. | CHECKED (E1–E5 misses recorded) | `model/c2b_run1.txt`, `model/c2b_run1.log`, `model/c2b_runs/*.json`; run 2026-09-16. | C27, C29, C30, D12 | — |
| C33 | Reasoning (paper), causes and meaning of C2b run 1: the E1 miss is a regime problem, not a code bug: in 1D random slices sparse stretches concentrate tick-count steps, and at n = 512 two runs reached neighbour differences 0.81–0.88 where tanh saturates and the linear calculation no longer applies (the 0.1 linearity estimate in note 9 was for grids); these runs also raise the 1D random exponent (W 0.42, 3.39, 1.51 across seeds). 1D wobble varies strongly from seed to seed because a few longest modes set it (grid n = 64: W 0.036, 0.069, 0.243), so 3 seeds are too few at d = 1. The random d = 2 tilt ratio miss (1.39) is in the linear regime (largest difference 0.142), so a smaller σ will not change it; d = 2 is the marginal case and it is not yet explained. So far: on grids the floor appears as calculated (1.40, 0.95, 0.52; tilt grows, holds, shrinks), fresh-each-tick rates keep a 'now' in every dimension (the floor comes from persistence), random slices separate the dimensions as clearly (1.96, 1.20, 0.64), and the frame jitter fades as about r^(−0.5) in 3D and not in 1D; but no pass is recorded. Proposed revise-and-retest: R1 σ = 0.0005 (largest difference about 0.22 by linear scaling), R2 10 fresh seeds (3–12); everything else unchanged; about 1.8 h with 5 workers. Options: (a) R1 and R2; (b) R1 only; (c) take the grid result and take stock of road C. Proposal (a). Inputs unchanged (3). Awaiting Allen. | CONDITIONAL | `C2b_Results.md`. | C32 | — |
| C34 | C2b run 2 (D13; `model/c2b_run2.py`, note 9's expected results and exit rule in its header before running; σ = 0.0005, seeds 3–12; 480 runs, 6 workers, 5,377 s): median per-seed β persistent grids 1.322, 1.057, 0.455 (per-seed sd 0.287, 0.123, 0.083), persistent random 1.709, 0.854, 0.566 (sd 0.481, 0.154, 0.116), fresh grids 0.495, 0.108, 0.022, fresh random 0.570, 0.130, 0.045; tilt ratios persistent grids 1.703, 1.079, 0.484, persistent random 5.186, 0.734, 0.539, fresh grids 0.345, 0.158, 0.258, fresh random 0.408, 0.165, 0.265. E1 NOT AS EXPECTED (regime clean: largest neighbour difference 0.164, rate spread ≤ 1.2e-8 σ, W change ≤ 0.0009; but 1D random n = 64 seed 6 mean degree 11.25 > 11); E2 NOT AS EXPECTED (d = 1 at 1.322 < 1.35); E3 NOT AS EXPECTED (d = 1 at 1.709 > 1.7); E4 NOT AS EXPECTED (random d = 2 at 0.734 < 0.8); E5 AS EXPECTED; separation by the rule not met (grid 1D−2D gap 0.265, random 2D−3D gap 0.288). The random-slice seed formula gave identical slices to seeds 3, 4, 5 and 11, 12 (1D n = 512) and 5, 6 and 7, 8 (3D n = 32). Within-slice tilt slopes (reported): persistent grids −0.080, −0.214, −0.565; persistent random +0.261, −0.139, −0.455; fresh grids −0.557, −0.866, −0.934; fresh random −0.545, −0.848, −0.913. Exit by the rule as written: 'E1 failed: fix and rerun (recorded).' | CHECKED (misses recorded) | `model/c2b_run2.txt`, `model/c2b_run2.log`, `model/c2b_runs2/*.json`; run 2026-09-16. | C27, C33, D13 | — |
| C35 | Follow-up diagnostic (not pre-registered, `model/c2b_pooled_diagnostic.py`): the exact linear-regime expectation on the grids from the discrete Laplacian eigenvalues (no simulation), √⟨W²⟩ at σ = 0.0005, has slopes 1.499, 0.987, 0.471 over the run sizes and tilt ratios 2.825, 0.972, 0.480, so the calculation holds at these sizes; the run's statistic, the median of per-seed log slopes from one realisation per size, is noisy in 1D (sd 0.29 grids, 0.48 random) because a few longest modes set W. The pooled measure, slope of ln √(mean over seeds of W²) against ln n (wobbles add as squares; the same averaging-coordinates lesson as C17 and Allen's prime triangles), on run 2's runs: persistent grids 1.456, 1.083, 0.456, persistent random 1.678, 0.897, 0.555, fresh grids 0.483, 0.112, 0.022, fresh random 0.574, 0.124, 0.040; pooled tilt ratios persistent grids 2.582, 1.163, 0.462, persistent random 4.337, 0.811, 0.536; all inside note 9's E2–E5 ranges, gaps 0.37, 0.63 (grids) and 0.78, 0.34 (random); grids within about 0.1 of the exact values; near edges: random d = 2 tilt 0.811, random 2D−3D gap 0.34. The E1 degree miss is a range too tight for N = 64 (mean-degree scatter about ±0.55), not a regime problem; the slice overlaps come from the seed formula when 10 or more redraws are needed (1D n = 512 slices are connected in about 3% of draws). C34's verdict stands. | CHECKED (diagnostic) | `model/c2b_pooled_diagnostic.py`, `model/c2b_pooled_diagnostic.txt`; 2026-09-16. | C34 | — |
| C36 | Reasoning (paper), meaning and options: with the wobble averaged as squares, the floor appears in ED's causal pattern on grids (matching the exact answer) and random slices: wobble about n^1.5, n^1, n^0.5, tilt growing, holding, shrinking, and fresh-each-tick rates keeping a 'now' in every dimension; no pass is recorded because the pre-registered verdict is 'fix and rerun' and the pooled measure was chosen after seeing run 2. Options: (a) revise and retest on fresh seeds 13–22 with (i) E2–E4 read by the pooled measure, (ii) E1 degree range 10 ± 3·√(20/N), (iii) random-slice seed 1000·(seed + 1) + redraw so slices never overlap, all else unchanged, about 1.5 h; (b) adopt the pooled measure on run 2 as a recorded revision without retest; (c) take the grid result and take stock of road C. Proposal (a), the C2a path (C19, C31). Inputs unchanged (3). Awaiting Allen. | CONDITIONAL | `C2b_Results.md` (run 2 sections). | C34, C35 | — |
| C37 | Literature (prior art for C2a's budget reading and road C): energetic causal sets (Cortês and Smolin, 'Energetic Causal Sets', PRD 90, 044035, 2014, arXiv:1308.2206; 'The Universe as a Process of Unique Events', PRD 90, 084007, 2014, arXiv:1307.6167) take fundamental processes as causally connected events whose links carry energy and momentum conserved at events, spacetime emerging only at the semiclassical level, with time-asymmetric dynamics in which new events arise from progenitor pairs by extremizing distinctions between causal pasts, and approximate time-reversal symmetry emerging in simulations. Relation to ED (reasoning): C2a's reading B (a budget passed forward along causal links and conserved) is a close cousin of their conserved link labels; ED's differences as recorded so far are that the budget sets offspring numbers (the balance, C19, C31) and clock rates rather than momentum, and that slices do not split or merge (A6 D22). Name searches for 'commitment dynamics' found no physics framework of that name. | CITED | arXiv abstracts as named; name searches 2026-09-16. | C14, C19, C31 | — |
| C38 | C2b run 3 (D14; `model/c2b_run3.py`, expected results and exit rule in its header before running; seeds 13–22, σ = 0.0005, pooled readings for E2–E4 and separation, E1 degree range 10 ± 3·√(20/N), random-slice seeds 1000·(seed + 1) + redraw; 480 runs, 6 workers, 5,493 s): pooled β persistent grids 1.543, 1.023, 0.424, persistent random 1.677, 0.955, 0.542; per-seed medians persistent grids 1.552, 1.005, 0.435 (sd 0.306, 0.202, 0.071), persistent random 1.700, 0.885, 0.547 (sd 0.572, 0.225, 0.095); fresh medians grids 0.506, 0.107, 0.029, random 0.589, 0.133, 0.033; pooled tilt ratios persistent grids 3.189, 1.051, 0.446, persistent random 3.588, 0.905, 0.526; fresh tilt ratios grids 0.359, 0.156, 0.260, random 0.422, 0.168, 0.262. E1 AS EXPECTED (grids exact, random slices connected with degree in range, largest neighbour difference 0.189, rate spread ≤ 5.0e-8 σ, W change ≤ 0.0015, no shared slices); E2 AS EXPECTED; E3 AS EXPECTED; E4 AS EXPECTED; E5 AS EXPECTED; pooled gaps 0.520, 0.599 (grids) and 0.722, 0.413 (random). Within-slice tilt slopes (reported): persistent grids −0.046, −0.223, −0.558; persistent random +0.226, −0.195, −0.430; fresh grids −0.554, −0.866, −0.934; fresh random −0.541, −0.851, −0.916. Exit: PASS, 'A common now needs slice dimension ≥ 3 without a ratio in ED's causal pattern; with commitment's fewest directions and whole numbers, three: consistent, not derived.' | CHECKED | `model/c2b_run3.txt`, `model/c2b_run3.log`, `model/c2b_runs3/*.json`; run 2026-09-16. | C27, C29, C34–C36, D14 | — |
| C39 | Reasoning (paper), what C2b means: in ED's causal pattern with persistent clock rates and bounded pull, a synced 'now' holds at large scales without a tuned ratio only for slice dimension ≥ 3 (1D tilt grows, 2D holds at a ratio-set value, 3D flattens), on grids (matching the exact answer, C35) and random slices; fresh-each-tick rates keep a 'now' in every dimension, so the floor comes from persistence (C2-Q5); with commitment's fewest directions, three (C23); the within-slice frame jitter fades with distance only in 3D, so one large-scale rest frame comes out of sync (C28). Consistent, not derived: slices given, not grown (C2-Q7); whole-number dimension assumed (A6 G-Q10); strong coupling only; the growth rule for d-dimensional slices without splitting remains open (wall 3). Road C now has: C1 causal growth reproducing 2D CDT; C2a the balance from budget passed forward (C31); C2b the slice-dimension floor seen in the causal pattern. Three runs recorded in full (run 1 regime miss, run 2 averaging miss, run 3 pass on fresh seeds). Working label 'Synced Now' (D15). Inputs unchanged (3). Next options: (a) take stock of road C and attempt 7; (b) on paper, a growth rule for d-dimensional slices without splitting; (c) weak coupling or grown 1D slices from C2a. | CONDITIONAL | `C2b_Results.md` (run 3 sections). | C22–C24, C28, C38, D15 | — |
| C40 | Literature (check on C2b's reading): lower critical dimensions for oscillator synchronization are known (frequency entrainment 2, phase 4; Hong, Park, Choi, PRE 72, 036217, 2005; renormalization-group and 2D-array studies, arXiv:0810.3075, 0711.3775); this search found no paper using them to explain why space has three dimensions or tying the rate floor to a synchronized 'now' staying space-like (a statement about this search, not a novelty claim); nearest relatives: energetic causal sets (C37), Hogan's exotic transverse correlations with Planck-scale drift between clock phases (arXiv:1607.03048), CDT without preferred foliation (C20). | CITED | Listings and abstracts; searches 2026-09-16. | C20, C37, C39 | — |
| C41 | Reasoning (paper), stock-take of road C and attempt 7: reached C1 (causal growth without splitting reproduces 2D CDT, pass, 1 run), C2a (Budgeted Causality: budget passed forward and conserved gives the balance, pass after one averaging revision, retested on fresh seeds, 2 runs), C2b (Synced Now: a synced 'now' needs slice dimension ≥ 3 without a ratio in ED's causal pattern, pass on run 3 after a regime fix and an averaging fix, 3 runs), and the frame reading (one large-scale rest frame produced by sync, C28). Still put in: slices given; whole-number dimension; commitment's fewest directions (labelled lead, not modelled); knobs k, L*, K, σ; strong coupling only; the cosmic excess. Walls: rest frame unchanged locally with a new large-scale reading; dark sector and handle-free rule untouched; growth has model support for smooth causal growth, the balance, and sync selecting three among given slices, but no rule grows a 3D slice; numbers inherited. Inputs 3 unchanged; What_ED_Needs row 6 now has three consistent reasons. Assessment: the growth wall moved with model support for the first time, the parts fit one picture from existing meanings, three dimensions has a drawable picture resting on independently known physics checked in ED's pattern, and the record is clean; limits: consistent not derived, C1 and C2b confirm known mathematics in ED's setting (the ED content is the readings and their fit), the slice-growing piece is open, no number or observational difference. Options: (a) road C3 on paper, growing a 2D slice without splitting; (b) checks on C2b (weak coupling, rates on C2a's grown circles); (c) plain-language attempt 7 write-up with the working names, possibly a repo note; (d) conclude attempt 7. Proposal (c) then (a). Awaiting Allen. | CONDITIONAL | `Taking_Stock.md`. | C1–C40, D1–D15 | — |
| C42 | Literature (road C3): random triangulated surfaces have Hausdorff dimension 4 and spectral dimension 2 (Ambjørn–Watabiki 1995, C20); restricting vertex degrees to 5, 6, 7 does not change the critical behaviour (Bowick, Catterall, Thorleifsson, hep-lat/9608076); DT surfaces with higher curvature terms show crumpled, flat and branched-polymer states at finite size, with a crossover between flat and fractal (listings); Euclidean DT in 3D and 4D has a crumpled phase (Hausdorff dimension diverging) and a branched-polymer phase (Hausdorff 2, spectral 4/3; Jonsson–Wheater NPB 515, 1998; hep-lat/0306030 listing); CDT's time slicing gives a good phase but 4D CDT slices remain fractal (C20); slices of 3D causal triangulations are in bijection with certain coloured 2D cell complexes (Durhuus–Jonsson, arXiv:1712.07502); 2+1 CDT with torus slices (Budd–Loll, PRD 88, 024015, 2013); peeling and Eden-type processes explore existing random maps (Budd arXiv:1506.01590; Curien notes); this search found no study of surfaces grown by local vertex splits with curvature or sync feedback. | CITED | Abstracts and listings as named; 2026-09-16. | C20 | — |
| C43 | Reasoning and counting (paper): for a slice of more than one dimension, no splitting or merging means fixed topology; for a surface (torus), the local moves are the vertex split (two neighbours u, w of v cut v's neighbour ring into two arcs, one per child; children linked to each other and to u and w; +1 vertex, +3 edges, +2 faces, Euler characteristic unchanged; children's degrees sum to deg v + 4, u and w gain one each) and its inverse, the edge collapse, allowed only under the link condition, which is what prevents pinching or splitting; average degree on a torus is exactly 6. C2a's reading B carries over (budget over forward links, offspring following budget, count settling at L*); the new choice is where children go (which u, w), which sets degrees and so curvature 6 − degree. | CONDITIONAL | `C3_Growing_A_Slice.md`. | C3, C14, C31, A6 D22 | — |
| C44 | Reasoning (paper): size is handled by the budget but shape is not: degrees wander under splits and collapses, random surfaces are fractal by default and degree bounds do not prevent it (C42). Candidates from ED's meanings: (i) uniform choice (least structure; fractal in equilibrium, unknown for growth); (ii) quadratic curvature cost preferring degrees near 6 (A6 S-Q2; flat up to a crossover in equilibrium, unknown for growth); (iii) sync favouring splits that help rates match (in 2D sync is marginal by C2b, so no large-scale flattening push). A 2D slice is the smallest test of the growth machinery, not a case ED's sync result expects to be flat at the largest scales; the moves generalize to 3D (bistellar moves). | CONDITIONAL | `C3_Growing_A_Slice.md`. | C38, C39, C42, C43 | — |
| C45 | Reasoning (paper), a lead: random 3D geometry's two bad phases each break one ED meaning: the crumpled phase (everything near everything, huge-degree vertices) breaks commitment (each link costs) and no infinities; the branched-polymer phase (spectral dimension 4/3 ≤ 2) breaks sync (rates cannot match, a synced now cannot hold). So in 3D, commitment and sync between them rule out both bad phases, leaving the middle where CDT finds its good phase; in 2D sync does not rule out the branched side. A lead on paper only; it counts under the census guard only if a model grows a 3D slice with no dimension or smoothness put in. | CONDITIONAL | `C3_Growing_A_Slice.md`. | C20, C39, C42, A6 C28 | — |
| C46 | Meaning, questions and exit rule: road C3's problem is specific (size by budget, topology by splits and link-condition collapses, shape open); random surfaces and 3D geometries are fractal or branched by default; ED offers quadratic curvature cost and, in 3D, sync plus commitment ruling out both bad phases; a 2D slice is the cheapest machinery test; consistent, not derived; inputs unchanged (3). Meaning questions with defaults: C3-Q1 no splitting or merging for a multi-dimensional slice means fixed topology with vertex splits and link-condition collapses as the moves (yes); C3-Q2 offspring follow C2a's budget, no child a collapse, extra children splits (yes); C3-Q3 curvature cost quadratic in (6 − degree) for a surface (yes, labelled reading of S-Q2); C3-Q4 uniform choice run as baseline (yes); C3-Q5 start with a 2D torus as machinery test with 3D the target (yes). Draft exit rule: spec model C3a (torus slice grown by C2a's budget with splits and link-condition collapses; uniform choice and quadratic cost λ ∈ {0.5, 1, 2}; L* about 400, 1,600, 6,400; readings slice mass and spectral dimension, degree spread and its growth with size, spacetime readings); uniform flat records 'growth alone keeps a surface flat'; only the cost flat at all sizes records 'quadratic energy keeps a grown surface flat at the sizes run: consistent, not derived; λ a knob'; flat only to a λ-set size records 'flat up to a crossover, as in equilibrium; 2D not reached at large scales' and goes to 3D on paper; neither records 'growing a flat 2D slice isn't reached with ED's local moves' and takes stock; structure checks exact; expected results fixed in the spec first. Options: (a) decide and spec C3a; (b) 3D on paper; (c) deeper literature step. Proposal (a). Awaiting Allen. | CONDITIONAL | `C3_Growing_A_Slice.md`. | C42–C45 | — |
| C47 | C3-Q1–C3-Q5 accepted and note 12's exit rule confirmed (D18): a multi-dimensional slice keeps its topology, with vertex splits and link-condition collapses as the only moves; offspring follow C2a's budget (no child a collapse, extra children splits); curvature cost quadratic in (6 − degree), labelled reading of S-Q2; uniform choice as baseline; a 2D torus as machinery test with 3D the target. C42–C46 stand as written. | CONDITIONAL | `C3a_Model_Spec.md`. | C46, D18 | — |
| C48 | Reasoning and arithmetic (paper): on a surface, an event with c ≥ 1 children shares its budget equally among them and a childless event passes its whole budget to the neighbour it collapses into (conserved, local); with mean offspring μ = 1 + k(b − 1), per-child budget near b/μ, so b′ ≈ b/(1 + k(b − 1)) with slope 1 − k at b = 1: stability window 0 < k < 2 as in C14; C3a uses k = 1. | CONDITIONAL | `C3a_Model_Spec.md`. | C14, C31, C47 | — |
| C49 | Specification (paper), model C3a: triangulated torus slice starting as a flat triangular grid n × n (n = 20, 40, 80; L* = 400, 1,600, 6,400; degree 6, budget 1); each tick offspring c geometric with mean max(0, 1 + k(b − 1)), k = 1; slice t+1 a copy with each event as its first child; collapses first in random order (c = 0 merges into a neighbour allowed by the link condition: exactly two shared neighbours each of degree ≥ 4; otherwise a counted forced keep; budget to the partner); then splits in random order (c − 1 splits of the newest child, choosing distinct neighbours u, w that cut the ring into two arcs; budget shared equally); forward links to children and to the absorbing child. Choice rules: U uniform; Q with weights e^(−λΔE), E = Σ(degree − 6)² over touched events, λ ∈ {0.5, 1, 2}. Stop outside [L*/10, 10 L*]. Runs: 12 settings × seeds 0–2, T = 1,000 (may be lowered by the timing trial before any run, recorded); calibrations F (flat n = 80 torus) and R (6,400-event torus randomized by 200 sweeps of uniform edge flips, seed 7). Readings: slice d_H, d_s by readings v2 at T/4, T/2, 3T/4, T; global D = 1/slope of ln mean eccentricity (20 random events) against ln L*; settled if median |d_H(T) − d_H(3T/4)| ≤ 0.15; degree spread and mean (degree − 6)²; balance and forced keeps; spacetime readings at L* = 400 reported only; structure checks every tick exact (V − E + F = 0, every edge in two triangles, every neighbour set one ring, no repeated links, budget to 1e-9). Expected results before code: E0 F reads d_H, d_s in [1.7, 2.3] and R reads d_H > 2.6; E1 structure exact, budget conserved, all survive, mean size within ±10% of L*; E2 U not flat at L* = 6,400 (low confidence); E3 Q2 flat at all sizes with D in [1.7, 2.4] (low confidence); E4 all Q runs settled. Exit rule: E1 fail code bug; E0 fail readings/sizes revision; decisive runs unsettled 'not settled in T ticks'; then U flat at all sizes with D in range 'growth alone keeps a surface flat'; else some Q flat at all sizes with D in range 'quadratic energy keeps a grown surface flat at the sizes run: consistent, not derived; λ a knob' then 3D on paper; else some Q flat at 400 only 'flat up to a crossover, as in equilibrium; 2D not reached at large scales' then 3D on paper; else 'growing a flat 2D slice isn't reached with ED's local moves' and take stock. Running order: code with implementation notes, timing trial (20 ticks per size for U and Q2, readings on one slice per size; lower T if over about 3 h on 6 workers, recorded), calibrations, growth runs resumable. Implementation questions: C3a-Q1 budget shared among children or passed to the absorbing neighbour (yes); C3a-Q2 collapses then splits in random order with counted forced keeps (yes); C3a-Q3 weights e^(−λΔE) for both moves (yes); C3a-Q4 flat start with settling check and two calibrations (yes). No code; nothing run. | CONDITIONAL | `C3a_Model_Spec.md`. | C43, C44, C47, C48 | — |
| C50 | C3a code and timing (not results): `model/c3a.py` (torus rotation system, vertex split, link-condition collapse with the degree ≥ 3 safeguard, Metropolis-corrected uniform edge flips for calibration R, per-tick structure check of degree sum 6V, ring validity and triangle orientation, budget reading on a surface, choice rules U and Q, spacetime builder, slice readings and eccentricity), `model/c3a_timing.py`, implementation notes C3a section, all written before any run. Harness test (structure only): flat torus, 300 random moves, 10 growth ticks each for U and Q1, and flip sweeps all passed every structure check with budget conserved. Timing trial (20 ticks from the flat start, check included): tick U 6.8, 27.4, 111.9 ms and Q2 19.5, 79.7, 354.3 ms at L* = 400, 1,600, 6,400; slice readings 0.4 s (1,600) and 5.5 s (6,400); spacetime readings 21.7 s on 39,778 events; flips 0.1 s per sweep at 6,400; projection for note 13's runs 1.41 h single process, 0.23 h on 6 workers. Harness finding: **slice readings are undefined at L* = 400** (r_max 2, fewer than 3 radii; a flat 20 × 20 torus has only balls of up to 37 events under the N/10 limit), so note 13's smallest size cannot give the 'flat at a size' test the exit rule uses; at 1,600 and 6,400 the readings are defined (r_lo–r_max 3–5 and 5–9). Tick times were measured on early ticks and could rise if degrees spread under U. Proposed revision before any run: sizes n = 40, 80, 160 (L* = 1,600, 6,400, 25,600), calibrations F and R at every size, 'flat at L* = 400' in the exit rule read as the smallest size; all else unchanged; estimated about 1 h on 6 workers. Awaiting Allen. | CHECKED (harness) | `model/c3a_timing.txt`; 2026-09-16. | C49, D19 | — |
| C51 | C3a run 1 (`model/c3a_run.py`, expected results and exit rule in note 13 with its pre-run revision and in the runner's header; 42 jobs, 6 workers, 10,289 s): calibrations F d_H 1.778, 1.901, 1.950 and d_s 2.002, 2.000, 2.000 at L* = 1,600, 6,400, 25,600 (eccentricity 26.0, 53.0, 106.0); R d_H 2.379, 2.690, 3.131, d_s 1.821, 1.823, 1.905, degree sd 3.17–3.26 (eccentricity 13.6, 23.75, 28.65). Growth medians at T (d_H; d_s; mean (deg − 6)²; max degree; eccentricity): U 2.544, 2.983, 3.197; 1.746, 1.893, 1.992; 13.05, 12.71, 12.36; 47, 58, 79; 14.35, 20.85, 32.50; D 3.392. Q0.5 2.186, 2.473, 2.760; 1.812, 1.949, 1.772; 2.21, 2.19, 2.19; 12, 11, 13; D 3.515. Q1 2.230, 2.478, 2.697; 1.807, 1.806, 1.834; 1.90, 1.92, 1.88; 11, 12, 13; D 2.584. Q2 2.149, 2.481, 2.662; 1.770, 1.801, 1.804; 1.80, 1.78, 1.78; 12, 11, 12; D 3.326. Settle changes Q ≤ 0.056 (all settled), U 0.045 and 0.131 at the larger sizes, undefined at 1,600 (readings undefined at two checkpoints). Spacetime readings at 1,600 (reported): U d_H 3.474, d_s 3.053; Q0.5 3.387, 2.812; Q1 3.296, 2.836; Q2 3.175, 2.583. All runs survived, late size 0.997–1.003 of L*, no forced keeps. E0 NOT AS EXPECTED (R at 1,600 reads 2.379 ≤ 2.6); E1 AS EXPECTED; E2 AS EXPECTED; E3 NOT AS EXPECTED (Q2 flat at 1,600 only); E4 AS EXPECTED. Exit by the rule as written: 'E0 failed: readings or sizes revision (recorded).' Run time 2.9 h against the 1 h estimate (readings at 25,600 about 100 s each, not timed in the trial). | CHECKED (E0, E3 misses recorded) | `model/c3a_run1.txt`, `model/c3a_run1.log`, `model/c3a_runs/*.json`; run 2026-09-16/17. | C49, C50, D20 | — |
| C52 | Reasoning (paper), diagnostic reading of C3a (not pre-registered): at 1,600 the readings barely separate flat from random (R 2.38), so the Q rules' 'flat at 1,600' (2.15–2.23) cannot be taken as flat. At 6,400 and 25,600, where both calibrations pass: U reads like the random surface (2.98, 3.20 against 2.69, 3.13); the quadratic cost suppresses local curvature (mean (deg − 6)² about 1.8–2.2 against about 12.5, largest degree 11–13 against 47–79) but every Q rule reads between flat and random and roughens with size (2.47–2.48, then 2.66–2.76), with λ barely mattering; the pattern of Bowick–Catterall–Thorleifsson for equilibrium surfaces (local curvature suppression does not prevent large-scale roughness, C42). Applying the exit rule at the calibrated sizes gives 'growing a flat 2D slice isn't reached with ED's local moves'; including 1,600 would give 'flat up to a crossover'; both agree 2D is not reached at large scales. Consistent with note 12 (C44): in 2D sync is marginal and gives no large-scale flattening push, and a local cost acts locally; something must act at large scales, which in 3D is what the sync floor does (C39, C45). Options: (a) revise the sizes to the calibrated ones (6,400, 25,600) and apply the exit rule to these runs, recording 'growing a flat 2D slice isn't reached with ED's local moves; the quadratic cost flattens locally but not at large scales' (recorded revision, no rerun, wide margins), then 3D on paper with sync as the large-scale ingredient; (b) a larger run (102,400) to test for a crossover, about 10 h or more; (c) take stock of road C3. Proposal (a). Inputs unchanged (3). Awaiting Allen. | CONDITIONAL | `C3a_Results.md`. | C42, C44, C45, C51 | — |
| C53 | Revision verdict for C3a (D21, option (a) of C52): sizes revised to the two where both calibrations pass (6,400 and 25,600), a recorded revision after seeing the run with no rerun (margins wide); applying note 13's exit rule to run 1's runs, no rule is flat at 6,400 (U 2.983; Q 2.473–2.481) or 25,600 (U 3.197; Q 2.662–2.760), giving the last row: 'Growing a flat 2D slice isn't reached with ED's local moves; the quadratic curvature cost flattens locally but not at large scales.' C51's pre-registered verdict remains on record. | CONDITIONAL | `C3a_Results.md` (decision section). | C51, C52, D21 | — |
| C54 | Literature (road C3, 3D): 3D dynamical triangulations have crumpled and branched-polymer phases separated by a first-order transition, the line ending at a critical end point with an added term (hep-lat/9712011, hep-lat/9802021 listings); 3D CDT has an extended phase (C20); growing simplicial complexes give hyperbolic small-world geometry with finite spectral dimension (Bianconi–Rahmede, Sci. Rep. 7, 41974, 2017); adaptive Kuramoto networks reinforcing synchronized links under limited connection resources form modular structure (Gutiérrez et al., PRL 107, 234103, 2011); triangulations of the same 3-manifold are connected by Pachner moves (1–4, 4–1, 2–3, 3–2), and an edge contraction in a closed PL manifold preserves topology iff Lk(a) ∩ Lk(b) = Lk(ab) (Dey, Edelsbrunner, Guha, Nekhayev). | CITED | Listings and abstracts as named; 2026-09-17. | C20, C42 | — |
| C55 | Reasoning (paper): a 3D slice is a 3-torus triangulation of fixed topology (C3-Q1); a childless event is an edge contraction under the link condition, extra children a vertex split (cutting the link sphere into two discs), and 2–3 and 3–2 flips rewire without changing the count as ED's cut-and-rejoin (A6 D5, D8), flagged that they change link number by one so 'in pairs' must be read as cut-and-rejoin; the budget works as in C3a (C48). | CONDITIONAL | `C3_3D.md`. | C43, C48, C54 | — |
| C56 | Reasoning and arithmetic (paper), refining C45: random 3D geometry has three bad shapes, each breaking a different existing ED meaning: crumpled (huge degrees) broken by commitment (each link costs) and no infinities; hyperbolic small world (bounded degree, exponential volume, negative curvature everywhere; Bianconi–Rahmede, A6's small worlds) broken by quadratic energy, since in 3D curvature sits on edges and edge valence above the flat value 2π/arccos(1/3) = 5.104 is negatively curved, so a quadratic cost in (edge degree − 5.10) penalizes it; branched polymer (thin necks, spectral dimension 4/3) broken by sync (C57); what is left is flat and three-dimensional. C3a's lesson applies to the curvature row: a local cost alone does not act at large scales, so sync must supply large-scale information. | CONDITIONAL | `C3_3D.md`. | C45, C53, C54 | — |
| C57 | Reasoning and scaling (paper): the tick-count field is set across the whole slice (the steady state of C2b's update), so the difference across one link carries information about the region behind it; a region of n events hanging off a neck of m links has persistent-rate surplus about σ√n against at most about K·m pull, so it stays locked only if m ≳ (σ/K)√n. Neck size: flat 3D m ~ n^(2/3) ≫ √n (no sync pressure); flat 2D m ~ n^(1/2) (borderline, set by σ/K, consistent with C3a finding no large-scale push); branched m ~ 1 (pressure grows with branch size). A growth rule favouring moves that reduce tick differences across links therefore widens exactly the necks that matter and switches off in a flat 3D slice: a large-scale restoring force built from local readings, the time-ordered form of A6 G-Q11. | CONDITIONAL | `C3_3D.md`. | A6 C29, C22, C39, C53, C56 | — |
| C58 | Reasoning (paper), limits: in equilibrium 3D triangulations the crumpled and branched phases meet at a first-order transition with no smooth phase between, and CDT finds one only with a time slicing, so whether ED's three meanings open a real middle is not known; commitment (toward few links, i.e. branched), sync (against necks) and curvature cost (toward edge valence 5.10) must balance, and their relative strengths are knobs (census guard); the curvature cost may act only locally as in 2D; whole-number dimension still assumed; 3D slices large enough to read (about 30³ events) may cost many hours per run in Python, so a cheaper static check comes first. | CONDITIONAL | `C3_3D.md`. | C54–C57 | — |
| C59 | Meaning, questions and exit rule: in 3D, ED's existing meanings line up one-to-one with random geometry's bad shapes (commitment against crumpling, quadratic energy against hyperbolic, sync against branching), and sync is the only one acting at large scales, through the tick-count field, zero in a flat 3D slice, borderline in 2D, growing across thin necks; consistent, not derived; a lead on paper; inputs unchanged (3). Meaning questions with defaults: C3-Q6 3D moves are link-condition edge contraction and vertex split, with 2–3 and 3–2 flips as cut-and-rejoin (yes, 'in pairs' flag); C3-Q7 sync acts on growth by favouring moves that reduce tick-count differences across links (yes, labelled reading, causal form of G-Q11); C3-Q8 commitment penalizes degree (yes); C3-Q9 3D curvature cost quadratic in (edge degree − 5.10) (yes, labelled reading); C3-Q10 a cheap static check C3b on stand-in slices first (yes). Draft exit rule for C3b: stand-ins flat 3D grid, branched (3D blocks in a random tree joined by thin necks), hyperbolic (bounded degree, exponential volume growth), crumpled (degrees growing with size), several sizes; signals: sync wobble and largest single-link tilt against size, links per event, volume growth exponent; pass if sync tilt grows with size only on branched, link cost grows only on crumpled, volume grows exponentially only on hyperbolic, flat flagged by none, recording 'each bad 3D shape is flagged by one existing ED meaning: consistent, not derived' then spec 3D growth C3c; a miss or a flag on flat records which meaning fails which shape and takes stock of road C3; ranges fixed in the C3b spec first. Options: (a) decide and spec C3b; (b) straight to a 3D growth spec; (c) take stock of road C3 and attempt 7. Proposal (a). Awaiting Allen. | CONDITIONAL | `C3_3D.md`. | C54–C58 | — |
| C60 | C3-Q6–C3-Q10 accepted and note 15's exit rule confirmed (D22): 3D moves are link-condition contraction, vertex split and 2–3/3–2 flips as cut-and-rejoin ('in pairs' flag); sync favours moves reducing tick-count differences across links (labelled reading); commitment penalizes degree; 3D curvature cost quadratic in (edge valence − 5.10) (labelled reading); a cheap static check C3b first. C54–C59 stand as written. | CONDITIONAL | `C3b_Model_Spec.md`. | C59, D22 | — |
| C61 | Specification (paper), static check C3b: stand-ins F periodic cubic grid n³ (n = 10, 20, 30), FR 3-torus random geometric graph with mean degree 14 (connected, redraws counted), B 4×4×4 grid blocks joined in a uniform random (Prüfer) tree with one link per tree edge between random events of the two blocks (16, 128, 432 blocks: 1,024, 8,192, 27,648 events), H random 6-regular simple connected graph, Cr random graph with mean degree √N connected; N ≈ 1,000, 8,000, 27,000; construction seeds 0–2, rate seeds 100 + seed. Signals: sync from the exact linear steady state of C2b's update, K(D − A)φ = deg∘(ω − Ω) with Ω the degree-weighted mean, sparse solve with σ/K = 1, largest single-link |φ_x − φ_y|, neck tilt ratio = pooled (RMS over seeds) largest link difference at the largest size over the smallest, with B's share of maxima on neck links and pooled wobble reported; commitment degree ratio (mean degree largest over smallest size) with max degree reported; curvature proxy readings v2's small_world flag at the largest size, d_H reported. Expected results before code: E1 all connected, F degree 6, FR mean degree in [13, 15], B one link per tree edge, H exactly 6-regular simple, Cr mean degree within ±10% of √N; E2 neck tilt ratio ≥ 3 for B and ≤ 1.5 for F, FR, H, Cr; E3 degree ratio ≥ 3 for Cr and ≤ 1.2 for F, FR, B, H; E4 exponential flag true for H and false for F, FR, B at the largest size (Cr reported, flag allowed). Extra flags on other bad shapes allowed and reported. Exit rule: E1 fails code or construction bug; E2–E4 all as expected records 'Each bad 3D shape is flagged by one existing ED meaning, and a flat 3D slice by none: consistent, not derived' then spec 3D growth C3c; a meaning missing its shape records '<meaning> doesn't flag <shape>' and takes stock of road C3; a meaning flagging F or FR records '<meaning> flags a flat 3D slice' and takes stock. Running order: code with implementation notes, timing trial (one seed per stand-in and size), 45 jobs resumable. Not tested: growth, balance of pressures, edge valence on triangulations, nonlinear unlocking. Implementation questions: C3b-Q1 the five stand-ins (yes); C3b-Q2 sync signal from the exact linear steady state (yes); C3b-Q3 exponential-growth flag as curvature proxy (yes, recorded as proxy); C3b-Q4 extra flags allowed, only a missed own shape or a flag on flat counts against (yes). No code; nothing run. | CONDITIONAL | `C3b_Model_Spec.md`. | C56, C57, C60 | — |
| C62 | C3b code and timing (not results): `model/c3b.py` (five stand-in builders: periodic cubic grid; 3-torus random geometric graph with mean degree 14 and connected redraws; 4³ open grid blocks in a Prüfer random tree with one random link per tree edge; configuration-model 6-regular graph repaired by partner swaps to simple and connected, close to but not exactly uniform; G(N, m) with mean degree √N; exact linear steady state by grounded conjugate gradient or direct solve with residual recorded; degree stats; ball-growth readings), `model/c3b_timing.py`, implementation notes C3b section, written before any run. First timing attempt: readings v2's walk part took 301 s on B at 8,192 events (hours projected at 27,648, diffusion on tree-like graphs); the attempt was stopped and `readings` changed, before any run and with no signal values seen, to the ball-growth part only (`mass_and_cut`: d_H, fit qualities, r_max, r_lo, small_world flag), which is all E1–E4 and note 16's reported d_H use. Second timing attempt: every stand-in at every size connected; F degree 6; FR mean degree 14.19, 13.90, 14.01; B tree edges 15, 127, 431 for 16, 128, 432 blocks, mean degree 4.53; H exactly 6-regular; Cr mean degree 31.62, 89.44, 164.32 (√N 31.6, 89.4, 164.3); solve residuals ≤ 1.6e-12; no redraws; ball readings undefined at N ≈ 1,000 for F, FR, H, Cr and at 8,000 for H and Cr (small radius ranges; E4 uses the largest size only); projection 1.4 min single process for 45 jobs. | CHECKED (harness) | `model/c3b_timing.txt`, `model/c3b_timing.log`; 2026-09-17. | C61, D23 | — |
| C63 | C3b run 1 (`model/c3b_run.py`, note 16's expected results and exit rule in its header before running; 45 jobs, 6 workers, 47 s): pooled (RMS over seeds 0–2) largest single-link tick difference at N ≈ 1,000/8,000/27,000 (B 1,024/8,192/27,648), neck tilt ratio, mean degree ratio, exponential-growth flag at the largest size (seeds flagged), d_H at the largest: F 5.900, 6.812, 7.882, ratio 1.34, degree 6.00 (ratio 1.00), flag 0/3, d_H 2.801; FR 6.395, 7.980, 8.926, ratio 1.40, degree 14.14, 13.93, 13.99 (0.99), 0/3, d_H 2.943–2.947; B 116.1, 383.3, 481.0, ratio 4.14, degree 4.53 (1.00), 0/3, d_H 1.731–1.894, largest difference on a neck link in 9 of 9 runs; H 5.647, 6.384, 7.033, ratio 1.25, degree 6.00 (1.00), 3/3, d_H 4.596; Cr 5.925, 6.647, 6.780, ratio 1.14, degree 31.62, 89.44, 164.32 (5.20), 3/3, d_H undefined. Pooled wobble exponent in N: F 0.175, FR 0.156 (0.53 and 0.47 in side length, as C2b's 3D value), B 0.814, H −0.009, Cr −0.019. E1 AS EXPECTED; E2 AS EXPECTED; E3 AS EXPECTED; E4 AS EXPECTED; extra flag (allowed): the growth flag also catches crumpled. Exit: PASS, 'Each bad 3D shape is flagged by one existing ED meaning, and a flat 3D slice by none: consistent, not derived.' | CHECKED | `model/c3b_run1.txt`, `model/c3b_run1.log`, `model/c3b_runs/*.json`; run 2026-09-17. | C61, C62, D24 | — |
| C64 | Reasoning (paper), what C3b means: branched slices strain at their necks (largest tick difference on a neck in every run, growing about 4× with size, while flat slices grow only 1.3–1.4× as the extreme of many local values); crumpled slices cost links (degree 5× growth, all others flat); hyperbolic slices grow exponentially and are caught only by the curvature proxy (fixed degree, easy sync), so quadratic energy is needed; flat 3D slices, grid or random, trip nothing; the paper lead C56–C57 holds in a direct static check. Limits: stand-ins were built with each shape's defining feature, so this checks that the signals read those features separately, not that growth avoids the bad shapes; the sync signal is the linear precursor of unlocking and the curvature signal a proxy (only the grid is a triangulation); the balance of the three pressures and their strengths (knobs) untested; consistent, not derived; inputs unchanged (3). Options: (a) spec C3c on paper, growing a 3D slice with the three pressures in the move weights and an honest cost plan; (b) take stock of road C3 and attempt 7; (c) update the attempt 7 write-up. Proposal (a). Awaiting Allen. | CONDITIONAL | `C3b_Results.md`. | C56, C57, C63 | — |
| C65 | Option (a) of C64 chosen (D25): specify C3c, growing a 3D slice with commitment, quadratic energy and sync acting together. C3b's pass (C63) stands. | CONDITIONAL | `C3c_Model_Spec.md`. | C64, D25 | — |
| C66 | Counting and reasoning (paper): in a closed 3D triangulation F = 2T and V − E + F − T = 0 give E = V + T, and mean edge valence is 6T/E; flat valence 5.104 gives T/V = 5.699, E/V = 6.699, mean degree 13.40; the cube-grid start (6 tetrahedra per cube) has degree 14, E = 7V, T = 6V, mean valence 36/7 = 5.143. Commitment (cost per link) lowers T/V and mean valence below 5.10, toward branched (the large vertex-weight side of 3D DT); quadratic curvature cost pins valences near 5.10 (large scales unknown, C53); sync rewarding links across strain works against branching but, unopposed, adds links wherever strained (toward crumpled). So commitment and sync pull against each other and curvature pins the local count; a flat middle, if any, is where they balance (C58 as counts). | CONDITIONAL | `C3c_Model_Spec.md`. | C53, C56–C58, C64 | — |
| C67 | Specification (paper), model C3c: triangulated 3-torus stored as tetrahedra and vertex links, flat cube-grid start (n = 16, 24: V = 4,096, 13,824), budget 1, persistent rates σ = 0.0005, K = 0.5; each tick offspring as C3a (k = 1); merges by link contraction under the 3D link condition Lk(v) ∩ Lk(a) = Lk(va), partner weights e^(−ΔS), forced keeps counted, budget to partner; splits (c − 1 per event) on the newest child, one option per neighbour u (new child takes u's star in the link sphere plus cone tetrahedra), weights e^(−ΔS), rate and tick count inherited, budget shared; one flip sweep (2–3/3–2) Metropolis with proposal correction and e^(−ΔS); C2b tick update. Cost S = α·E + λ·Σ_edges (valence − 5.104)² − γ·Σ_links ((φ_x − φ_y)/(σ/K))², ΔS over touched elements. Settings S0 (0,0,0; uniform, flips always accepted), S1 (1,0,0), S2 (0,1,0), S3 (0,0,1), S4 (1,1,1), S5 (1,1,4). Calibrations FC flat start and RC 50 sweeps of uniform flips, at each size. Readings at T/2, 3T/4, T: ball-growth d_H and exponential flag; mean and max degree; valence mean, spread and mean (valence − 5.104)²; neck strain as C3b's largest linear-steady-state link difference with fresh rates, in units of FC's at the same size; settled if |d_H(T) − d_H(3T/4)| ≤ 0.15 and max degree changes ≤ 20%; balance and forced keeps, in-run sync strain reported. Shape classification at a size (median of seeds): crumpled if max degree ≥ 3× FC's; hyperbolic if the exponential flag is set; branched if neck strain ≥ 3× FC's or d_H < 2.3; flat 3D if none and d_H in [2.5, 3.5]; else unclassified. Structure checks every tick (each triangle in exactly two tetrahedra; each vertex link a connected 2-sphere; V − E + F − T = 0; no repeated tetrahedra; budget to 1e-9). Seeds 0, 1. Expected results before code (low confidence except E0, E1): E0 FC flat 3D and RC not flat at both sizes; E1 structure exact, survival, size within ±10%; E2 S0 not flat at the larger size (likely crumpled); E3 S1 branched at the larger size; E4 S3 not flat at the larger size (likely crumpled); E5 S4 flat 3D at both sizes, settled; S2, S5 reported. Exit rule: E1 fail code bug; E0 fail readings revision; S4/S5 unsettled 'not settled in T ticks'; then S4 or S5 flat at both sizes with S0 not flat records 'Commitment, quadratic energy and sync together grow a flat 3D slice at the sizes run: consistent, not derived; α, λ, γ are knobs' then larger sizes and a knob scan; S0 flat at both sizes records 'growth alone keeps a 3D slice flat at these sizes' and takes stock; S4 and S5 not flat but each a named bad shape records 'the three pressures don't balance at unit strengths: <shapes>' then knob scan on paper or take stock; otherwise 'a flat 3D slice isn't grown with ED's local moves at these strengths' and take stock. Cost plan: plain Python, no compiled speed-ups; 24 runs plus 4 calibrations on 6 workers; code with small-torus move tests (n = 4, 6), timing trial (10 ticks per size for S0, S4, plus readings), T the largest of 500, 300, 200 fitting about 10 h on 6 workers, else options to Allen before any run. Implementation questions: C3c-Q1 split options limited to link-neighbour stars with flips supplying the rest (yes); C3c-Q2 one flip sweep per tick with the same cost (yes); C3c-Q3 sync term −γΣ(normalized difference)² (yes, labelled); C3c-Q4 unit strengths plus S5 γ = 4 as knobs (yes); C3c-Q5 sizes 4,096 and 13,824, two seeds, T by timing within about 10 h (yes). No code; nothing run. | CONDITIONAL | `C3c_Model_Spec.md`. | C55–C58, C63, C65, C66 | — |
| C68 | C3c code and timing (not results): `model/c3c.py` (3-torus stored as tetrahedra with vertex stars, edge valences, neighbour sets and random-access sets of tetrahedra and valence-3 edges; flat cube-grid start with 6 tetrahedra per cube; 3D link-condition merge, star split, 2–3 and 3–2 flips with proposal correction to the uniform measure; ΔS over touched edges and links; tick with offspring, merges, splits, one flip sweep and the C2b update; full structure check; readings with ball growth, degrees, valences and C3b's neck strain), `model/c3c_timing.py`, implementation notes C3c section (including S0's flips read as no cost-based rejection with the proposal correction kept, recorded as an interpretation), all before any run. Move tests on n = 4 and 6 (harness only): direct merges, splits and flip sweeps, and 10 growth ticks each of S0 and S4 with the check after every tick, all clean, budget conserved exactly, keys consistent; flat start degree 14, T = 6V, E = 7V. Timing (10 ticks from the flat start with checks): V = 4,096 tick S0 2.9 s, S4 6.6 s, readings 0.5 s (defined, radii 2–4), flip sweep 0.1 s; V = 13,824 tick S0 9.9 s, S4 23.2 s, readings 0.9 s (radii 3–6), flip sweep 0.5 s. Projection for 24 runs plus calibrations on 6 workers: T = 500 about 7.5 h, T = 300 about 4.5 h, T = 200 about 3.0 h. By note 18's cost plan (largest T fitting about 10 h), T = 500. Caveats: tick times measured on early ticks may rise as shapes change; readings at 4,096 have only three radii. | CHECKED (harness) | `model/c3c_timing.txt`, `model/c3c_timing.log`; 2026-09-17. | C67, D26 | — |
| C69 | Health peek at two C3c saves (D29; counts only, no dimension or strain readings; logged as a look before the end): S1 (commitment only) at tick 150 had 13,620 events (0.985× the start), 4.78 tetrahedra per event (flat 5.70), 5.78 links per event (flat 6.70), mean degree 11.6 (flat 13.4), largest 346, mean edge valence 4.961 (flat 5.104), 4 forced keeps in the last tick; S4 (all three at unit strengths) at tick 200 had 16,329 events (1.181× the start), 31.60 tetrahedra per event, 32.60 links per event, mean degree 65.2, largest 1,486, mean edge valence 5.816, and 2,408 forced keeps in the last tick. Reading: S4 is densifying, and the mechanism is visible in the counts: in a dense slice a childless event often has no partner that passes the link condition, so it is forced to keep a child, which breaks the budget's size control and pushes the event count up (1.18× and rising). That is the rule's behaviour at these strengths, not a code bug: the moves are exact (structure checks clean every tick), the budget is conserved, and the incremental cost matches a full recomputation to 5e-13. Left to run, memory would likely fail before T = 500 and each tick keeps slowing. | CHECKED (health peek) | `model/c3c_runs/ckpt/*.pkl` at 15:06 on 2026-09-17; log entry. | C67, C68 | — |
| C70 | C3c run 1 (`model/c3c_run.py` with the D29 guards; expected results, shape rules and exit rule fixed in note 18 and its D29 revision before any results; 28 jobs, 8.1 h in the final session after two restarts, 3 workers): calibrations FC flat 3D at 13,824 (d_H 2.657, largest degree 14, no growth flag) but flagged hyperbolic at 4,096 (d_H 2.501, balls too small for three radii), RC crumpled + hyperbolic at both sizes (d_H undefined, largest degree 54 and 60, valence sd 2.66). Growth: S0 densified at both sizes (guard at ticks 137–144; 20.1–20.5 tetrahedra per event against 5.70 flat; mean degree 42–43; events 1.11–1.13×; forced-keep share 0.12–0.14); S1 ran all 500 ticks and read crumpled + hyperbolic at both sizes (mean degree 13.77 and 15.99, largest 224 and 406, valence mean 5.128 and 5.250 with sd 2.58 and 2.70, neck strain 10.6 and 14.4 against FC 6.5 and 7.0, d_H undefined at every checkpoint, late size 1.002× the start); S2 ran all 500 ticks and read crumpled + hyperbolic (mean degree 11.0, largest 94 and 154, valence mean 4.91 with sd 1.06, mean (valence − 5.104)² 1.15–1.16, neck strain 15.5 and 17.9, in-run strain 42–53, d_H undefined); S3 densified after 5 ticks at both sizes (21–22 tetrahedra per event, mean degree 44–46); S4 densified (ticks 128–200; at 13,824, 31.6 and 33.5 tetrahedra per event, mean degree 65–69, largest 1,486 and 1,725, events 1.18–1.19×, forced share 0.15–0.17); S5 densified (ticks 60–150; at 13,824, 37.3 and 37.4 tetrahedra per event, mean degree 76–77, largest 2,135, events 1.36–1.39×, forced share 0.26–0.27). E0 NOT AS EXPECTED; E1 AS EXPECTED (every structure check exact every tick, budget conserved, every stop a recorded guard); E2 AS EXPECTED; E3 NOT AS EXPECTED (S1 read crumpled, not branched); E4 AS EXPECTED; E5 NOT AS EXPECTED. Exit by the rule as written: 'E0 failed: readings revision (recorded).' No setting produced a flat 3D slice. | CHECKED (E0, E3, E5 misses recorded) | `model/c3c_run1.txt`, `model/c3c_run1.log`, `model/c3c_runs/*.json`; run 2026-09-17. | C67, C68, C69, D27, D29 | — |
| C71 | Reasoning (paper), what C3c means: the machinery checks out (structure exact every tick, budget conserved, incremental cost matching a full recomputation to 5e-13, guards converting runaways into recorded outcomes), so the failures are in the model. (i) Nothing balances the number of links and tetrahedra: the budget balances events (C2a) and did so in every run, but tetrahedra per event rose from 5.70 to 20 or more even with no pressure at all (S0), which is the known behaviour of random 3D triangulations when nothing weights the building-block count (in CDT and DT that is the cosmological-constant coupling per simplex); commitment is ED's version of that weight and at strength 1 holds the mean (S1 mean degree 13.77 against 13.40 flat) but not the extremes (largest degree 224 and 406), while sync alone is the fastest to densify, as C66's counting said. (ii) The readings cannot measure dense slices: a few hops reach everything, so fewer than three ball radii fit and the mass dimension is undefined, which also mis-flagged the flat calibration at 4,096 and is what fails E0; so S1's and S2's 'crumpled + hyperbolic' is partly a reading limit, not a clean measurement. So road C3's missing piece in 3D is a second balance, on links per event, next to C2a's balance on events; commitment is the candidate from ED's meanings but its strength is a knob. Options: (a) on paper, the link balance, then spec C3d with repaired readings; (b) repair readings and rerun as is; (c) take stock of road C3 and attempt 7. Proposal (a) then (c) if the paper work doesn't open a way. Inputs unchanged (3). Awaiting Allen. | CONDITIONAL | `C3c_Results.md`. | C48, C66, C68–C70 | — |
| C72 | Literature (the link balance): the number of triangulations grows exponentially with the number of building blocks (Tutte in 2D; the exponential bound assumed and debated in higher dimensions, hep-th/9612069, hep-lat/9403019), so in dynamical triangulations the bare cosmological constant must be tuned to its critical value for the infinite-volume limit, with runaway on one side and collapse on the other (DT and CDT reviews, arXiv:1904.05755, Scholarpedia); feedback that drives a system to criticality without tuning is the self-organized-criticality idea, including attempts in quantum gravity (hep-th/0412307), and ED already did this once for events (A7 C31). | CITED | Listings and abstracts; 2026-09-18. | C9, C31, C70 | — |
| C73 | Reasoning and counting (paper): C3c left out two decided ED meanings: no infinities (a finite ceiling on links per event, A4 D32 / PF-Q1; C3c reached 224, 406 and 2,135 neighbours) and commitment as a budget that holding links uses up (C2a's reading A, which destabilized offspring but has the right sign for links: a crowded event's budget is used up so it cannot take more, a sparse one can). With one unit of commitment per link, split between its ends, total links equal the total budget exactly and links per event equal B_L/V, so the count of building blocks is pinned by a conservation law instead of a coupling tuned to a critical value: no fine-tuning, any budget value sits stable. The flat value is E/V = 6.70 (C66), so setting the budget there puts the density in, leaving shape as what is tested; the ceiling kills the crumpled extremes as a hard limit rather than a cost. | CONDITIONAL | `C4_Link_Balance.md`. | C14, C31, C56, C66, C70–C72 | — |
| C74 | Meaning, questions and exit rule: C3c's runaway is explained by the two omitted meanings; the event balance and the link balance are the same trick twice, a conserved budget in place of a tuned coupling, so what dynamical triangulations tune (the weight per building block) ED would hold by conservation. Not fixed: shape at fixed density (curvature and sync must still choose between crumpled, branched and flat), the readings on dense slices (C3c's flat calibration failed at 4,096 events), and the numbers (budget per event, ceiling, three strengths are knobs). Consistent, not derived; inputs unchanged (3). Meaning questions with defaults: C4-Q1 holding a link uses up an event's commitment budget (yes, labelled reading); C4-Q2 that budget is conserved so the total link count is pinned, not tuned (yes); C4-Q3 a hard ceiling on links per event, value a knob (yes; A4 D32); C4-Q4 budget per event set to the flat value 6.70 links per event, recorded as a knob and as density put in (yes); C4-Q5 offspring keep C2a's reading B as a separate budget (yes, two budgets). Draft exit rule: spec C3d as C3c plus the conserved link budget, the ceiling and repaired readings (sizes chosen so the flat calibration reads flat); record 'with the link balance and the ceiling, ED's meanings grow a flat 3D slice at the sizes run: consistent, not derived; density, ceiling and strengths are knobs' if the density holds and some setting reads flat 3D; 'the link balance fixes density but not shape: <shapes>' if the density holds and none reads flat, then take stock; a code or reading problem if the density does not hold; expected results, ranges and an honest cost plan fixed in the spec first. Options: (a) decide and spec C3d; (b) take stock of road C first; (c) repair the readings on paper first. Proposal (a) with (c) folded in. Awaiting Allen. | CONDITIONAL | `C4_Link_Balance.md`. | C70–C73 | — |
| C75 | C4-Q1–C4-Q5 accepted and note 20's exit rule confirmed (D31): holding a link uses up an event's commitment budget (labelled reading); that budget is conserved so the total link count is pinned rather than tuned; a hard ceiling on links per event, value a knob; the budget set at the flat value with density recorded as put in; two budgets, one for children (C2a reading B) and one for links. C72–C74 stand as written. | CONDITIONAL | `C3d_Model_Spec.md`. | C74, D31 | — |
| C76 | Specification (paper), model C3d: C3c's model (note 18 with its D29 revision) plus (i) a conserved link budget B_L = round(6.699 × V₀) held as a pool, with moves that would exceed it refused and refusals counted (the cube-grid start at E = 7V trades the difference away in the first ticks, recorded); (ii) a ceiling of 30 links per event (about 2.2× the flat mean 13.4; knob, from A4 D32); (iii) repaired readings: sizes V = 8,000 (n = 20) and 13,824 (n = 24), calibrations run first as a gate that stops the run if the flat calibration does not read flat 3D at both sizes, the walk-based spectral dimension restored, and diameter and mean distance reported; (iv) settings S0 (0,0,0), S1 (1,0,0), S2 (0,1,0), S3 (0,0,1), S4 (1,1,1), T = 200, seeds 0 and 1, so 20 growth runs plus 4 calibrations, with C3c's guards kept. Readings at T/2, 3T/4, T: d_H, d_s, exponential flag, diameter and mean distance, links and tetrahedra per event, mean and largest degree, valence mean, spread and mean (valence − 5.104)², neck strain against the flat calibration, refusals by the budget and by the ceiling, event count against the start and forced keeps. Shape classification: crumpled if largest degree ≥ 3× the flat calibration's or tetrahedra per event ≥ 2× 5.70; hyperbolic if the growth flag is set; branched if neck strain ≥ 3× the flat calibration's or d_H < 2.3 or d_s < 2.0; flat 3D if none of these and d_H in [2.5, 3.5] and d_s in [2.3, 3.7]; else unclassified. Expected results before code (low confidence except E0, E1): E0 flat calibration flat 3D at both sizes and the randomized one not; E1 structure exact, both budgets conserved, no event over the ceiling, links at or below B_L with the shortfall only from refusals, event count within ±10% of the start; E2 S0 not flat 3D at the larger size; E3 S4 flat 3D at both sizes and settled; E4 every setting settled; S1, S2, S3 reported. Exit rule: E0 fail stops before the growth runs as a readings revision; E1 fail on structure or a budget is a code bug; a deciding setting unsettled reads 'not settled in T ticks'; then S4 flat at both sizes with S0 not flat records 'With the link balance and the ceiling, ED's meanings grow a flat 3D slice at the sizes run: consistent, not derived; the density, the ceiling and the three strengths are knobs' and goes on to larger sizes and a strength scan; S0 flat at both sizes records 'at pinned density, growth alone keeps a 3D slice flat' and takes stock; density holding with nothing flat records 'the link balance fixes density but not shape: <shapes>' and takes stock; anything else is recorded as it reads. Cost plan: about 14 s and 24 s per tick at the two sizes on 3 workers, so about 47 and 80 minutes per run and about 7 hours in total, with saves every 10 ticks; if the timing trial exceeds about 10 hours, T drops to 150, recorded before any run. Implementation questions: C3d-Q1 link budget as a pool with counted refusals (yes); C3d-Q2 ceiling 30 (yes, knob); C3d-Q3 calibrations as a gate (yes); C3d-Q4 spectral dimension restored plus diameter and mean distance (yes); C3d-Q5 five settings, two sizes, two seeds, T = 200 (yes). No code; nothing run. | CONDITIONAL | `C3d_Model_Spec.md`. | C66, C70, C71, C73, C75 | — |
| C77 | C3d code and timing (not results): `model/c3d.py` (C3c's slice and moves plus a conserved link budget held as a pool, a ceiling of 30 links per event, per-move effect counting of links born and dying and of degree changes, counted refusals, and repaired readings: ball growth, the walk-based spectral dimension, diameter and mean distance, counts and neck strain), `model/c3d_timing.py`, `model/c3d_run.py` with note 21's expected results, classification and exit rule in its header and the calibration gate before any growth run, all written before any run. Move tests on n = 4 and 6 (harness only): a first version froze because the cube-grid start sits above the budget (E = 7V against 6.699V) and the gate refused every move including those that give links back; fixed before any run so that only net additions need free budget, and the start pays the difference down. After the fix, 12 ticks at both settings: structure exact every tick, links + pool exactly equal to the budget, no event over the ceiling, children's budget conserved exactly; the ceiling binds (events reach 30 links, hundreds of moves refused per tick at n = 6). Runner smoke test clean (status, both budgets, ceiling, checkpoints, save removal, calibration path). Timing: V = 8,000 tick S0 7.6 s and S4 13.7 s, flat readings with the walk 2.9 s (d_H and d_s defined, radii 3–5, diameter 15); V = 13,824 tick S0 13.1 s and S4 24.4 s, readings 3.2 s (radii 3–6, diameter 18); flip sweep 0.3 and 0.5 s. Projection for 20 growth runs plus 4 calibrations on 3 workers: T = 200 about 6.4 h, T = 150 about 4.8 h. By the cost plan (largest T inside about 10 h), T = 200 stands. | CHECKED (harness) | `model/c3d_timing.txt`, `model/c3d_timing.log`; 2026-09-18. | C76, D32 | — |
| C78 | C3d run 1 (`model/c3d_run.py`, note 21's expected results, classification and exit rule in its header before any results; calibration gate then 20 growth runs, 5.6 h on 3 workers): E0 AS EXPECTED, the gate passed — flat calibration reads flat 3D at both sizes (d_H 2.629 and 2.657, d_s 3.061 and 3.050, diameters 15 and 18, links per event 7.000) and the randomized calibration reads crumpled + hyperbolic (diameters 8 and 9, 13.6 tetrahedra per event), so C3c's measurement failure is fixed. Growth at 13,824 (medians of 2 seeds): S0 hyperbolic, 8.357 links per event, mean degree 16.71, largest 30, diameter 12.5, late size 0.728× the start; S1 hyperbolic + branched, 4.808, 9.62, 30, 13.0, 0.945, d_H 3.866 and d_s 1.593; S2 hyperbolic + branched, 5.375, 10.75, 30, 15.0, 0.998, d_H 4.322 and d_s 1.611 (the only settled case); S3 hyperbolic, 11.754, 23.51, 30, 11.5, 0.597; S4 hyperbolic, 9.707, 19.41, 30, 11.5, 0.741; the 8,000 runs agree. Refusals by the ceiling ran 13,754–61,591 per tick and abandoned splits 28–1,885 per tick; refusals by the link budget appeared only for S3 and S4 (up to 23,813 per tick). E1 NOT AS EXPECTED, and checked run by run: 0 structure failures, 0 children-budget failures, 0 link-budget failures, 0 ceiling violations, 0 guard stops, all 20 runs surviving 200 ticks; the only miss is the event count, outside ±10% in 12 of 20 runs (0.60–0.75 of the start for S0, S3, S4). E2 AS EXPECTED; E3 NOT AS EXPECTED; E4 NOT AS EXPECTED (only S2 at 13,824 settled). Exit by the rule as written: 'E1 failed: code bug (recorded)' — recorded, with the check above showing it is not a code bug. | CHECKED (E1, E3, E4 misses recorded) | `model/c3d_run1.txt`, `model/c3d_run1.log`, `model/c3d_runs/*.json`; run 2026-09-18. | C76, C77, D33 | — |
| C79 | Reasoning (paper), what C3d means: the link balance worked (density no longer runs away, which was C3c's failure) and the repaired readings worked (the gate passed), but the ceiling at 30 dominated everything: every setting at both sizes ended with its largest degree exactly at the ceiling, with 14,000–62,000 moves per tick refused by it, and the refused moves are splits (28–1,885 abandoned per tick). That breaks the event balance: the budget restores the event count by making more children, but children cannot be made when every split option is refused, while merges still go through, so the slice shrinks to 0.60–0.75 of the start (S0, S3, S4) and links per event rises above flat (8.4–11.8) even though the total link count is pinned. Diameters collapse from 18 to 11–15, so the ball reading often cannot fit three radii and most settings read 'hyperbolic', which at these diameters mostly means too small across to measure; the two settings that held their size (commitment only, curvature only) are the two that could be measured and both read branched (d_H 3.87 and 4.32 with d_s 1.59 and 1.61). So three of ED's four ingredients now behave (event budget, link budget, readings) and the fourth, the no-infinities ceiling, is doing harm at the value chosen: 30 was picked as about 2.2× the flat mean without evidence, and it sits close enough to the working range to block the moves the event balance needs. No flat 3D slice was grown; inputs unchanged (3). Options: (a) a short labelled diagnostic, not pre-registered, repeating S0 and S4 at one size with T = 100 with the ceiling at 60 and then removed, to see whether the event count holds and diameters recover (about 40 minutes); (b) take stock of road C and attempt 7; (c) respec C3e now with a looser ceiling and a size guard and rerun in full. Proposal (a), then (b) or (c). Awaiting Allen. | CONDITIONAL | `C3d_Results.md`. | C73, C76, C78 | — |
| C80 | Follow-up diagnostic for C3d (not pre-registered, D34; `model/c3d_diagnostic.py`, V = 8,000, T = 100, seed 0, settings S0 and S4, ceilings 30, 60 and none; five of six rows at the time of recording, the sixth running): ceiling 30 reproduces C3d at the shorter length (events 0.721× and 0.834× the start, links per event 7.72 and 7.99, diameters 12 and 10, largest degree at the ceiling, about 33,000 ceiling refusals per tick, d_H and d_s undefined); ceiling 60 does not restore the event count (0.826× for S0 and 0.561× for S4, links per event 8.11 and 11.91, diameters 11 and 7, refusals split between ceiling 16,000–18,600 and link budget 5,553–16,085); no ceiling likewise (S0 0.711×, links per event 9.42, diameter 9, largest degree 230, ceiling refusals 0 but link-budget refusals 13,947). So the ceiling is not the cause: with it loosened the block moves to the link budget. Reading: a split must buy about 7 links while a 2–3 flip buys one, and a full sweep of flips each tick drains the conserved pool, so splits are refused (245–626 abandoned per tick) while merges continue, shrinking the slice and crowding the survivors. C3d run 1's verdict stands. | CHECKED (diagnostic) | `model/c3d_diagnostic.py`, `model/c3d_diagnostic.log`; 2026-09-18. | C78, C79 | — |
| C81 | Specification (paper), model C3e: C3d's model with one change, flips paired as ED decided (A6 D5, D8, strictly rather than C3-Q6's loose reading): a flip attempt is one 2–3 and one 3–2 at disjoint places, accepted or refused as a single move, so the link and tetrahedron counts are unchanged and rewiring no longer competes with growth for the conserved link pool; one pair per two events per tick, keeping the number of individual flips as before; acceptance by the pair's combined cost change and proposal correction; refused if either move is invalid, the two overlap, or the ceiling would break. Rest as C3d: sizes 8,000 and 13,824, settings S0–S4 at unit strengths, two seeds, link budget at 6.699 links per event, calibration gate first, guards kept, readings and shape rules unchanged; ceiling 60 with a no-ceiling contrast for S0 and S4 at the larger size, one seed each; T = 150. Expected results before code: E0 the gate passes as in C3d; E1 structure, both budgets and the ceiling exact, event count within ±10% of the start, and flips never changing the link count; E2 S0 not flat 3D at the larger size; E3 S4 flat 3D at both sizes and settled; E4 every setting settled; E5 accepted paired flips per tick above zero in every setting. Exit rule: E0 fail stops before the growth runs; E1 failing on structure, a budget or the ceiling is a code bug; E1 failing only on the event count records 'the two balances still fight: growth cannot be sustained at pinned density' and takes stock; E5 failing sends the pairing rule back to Allen; then S4 flat at both sizes with S0 not flat records 'With the link balance, paired cut-and-rejoin and the ceiling, ED's meanings grow a flat 3D slice at the sizes run: consistent, not derived; the density, the ceiling and the three strengths are knobs' and goes on to a knob scan; S0 flat at both sizes records 'at pinned density, growth alone keeps a 3D slice flat'; counts holding with no flat shape records 'the balances hold the counts but not the shape: <shapes>'. Cost plan: about 1.5× C3d's tick cost, so roughly 50 and 90 minutes per run and about 7 h on 3 workers, with the timing trial confirming and T dropping to 100 if it exceeds about 10 h. Implementation questions: C3e-Q1 paired flip as one move (yes); C3e-Q2 one pair per two events per tick (yes); C3e-Q3 ceiling 60 with a no-ceiling contrast (yes); C3e-Q4 T = 150 (yes). No code; nothing run. | CONDITIONAL | `C3e_Model_Spec.md`. | C78–C80, A6 D5, D8 | — |
| C82 | C3e code and timing (not results): `model/c3e.py` (paired flip: one 2–3 and one 3–2 at disjoint tetrahedra, refused if the 2–3 would create the triangle the 3–2 needs absent, applied as one link- and tetrahedron-neutral move with the pair's combined cost and a proposal correction of n3 before over n3 after, one pair per two events per tick; ceiling 60; everything else from C3d), `model/c3e_timing.py` and `model/c3e_run.py` with note 23's expected results and exit rule in its header and the no-ceiling contrast runs, all written before any run. Move tests on n = 4 and 6 (harness only), 12 ticks at both settings: structure exact, links + pool exactly the budget, no event over the ceiling, children's budget exact, and every tick's flips link-neutral; splits were refused 0–2 times per tick against hundreds in C3d, event counts ended at 0.88–1.05 of the start, and links per event 5.8–6.9 against the flat 6.699. Runner smoke test clean, including a no-ceiling contrast job. Timing: V = 8,000 tick S0 11.8 s and S4 19.4 s, flat readings with the walk 2.0 s (diameter 15); V = 13,824 tick S0 18.4 s and S4 27.4 s, readings 3.7 s (diameter 18), about 1.5× C3d's tick cost as the plan projected. Projection for 20 growth runs, 2 contrast runs and 4 calibrations on 3 workers: T = 150 about 6.8 h, T = 100 about 4.6 h. By the cost plan, T = 150 stands. | CHECKED (harness) | `model/c3e_timing.txt`, `model/c3e_timing.log`; 2026-09-18. | C81, D35 | — |
| C83 | Parked road (paper, not started), from Allen's prime work in side conversation: the sieve makes randomness with no chance in it, since many incommensurate periods (the wheel) interfere and the survivors pass most tests for random, whereas ED assumes chance in offspring counts, clock rates and move choices, with the Born rule supplied in form (A2 C11). Road D asks whether ED's balance needs genuine chance or whether incommensurate periods would do: keep C2a exactly (budget passed forward and conserved, k = 1, L* = 100 and 200, T = 400) and replace only the offspring draw with a deterministic wheel rule, then read it with the same pre-registered checks (survival, telescoped offspring average, mean slice length, geometry). Outcomes: the balance still self-organizing would narrow what ED assumes and touch the census guard; it failing would sharpen what the Born-rule input does; self-organizing with different geometry would locate where chance and pseudo-chance differ. Limits: passing a few checks is not being random, the periods are knobs, and this is about the balance, not the Born rule itself. Also recorded as a reading habit: subtract the expected trend and look at what is left, sorted by local class, which existing C2a, C3d and C3e results already support. | CONDITIONAL (parked) | `Possible_Roads.md`. | C31, A2 C11 | — |
| C84 | C3e run 1 (`model/c3e_run.py`, note 23's expected results, classification and exit rule in its header before any results; calibration gate, 20 growth runs and 2 contrast runs, 6.4 h on 3 workers): E0 AS EXPECTED (gate passed at both sizes, as C3d); E5 AS EXPECTED (paired flips link-neutral in every tick of every run, 126–674 accepted pairs per tick); splits no longer starved for S0, S1, S2 (abandoned splits 0.2–211 per tick against hundreds to thousands in C3d) and their event counts held at 0.940, 0.984 and 1.001 of the start; but S3 and S4 collapsed to 0.365 and 0.534, with links per event 20.63 and 14.51 against the flat 6.699, mean degree 41.3 and 29.0, diameters 8.0 against the flat calibration's 18. Every setting read 'crumpled + hyperbolic' (S1 at the larger size also branched) and every mass and spectral dimension was undefined, diameters running 7.5–13.5. Contrast runs without the ceiling: no pressure survived at 0.756 of the start with diameter 15 (the flat value) and largest degree 349; all three hit the densification guard at 0.318 with 21.1 links per event. E1 NOT AS EXPECTED, and checked run by run: 0 structure failures, 0 children-budget failures, 0 link-budget failures, 0 ceiling violations, 0 flip-neutrality failures; the only miss is the event count, outside ±10% in 9 of 22 runs, all of them sync settings plus one contrast run. E2 AS EXPECTED; E3 NOT AS EXPECTED; E4 NOT AS EXPECTED. Exit by the rule as written: 'E1 failed: code bug (recorded)' — recorded, with the check above showing it is not a code bug. | CHECKED (E1, E3, E4 misses recorded) | `model/c3e_run1.txt`, `model/c3e_run1.log`, `model/c3e_runs/*.json`; run 2026-09-18. | C81, C82, D36 | — |
| C85 | Reasoning (paper), what C3e means: the paired-flip diagnosis was right, since three of five settings now hold their event count where none did in C3d; what collapses the slice is sync itself, not the ceiling and not rewiring — wherever sync is on, the event count falls to a third or a half and the survivors crowd at 14–21 links each, the direction C66's counting predicted (sync rewards links, so unopposed it crowds), with the new fact that commitment and curvature at unit strength do not hold it back. Two faults of my own specs are recorded: the crumpled test (largest degree ≥ 3× the flat calibration's 14, i.e. 42) sits below the ceiling of 60, so any slice touching the ceiling is labelled crumpled automatically, making C3e's shape labels unreliable; and nothing could be measured for dimension because diameters fell to 7.5–13.5 against the flat 15 and 18, the exception being the no-ceiling no-pressure contrast run, which kept diameter 15. Road C3 now has three 3D attempts with three understood walls (C3c density runaway from no link balance; C3d shrinkage from the ceiling blocking splits; C3e sync-driven shrinkage plus a ceiling-tangled shape rule); what has stayed fixed is the link balance, the calibration gate and rewiring that does not steal from growth; what is unresolved is whether ED's three pressures can balance at fixed density and how to measure a slice whose diameter keeps falling. Options: (a) take stock of road C and attempt 7; (b) C3f with the shape rule separated from the ceiling and a scan of sync's strength below 1, about 7 h; (c) paper work on sync's form in the move cost, questioning whether rewarding links across strain is the right reading of C3-Q7. Proposal (a), and if a fourth attempt follows, (c) before (b). Inputs unchanged (3). Awaiting Allen. | CONDITIONAL | `C3e_Results.md`. | C66, C79, C80, C84 | — |
| C86 | Reasoning (paper), second stock-take of road C and attempt 7 (note 25): reached C1 (pass, 1 run), C2a Budgeted Causality (pass, retested), C2b Synced Now (pass on run 3), C3b (pass), against four walls with causes identified: C3a a local curvature cost does not flatten at large scale; C3c density ran away with no link balance; C3d the ceiling blocked splits; C3e sync collapses the slice. Working pieces: the event balance, the link balance, the readings when slices are wide enough, paired cut-and-rejoin, and the guards. Still put in: the slice's dimension, whole-number dimension, the density, the knobs (k, L*, K, σ, α, λ, γ, ceiling), commitment's fewest-directions lead, and the cosmic excess; inputs supplied 3, unchanged. Recorded honestly: eight specification errors of Claude's, each caught by a run or a test (readings undefined at the smallest size; tick cost estimated from early ticks; the no-infinities ceiling left out; a ceiling of 30 tight enough to block growth; a budget gate that refused the moves that pay it down; flips read loosely against A6 D5/D8; the crumpled test set below the ceiling; no floor on diameter), so the walls are real but C3d's and C3e's shape labels are not trustworthy. What the runs say about the picture: at pinned density every setting's diameter fell, which is what random rewiring does (shortcuts shrink diameters, the small-world effect), and sync as specified rewards exactly those shortcuts, because the most strain sits between distant regions — so the suspicion is about the reading of C3-Q7 (sync in the move cost) rather than its strength; sync's job in C2b was to pick the slice's dimension, not to shape the slice. Attempt 7 overall: three consistent-not-derived results each earned against results fixed in advance, one structural difference from CDT that survived every run (what CDT tunes, ED conserves), a growth wall moved but not fallen, and nothing observable or new in number. Options: (a) paper work on sync's form then a cheap targeted test; (b) C3f with the shape rule and a diameter floor plus a scan of sync's strength below 1 (about 7 h); (c) drop sync from growth and test commitment plus curvature alone (about 3 h); (d) conclude attempt 7. Proposal (a), then (c) and (b) as one run. Awaiting Allen. | CONDITIONAL | `Taking_Stock_2.md`. | C41, C53, C63, C70, C78, C84, C85 | — |
| C87 | Literature (sync's form in growth): oscillator networks rewired by phase differences under a limited connection budget organize into modular small-world structure (Gutiérrez et al., PRL 107, 234103, 2011; C54); a few long links collapse distances to logarithmic (Watts–Strogatz), while in spatial networks a cost growing with link length suppresses shortcuts and keeps distances lattice-like, the shortcut density deciding which regime holds (Barthélemy, Spatial Networks, Phys. Rep. 499, 1, 2011, arXiv:1010.0302); random triangulations with no weight on the building-block count crumple to diameters far below flat (C42, C54, C72); and in 4D CDT's de Sitter phase a single spatial slice is fractal, Hausdorff about 3 with spectral about 1.5 (C20). | CITED | Listings and abstracts; 2026-09-18. | C20, C42, C54, C72 | — |
| C88 | Reasoning (paper), why C3e's sync collapses the slice: the cost as specced pays for a link in proportion to the tick difference it carries, and the largest differences sit between regions far apart, so the reward falls on exactly the links that shorten distances; that is the shortcut mechanism the adaptive-sync literature reports, and it matches the run (links per event 14–21, diameters 8, event counts halved with sync on, sizes holding with sync off). It is a matter of form, not strength: a smaller γ weakens the pull without changing its direction. The deeper problem is that every weight in C3c–C3e is local (links, valences, single-link tick differences) while rough configurations vastly outnumber smooth ones, so local weights must fight entropy and lost in every run; the one non-local quantity ED has is the tick field (C57), but rewarding the relief of strain flattens it, and a flat field is what an expander-like small-diameter slice gives, so the cost as written asks for the shape we do not want. | CONDITIONAL | `C5_Sync_Form.md`. | C57, C66, C84, C87 | — |
| C89 | Reasoning (paper), the target and four readings: C3c–C3e demanded that a grown slice read flat 3D, but CDT's own good phase has fractal slices (C20) and what is smooth there is the spacetime, not the slice; C1 judged ED the same way CDT is judged, on the spacetime pattern, so road C3 has been holding ED to a stricter standard than the theory it is compared with, and the fix is to measure the grown spacetime with the same machinery. Four readings of sync's form: (i) as specced, rewarding strain-carrying links — fails by C3e and by the literature; (ii) locality-capped, the same reward only for links whose ends were already within a few hops — plausible, keeps neck-widening (a neck's two sides are two hops apart through the neck) and bans shortcuts, matching the spatial-network lesson; (iii) a condition rather than a reward, refusing moves that would leave a link's tick difference above the space-like limit — closest to what C2b established; (iv) out of the growth cost entirely, sync remaining a selection principle — the cleanest control. | CONDITIONAL | `C5_Sync_Form.md`. | C20, C22, C57, C87, C88 | — |
| C90 | Meaning, questions and exit rule: sync's failure in C3e is a failure of form, not strength; the honest reading of C2b is a condition to satisfy rather than a quantity to maximize; and the larger correction is the target, since slices need not be flat in ED or in CDT and the thing to measure is the grown spacetime. Consistent, not derived; nothing computed; inputs unchanged (3). Meaning questions with defaults: C5-Q1 sync in growth is a condition, not a reward (yes, reading iii); C5-Q2 any reward form kept for comparison must be locality-capped (yes, reading ii as contrast); C5-Q3 road C3 judges the grown spacetime pattern, not the slice alone (yes); C5-Q4 keep a no-sync control (yes, reading iv). Draft exit rule for C3f: carry the label fixes (crumpled test independent of the ceiling; a diameter floor below which a run is recorded 'too small to measure' rather than shaped), make spacetime readings the main result with slice readings alongside, and run four settings (commitment and curvature; plus sync as a condition; plus locality-capped sync; and no pressure as baseline); record 'with the link balance, paired cut-and-rejoin and <that sync form>, ED's growth gives a four-dimensional pattern at the sizes run: consistent, not derived' if some setting's spacetime reads four-dimensional and smooth; 'ED's local weights don't select smooth geometry against entropy; the wall is the ensemble, not the meanings' if none does while the counts hold; a model or code problem if the counts do not hold; expected results, ranges and a cost plan fixed in the spec first. Options: (a) decide and spec C3f; (b) reread C3e as spacetime first, which needs a short rerun since forward links were not saved; (c) take stock again. Proposal (a). Awaiting Allen. | CONDITIONAL | `C5_Sync_Form.md`. | C87–C89 | — |
| C91 | C5-Q1–C5-Q4 accepted and note 26's exit rule confirmed (D40): sync in growth is a condition rather than a reward; a locality-capped reward would be the contrast; the grown spacetime is judged rather than the slice alone; a no-sync control is kept. C87–C90 stand as written. | CONDITIONAL | `C3f_Model_Spec.md`. | C90, D40 | — |
| C92 | Specification (paper), model C3f, with a correction to C88 recorded first: every C3e move is already local (a split joins events that were within two hops, a 2–3 flip joins apexes two hops apart, merges only remove), so no move can create a long-range link, the locality cap of reading (ii) is already satisfied and is dropped, and C88's wording that sync 'rewards shortcuts' was a guess at the mechanism; the evidence that sync collapses the slice stands, and C3f measures the route by recording the per-tick balance of merges, splits, forced keeps and refusals by cause. The model is C3e's (paired flips, conserved link budget, ceiling 60, guards, calibration gate) with three changes: (i) sync becomes a condition, refusing any move that would leave a link with a tick difference above s_max = 3× the flat calibration's largest single-link difference at that size (relative because at σ/K = 0.001 the absolute space-like limit never bites; the factor is a knob), with its refusals counted separately; (ii) the grown spacetime is the main reading — events of the last 30 ticks (8,000) or 20 ticks (13,824) with in-slice and forward links, read by ball growth only, with flat 3+1 expected near 4, slice readings still reported but no longer deciding; (iii) the labels are fixed — crumpled becomes mean degree ≥ 2× the flat mean (26.8) or tetrahedra per event ≥ 2× flat (11.4), with largest degree reported only, and 'too small to measure' becomes its own outcome when the mass dimension is undefined or the diameter is below 8. Settings: A commitment and curvature (1, 1, no sync), B the same with sync as a condition, C sync alone as a condition, D no pressure; sizes 8,000 and 13,824, T = 150, two seeds, so 16 growth runs plus 4 calibrations, with per-tick bookkeeping of merges, splits, forced keeps and refusals by cause. Expected results before code: E0 the gate passes; E1 structure, budgets, ceiling and flip neutrality exact with A and D holding their event counts within ±10%; E2 B and C holding theirs too, unlike C3e's sync settings, since a condition only refuses; E3 B's spacetime mass dimension in [3.3, 4.7] with at least three usable radii (the hypothesis, low confidence); E4 D's spacetime not in that range; E5 every setting measurable at both sizes. Exit rule: E0 fail is a readings revision; E1 failing on structure, a budget, the ceiling or flips is a code bug; E1 or E2 failing only on the event count is recorded as 'the counts still drift under <setting>' with the bookkeeping naming the cause; then B in range with D not records 'With the link balance, paired cut-and-rejoin and sync as a condition, ED's growth gives a four-dimensional pattern at the sizes run: consistent, not derived; the density, the ceiling, the sync threshold and the strengths are knobs'; A also in range records the same with sync not needed; D in range records 'growth alone gives a four-dimensional pattern at these sizes'; none in range with counts holding records 'ED's local weights don't select smooth geometry against entropy; the wall is the ensemble, not the meanings'; unmeasurable runs record 'too small to measure at these sizes'. Cost plan: about 4.5–5 h of growth on 3 workers plus spacetime readings on 240,000–280,000 events each, about 6 h in total, with the timing trial confirming and the pattern shortened to 20 and 15 ticks if a reading exceeds about 5 minutes. Implementation questions: C3f-Q1 the sync condition at 3× the flat strain (yes, knob); C3f-Q2 drop the locality-capped contrast (yes); C3f-Q3 spacetime from the last 30 and 20 ticks by ball growth only (yes); C3f-Q4 crumpled by mean degree and tetrahedra per event, with 'too small to measure' as its own outcome (yes); C3f-Q5 four settings, two sizes, two seeds, T = 150 (yes). No code; nothing run. | CONDITIONAL | `C3f_Model_Spec.md`. | C84, C88, C89, C91 | — |
| C93 | C3f code and timing (not results): `model/c3f.py` (sync as a condition refusing any move that would create a link carrying more than s_max, the cost reduced to commitment and curvature, per-tick bookkeeping of merges, splits, forced keeps and refusals by cause, and a record of children and absorptions so the grown spacetime can be built), `model/c3f_timing.py`, written before any run. Move tests on n = 4 and 6 (harness only): structure, both budgets, the ceiling and flip neutrality exact in every setting, and the spacetime builder produced patterns from the recorded children and merges; the sync condition never fired at those sizes. A first timing attempt showed the sync setting costing double (39.1 s against 18.4 s per tick at 8,000 events) because `allowed` walked the move's edges twice, once for budget and ceiling and again for the sync check; fixed to a single pass before any run, with no change of behaviour. After the fix: tick with checks 16.6 s (A) and 16.8 s (B) and 8.9 s (D) at 8,000, and 28.6 s, 32.0 s and 18.8 s at 13,824; spacetime readings over 10 ticks 87,851 and 151,643 events, taking 8–17 s, with the ball reading defined at both sizes (radii 2–4 and 3–5); projection for 16 growth runs plus calibrations on 3 workers: T = 150 about 4.8 h, T = 100 about 3.2 h, so T = 150 stands. Ceiling refusals run 171–321 per tick in A and B and 8,207–14,560 in D; sync refusals were zero everywhere in the first 10 ticks. | CHECKED (harness) | `model/c3f_timing.txt`, `model/c3f_timing.log`; 2026-09-18. | C92, D41 | — |
| C94 | Strain diagnostic for C3f (not pre-registered, D41; `model/c3f_strain_diagnostic.py`, V = 8,000, 40 ticks, seed 0): the flat calibration's largest link strain is 6.461. With commitment and curvature on, the largest link strain climbs 1.13× flat at tick 5, 1.40× at 10, 1.91× at 20, 2.11× at 30 and 2.36× at 40, with links over 1×, 1.5×, 2× and 3× flat numbering 1, 37, 244 (15 over 1.5×), 492 (50, 4) and 806 (120 over 1.5×, 25 over 2×, none over 3×) out of about 42,700; with no pressure the largest strain stays near 1.04–1.30× flat and nothing exceeds 1.5×. So the condition would bind in the settings it is meant for and leave the baseline alone, 3× cannot fire within the run length, 2× touches a handful of links late, and 1.5× acts on the growing tail from about tick 20. Revision recorded before any run: s_max = 1.5 × the flat calibration's largest link strain, replacing 3×, still a knob but now set from measurement. | CHECKED (diagnostic) | `model/c3f_strain_diagnostic.txt`, `.log`; note 27 threshold section; 2026-09-18. | C92, C93 | — |
| C95 | C3f run 1 (`model/c3f_run.py`, note 27's expected results, classification and exit rule with the D41 threshold in its header before any results; calibration gate then 16 growth runs, 4.8 h on 3 workers): E0 AS EXPECTED, the gate passed with the flat calibration reading flat 3D at both sizes and the randomized one reading 'too small to measure' (diameters 8 and 9) instead of being mislabelled as in C3d and C3e. Growth (medians of 2 seeds, 8,000 / 13,824): A commitment and curvature — events 1.001 and 1.002 of the start, 5.169 and 5.150 links per event, mean degree 10.34 and 10.30, slice diameters 10.5 and 12.5, slice too small to measure, spacetime d_H 5.977 and 6.439 over 249,000 and 292,000 events; B the same with the sync condition — identical to A in every figure; C sync alone — events 0.942 and 0.940, 7.026 and 7.150 links per event, diameters 9.0 and 10.5, spacetime d_H 5.518 and 5.795; D no pressure — identical to C. Per tick: merges and splits matched (about 4,676 and 8,087 in A/B, 4,405 and 7,596 in C/D), forced keeps 0–22, ceiling refusals 1,398–2,404 in A/B and 18,588–32,560 in C/D, and sync refusals zero everywhere. E1 AS EXPECTED (structure, both budgets, the ceiling and flip neutrality exact in all 16 runs, event counts held); E2 AS EXPECTED; E3 NOT AS EXPECTED (B's spacetime 5.98 and 6.44 against the expected 3.3–4.7); E4 AS EXPECTED; E5 NOT AS EXPECTED (every slice too small to measure, diameters 9–12.5 against the flat 15 and 18). Exit by the rule as written: 'Too small to measure at these sizes.' | CHECKED | `model/c3f_run1.txt`, `model/c3f_run1.log`, `model/c3f_runs/*.json`; run 2026-09-18/19. | C92, C93, C94, D42 | — |
| C96 | Reasoning (paper), what C3f means: the balances are finished — event counts held at 1.00 (A, B) and 0.94 (C, D), structure and both budgets and the ceiling and flip neutrality were exact in all 16 runs, merges and splits ran neck and neck with almost no forced keeps, and nothing that broke C3c, C3d or C3e broke here. But sync as a condition is toothless for a structural reason: A and B are identical to the last digit, as are C and D, because the condition refuses only moves that would create a link carrying too much strain, while every move creates links between events that are already neighbours or share a parent and therefore have nearly equal tick counts; the high-strain links are old ones the condition never re-examines. So the reading was not really tested, and what was tested is commitment and curvature twice. The spacetime readings were defined everywhere and read d_H 5.5–6.4 where flat 3+1 should read about 4, with slices too small across to measure (diameters 9–12.5 against 15 and 18); the pressures change the pattern but not toward flatness, giving sparser slices (5.15 links per event against 7.15) that are nonetheless more connected in spacetime (6.44 against 5.80). So with the counts holding and no setting in range, the honest wall is the exit rule's other branch, 'ED's local weights don't select smooth geometry against entropy', even though the 'too small to measure' branch fired first on the slice readings. Options: (a) record the wall and conclude road C, then a full attempt 7 write-up and stock-take; (b) give sync teeth by re-reading the condition to apply to links that exist after a move rather than only to links created, then rerun B against A (about 5 h); (c) larger slices so the slice readings resolve. Proposal (b), then (a) regardless of its outcome. Inputs unchanged (3). Awaiting Allen. | CONDITIONAL | `C3f_Results.md`. | C57, C88, C90, C95 | — |
| C97 | Created-link strain diagnostic (not pre-registered, D43; `model/c3g_created_strain.py`, setting A at V = 8,000, 25 ticks, seed 0): the strain carried by links the moves actually create, in the calibration's normalized units, is mean 0.74 with 95th percentile 2.43 and largest 7.99 in the first five ticks, mean 1.30 with 95th 3.94, 99th 5.72 and largest 14.63 around tick 15, and mean 2.20 with 95th 6.59, 99th 9.61 and largest 20.32 by tick 25, against the flat calibration's largest link strain of 6.461 and the whole slice's mean 1.11 and 95th 4.16. So C3f's threshold of 9.69 (1.5× flat) sits at about the 99th percentile of created-link strain and should refuse about one created link in a hundred by tick 25. | CHECKED (diagnostic) | `model/c3g_created_strain.txt`, `.log`; 2026-09-19. | C94, C95 | — |
| C98 | Code error found and fixed (Claude's ninth in road C3), which explains C3f's identical settings: the sync threshold is taken from the flat calibration, whose solver runs with rate spread and pull both 1, so it is in normalized units of order 6.5, while the condition compared it with the run's raw tick differences, which are of order σ/K = 0.001; the check could therefore never fire, which is why C3f's setting B was identical to A and C to D digit for digit (C95). The comparison now divides by σ/K before testing. Harness check after the fix: at a normalized threshold of 2.0 the condition refused 3,078 moves over 15 ticks on a small torus, where it had refused none; a runner smoke test gives 156.8 sync refusals per tick in setting B against 0 in A, with B's run differing from A's. C3f run 1's results stand as recorded, produced by the unfixed version, and what they tested was commitment and curvature twice. | CHECKED (code fix) | `model/c3f.py` (scaled comparison, commented); harness checks 2026-09-19. | C95, C96, C97 | — |
| C99 | C3g run 1 (`model/c3g_run.py`, C3f rerun with the units fix C98 and one added expected result E6, all in its header before any results; calibration gate then 16 growth runs, 4.4 h on 3 workers): gate passed as in C3f. Medians of 2 seeds at 8,000 / 13,824 — A commitment and curvature: sync refusals 0 per tick, slice diameters 10.5 and 12.5, 5.169 and 5.150 links per event, spacetime d_H 5.977 and 6.439, largest link strain 15.57 and 13.83, late size 1.001 and 1.002; B the same with the sync condition: sync refusals 752–1,489 per tick at the larger size and 13–28 for C, slice diameters 13.5 and 15.0, 5.121 and 5.118 links per event, spacetime d_H 5.894 and 6.329, largest link strain 19.62 and 17.59, late size 1.009 and 1.008, splits abandoned 0.1–0.3 per tick, forced keeps 41 and 76; C sync alone: diameters 8.5 and 10.5, spacetime 5.507 and 5.753, late size 0.941 and 0.942; D no pressure: diameters 9.0 and 10.5, spacetime 5.518 and 5.795, late size 0.942 and 0.940. E0 AS EXPECTED; E1 AS EXPECTED; E2 AS EXPECTED; E3 NOT AS EXPECTED (B's spacetime 5.89 and 6.33 against 3.3–4.7); E4 AS EXPECTED; E5 NOT AS EXPECTED (every slice still too small to measure); E6 AS EXPECTED (the condition fires and B differs from A throughout). Exit by the rule as written: 'Too small to measure at these sizes.' | CHECKED | `model/c3g_run1.txt`, `model/c3g_run1.log`, `model/c3g_runs/*.json`; run 2026-09-19. | C95, C97, C98, D43 | — |
| C100 | Reasoning (paper), what C3g means: the sync condition works in the direction ED argued for and not nearly far enough. Slices widened for the first time in six 3D runs, diameters going 10.5 to 13.5 and 12.5 to 15.0 against the flat slice's 15 and 18, and the spacetime reading moved 6.44 to 6.33 where flat 3+1 should read about 4; the largest link strain rose from 13.83 to 17.59, as it must, since refusing to create strained links leaves strain on older links rather than relieving it; growth was not strangled (splits abandoned 0.1–0.3 per tick, event counts 1.01). Sync alone does almost nothing (C against D: diameters 8.5 against 9.0, spacetime 5.51 against 5.52), so it matters only alongside commitment and curvature, which are what make the slice sparse enough for strain to build. The wall therefore stands — no setting near flat, slices too narrow to read — but its shape is clearer, and the one reading ED actually argued for is the only thing that has pushed the pattern toward flatness, from a threshold set at the 99th percentile of created-link strain. The question that decides whether this is a mechanism or a rounding error is whether a tighter threshold pushes further. Options: (a) a short labelled threshold scan (setting B, larger size, one seed, thresholds 6, 3, 1.5 instead of about 10; about 1.5 h); (b) record the wall and conclude road C, then the attempt 7 write-up and stock-take; (c) the compiled port and slices ten times larger so the slice readings resolve. Proposal (a), then (b) or (c) by what it shows. Inputs unchanged (3). Awaiting Allen. | CONDITIONAL | `C3g_Results.md`. | C96, C99 | — |
| C101 | Sync threshold scan (not pre-registered, D44; `model/c3g_threshold_scan.py`, setting B at V = 13,824, seed 0, T = 150, thresholds 6, 3 and 1.5 against C3g's about 10): threshold 10 gave events 1.008× the start, slice diameter 15.0, slice readings undefined, spacetime d_H 6.329, 1,403 sync refusals and 76 forced keeps per tick; threshold 6 gave 1.055×, diameter 25.0, undefined, 6.350, 3,993 and 376; threshold 3 gave 1.173×, diameter 23.0, slice d_H 4.801 with d_s 1.348, spacetime 5.569, 9,902 and 1,156; threshold 1.5 gave 1.301×, diameter 16.0, slice d_H 4.386 with d_s 1.592, spacetime 5.390, 54,702 and 5,735, with 719 splits abandoned per tick. So the push does not saturate but does not point at flat space: diameters overshoot the flat 18 (25 and 23), the slice readings once defined say branched (spectral dimension 1.35 and 1.59, below the branched flag's 2), the spacetime drifts toward 4 only as the slice goes stringy, and the tightest setting strangles growth with the event count drifting 30%. | CHECKED (diagnostic) | `model/c3g_threshold_scan.txt`, `.json`, `.log`; note 29 scan section; 2026-09-19. | C99, C100 | — |
| C102 | Reasoning (paper), what the scan settles for road C: tightening the sync condition trades one bad shape for another — loose gives crumpled, tight gives branched — and at these sizes there is no flat middle between them. That is the same shape as the known result for random three-dimensional geometry, where the crumpled and branched phases meet at a first-order transition with no smooth phase between (C54, C72), except that here it arises under ED's own meanings and conserved budgets rather than under a tuned coupling. Combined with C3f and C3g, road C3's position is: the counts, structure, budgets, ceiling and rewiring are solved and hold; the one reading ED argued for does act and moves the pattern, but along the crumpled-to-branched line rather than toward flat; no setting at these sizes reads as flat three-dimensional space, and the slice readings resolve only when the slice has already gone stringy. What remains untested is whether a flat middle exists at sizes ten times larger, which needs a compiled model, and whether judging the grown spacetime rather than the slice changes the 2D result (C3a was judged by the stricter standard, C89). Inputs unchanged (3). | CONDITIONAL | `C3g_Results.md` (scan section). | C54, C72, C89, C96, C100, C101 | — |
| C103 | Road C verdict (D45, note 30 `Road_C_Verdict.md`), recorded and the road closed: ED's local growth rules, with its conserved budgets and its three meanings, move the pattern along a line from crumpled to branched, and at the sizes reachable here (up to 13,824 events per slice, 150 ticks) there is no flat three-dimensional middle between them; the counts are solved and the shape is not. Reached on the road: C1 reproducing 2D CDT (pass), C2a Budgeted Causality (pass, retested), C2b Synced Now (pass on run 3), C3b the static three-meanings check (pass); C3a not reached and judged by a standard later corrected (C89); C3c–C3g the wall. Solved and holding: the event balance, the link balance, topology-safe moves with paired cut-and-rejoin, the readings with their calibration gate and an honest 'too small to measure' outcome, and guards that turn runaways into recorded outcomes. What the wall is not: not a code failure (structure, budgets, ceiling and rewiring exact in every C3f and C3g run; incremental cost matching a full recomputation to 5e-13), not a failure of sync's reading (once the units bug was fixed it acted, and was the only ingredient that moved the pattern), and not a statement about larger sizes (every slice here was at most 24 events across). In the literature the same shape appears as random 3D geometry's first-order crumpled-to-branched transition with no smooth phase between (C54, C72), which CDT escapes by summing over geometries with a causal slicing rather than growing one history under local weights; ED's structural difference held throughout, that what CDT tunes ED conserves, which bought the counts and not the shape. Carried forward: whether a flat middle exists about ten times larger (needs a compiled model), whether the 2D result changes when judged on the grown spacetime, road D on chance (C83), and whether sync can act other than as a move-by-move condition. Inputs supplied 3, unchanged. | CONDITIONAL (road closed) | `Road_C_Verdict.md`. | C8, C31, C38, C63, C95, C99, C101, C102 | — |
| C104 | Attempt-level stock-take (D47, note 31 `Taking_Stock_3.md`), reasoning only. Attempt 7 walked attempt 6's single open problem as road C and ended with four passes — C1 reproducing 2D CDT, C2a Budgeted Causality (retested), C2b Synced Now (run 3), C3b the static three-meanings check — C3a not reached, and C3c–C3g at the wall (C103). Cost: about 44 hours of compute, of which 37.1 are recorded run totals; 8 models; 6 labelled diagnostics; 103 claims, 46 decisions, 54 recorded steps, 31 notes. Census: inputs supplied 3, unchanged all attempt; knobs used or added are k, L*, K, sigma, alpha, lambda, gamma, the neighbour ceiling, the link density, the sync threshold and the run length, none derived and each recorded. Walls: wall 1 (rest frame) unchanged as a risk but with the new reading that a single large-scale rest frame comes out of sync rather than being imposed (C28), with local Lorentz invariance untouched; wall 3 (growth) moved from 'no rule at all' to a rule that holds the counts, keeps topology and rewires without splitting yet does not produce smooth geometry, so the wall is now about shape; wall 4 (numbers) has more knobs than before, all recorded; walls 2 and 5 untouched. Worth: the structural difference that what CDT tunes ED conserves, Synced Now, the static check, and a complete record including nine of Claude's specification and code errors. Deductions: nothing derived, no new number or measurable, two of three 3D runs spent on Claude's bookkeeping, and resolution a hard limit (slices of 14,000 events are 24 across). Carried forward: larger sizes via a compiled model, the 2D result judged on the grown spacetime, road D on chance (C83), sync acting other than move by move, and whether ED's conserved balance is a critical point. | CONDITIONAL (stock-take) | `Taking_Stock_3.md`. | C103 | — |
| C105 | Attempt 7 concluded (D48, RD56) and attempt 8 opened. What attempt 8 inherits, summarised in A8 note 1 `Carry_Forward.md`: the meanings of attempts 4–6 unchanged, plus those decided in attempt 7 — budget passed forward and conserved (D8), sync as a shared now with persistent rates (D11, D12), the link budget conserved (D30), cut-and-rejoin only in pairs (D35), sync in growth as a condition rather than a reward (D39, D41), readings taken on the grown spacetime with a calibration gate and 'too small to measure' as its own outcome (D40), and the names Commitment Dynamics, Budgeted Causality and Synced Now (D15). Inputs supplied 3, unchanged through the attempt. Walls: 1 rest frame a risk, with the new reading that a single large-scale rest frame comes out of sync (C28); 2 dark sector untouched; 3 growth restated — ED has a rule that holds the counts, keeps topology and rewires without splitting, and it runs crumpled to branched with no flat three-dimensional middle at reachable sizes (C103), so the wall is now about shape; 4 numbers inherited with eleven recorded knobs; 5 handle-free rule chosen. Standing results: C1, C2a, C2b, C3b. Reusable tools: c3c rotation-system model with topology-safe moves and an exact check, the conserved link pool with ceiling, paired flips, sync as a one-pass condition, readings with a calibration gate, and the resumable runner. Cost lessons: resolution was the binding limit (13,824-event slices are 24 across), pure Python capped the size at about 3 hours a run, measure a gate before tightening it, and nine of Claude's specification and code errors were caught by runs or tests. Opening road for attempt 8: road E, resolution — does a flat three-dimensional middle exist about ten times larger, which needs a compiled port reproducing attempt 7's runs on identical seeds before it is trusted. Also carried, not opened: road D on chance (C83), the 2D result judged on the grown spacetime (C89), sync acting other than move by move, and whether ED's conserved balance is a critical point. | CONDITIONAL | `Taking_Stock_3.md`; `../ED_Attempt_08/Carry_Forward.md`. | C103, C104 | — |
