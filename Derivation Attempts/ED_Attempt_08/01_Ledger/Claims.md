# Claims (ED_Attempt_08)

*Statuses and rules as in `../../ED_Attempt_01/01_Ledger/README.md`. References to earlier attempts are written A1- through A7-ledger.*

| ID | claim | status | evidence | inputs | outside check |
|---|---|---|---|---|---|
| C1 | Reasoning: carried into attempt 8 from A7-ledger C105 and RD56: the meanings of attempts 4–6 (A7 note 1) and those decided in attempt 7 (budget passed forward and conserved D8; sync as a shared now with persistent rates D11, D12; the link budget conserved D30; cut-and-rejoin only in pairs D35; sync in growth as a condition not a reward D39, D41; readings on the grown spacetime with a calibration gate and 'too small to measure' as its own outcome D40; names D15); inputs supplied 3 (Born rule in form, A2 C11; horizon area law, A5 C17; one time dimension, A5 C37); walls: 1 rest frame a risk with the new reading that a large-scale rest frame comes out of sync (A7 C28), 2 dark sector untouched, 3 growth restated as a wall about shape — ED's rule holds the counts, keeps topology and rewires without splitting, and runs crumpled to branched with no flat three-dimensional middle at reachable sizes (A7 C103), 4 numbers inherited with eleven recorded knobs, 5 handle-free rule; also open: spin, charge, particle identification, PF-Q2, the discrete-to-continuum step, the time-ordered smoothness theory, short waves in the moving sector. Standing results: A7 C1 (2D CDT match), C2a Budgeted Causality, C2b Synced Now, C3b the static three-meanings check. Reusable tools in `../ED_Attempt_07/model/`: the rotation-system slice with topology-safe moves and an exact check, the conserved link pool with ceiling, paired flips, sync as a one-pass condition, readings with a calibration gate, and the resumable runner. Cost lessons: resolution was the binding limit, pure Python capped the size, measure a gate before tightening it, and nine of Claude's errors were caught by runs or tests. Opening road E, resolution: E1 on paper (what the port must reproduce, what counts as a flat middle at the larger size, the exit rule), E2 the port checked against attempt 7 on identical seeds, E3 the larger runs. Also carried, not opened: road D on chance (A7 C83), the 2D result judged on the grown spacetime (A7 C89), sync acting other than move by move, and whether ED's conserved balance is a critical point. | CONDITIONAL | `Carry_Forward.md`. | A7-ledger C105, RD56 | — |
| C2 | Literature (road E, note 2): finite-size effects in three-dimensional dynamical triangulations are large and run against finding a smooth middle — the crumpled phase is dominated by triangulations with singular vertices whose local volume grows in proportion to the whole manifold, and that singular structure only takes over above a certain volume, which is why degenerate triangulations were introduced to cut the effects by about two orders of magnitude; the crumpled-to-branched transition is strongly first order. Separately, Hausdorff dimension is extracted from volume growth with geodesic distance, the radius grows so slowly with volume that much larger volumes are needed for a reliable value, and measurements tend to underestimate, with fits already difficult at 32,000 simplices in two dimensions. Read against A7: the prior is against a flat middle appearing at larger size, and ED's slices are not small in simplex count (13,824 events carries about 79,000 tetrahedra, the upper end of studied 3D DT sizes) but small in linear extent, which is what the readings need. | CITED | hep-lat/9712011, hep-lat/9807026, hep-lat/9810049, 1701.06829, 2208.12718, 2311.06910 (listings); 2026-09-19. | A7-ledger C103 | — |
| C3 | Arithmetic (note 2, road E): for the flat cube-grid slice of side n, V = n^3 events, about 6V tetrahedra and a diameter of about 0.75n hops, so n = 20/24/32/40/52 gives V = 8,000/13,824/32,768/64,000/140,608, tetrahedra 48,000/83,000/197,000/384,000/844,000 and flat diameters 15/18/24/30/39. Ten times attempt 7's largest volume buys 2.2 times the hops, because radius goes as volume^(1/3); a hundred times would buy 4.6 times and is out of reach. Measured Python cost 28.6 s per tick at n = 24 scales by volume to 68/132/291 s per tick at n = 32/40/52, so one 150-tick run is 1.2/2.8/5.5/12.1 h, and a campaign of about 56 runs is roughly 190 h single process (65 h on three workers). At n = 52 the tetrahedron table is 844,000 x 8 four-byte integers, about 27 MB, so memory stops being the constraint that exhausted 15 GB of Python objects in attempt 7. | CHECKED (arithmetic) | `E1_Resolution.md` section 2 and 7; A7 `model/c3f_timing.txt`. | A7-ledger C99 | — |
| C4 | Reasoning (note 2, road E, proposed to Allen as E-Q1): 'is there a flat middle' is not a question a ten-times port can settle — the literature prior is against it (C2) and the readings gain only 2.2x in hops (C3) — so road E is reframed around the character of ED's crumpled-to-branched crossing: as slices grow, does the crossing sharpen (first order, as in dynamical triangulations) or broaden (continuous)? This is answerable at reachable sizes because finite-size scaling needs a sharp order parameter rather than a resolved dimension, and links per event, the branched flag (spectral dimension below 2) and diameter over volume^(1/3) were all measurable at 13,824 events in A7's threshold scan. It is also the one place ED's structural difference could show: dynamical triangulations reach the crossing by tuning a coupling while ED reaches it by conserving a budget with sync's threshold as the knob, and a continuous transition where the tuned version is first order would be a real difference and what a continuum limit needs; a flat middle is a special case of a continuous crossing, so nothing is given away. Census: road E cannot reduce ED's input count, which stays at 3 either way; it settles whether wall 3 stands and can produce one new statement about the balance. | CONDITIONAL (proposed) | `E1_Resolution.md` section 3. | C2, C3, A7-ledger C103 | — |
| C5 | Correction to note 1, recorded before any code (note 2): the port cannot reproduce A7's runs bit for bit on identical seeds, because moves are drawn from Python sets whose iteration order depends on insertion and deletion history rather than on the seed, so a rewrite onto arrays diverges from the first differing draw by construction. This is the same fact Claude got wrong once in A7, when a resumed run was claimed to follow an identical random path. Replaced by a three-tier validation ladder: tier 1, predicates and costs (link condition, budget, ceiling and sync gates, n3_after_exact, dS) exact for the same state and proposed move, integers bitwise and floats to 1e-12; tier 2, a fixed scripted move sequence giving an identical complex (V, E, T, sorted degree sequence, check() passing) in both codes; tier 3, whole runs at n = 24 with eight seeds in the port, with A7's two values per setting falling inside the port's seed-to-seed spread for links per event, diameter, spacetime d_H and the refusal counts. Tier 3 requires the port to supply the spread A7's two seeds could not define, and that cost is counted. | CONDITIONAL (proposed) | `E1_Resolution.md` section 4. | A7-ledger C105 | — |
| C6 | Specification (note 2, road E, proposed): E2 is `c3c`-`c3f` rewritten onto flat integer arrays (tetrahedra as 4 vertices plus 4 face-neighbours, edges and degrees as arrays, random-choice sets as index-swap pools) with the inner loop compiled by Numba, about 1,500 lines of which c3c.py's rotation system and topology-safe moves are the hard part. E3 runs four sizes n = 24, 32, 40, 52 at setting B with sync's threshold scanned and T = 150, plus one T = 300 check at n = 24. Expected results fixed before any code: E3-0 the calibration gate passes at every size; E3-1 structure, budgets, ceiling and flip neutrality exact with event counts within +-10% except at the tightest threshold; E3-2 at n = 52 the slice readings are defined, with at least six usable radii and a diameter of at least 20; E3-3 the crossing sharpens, the threshold range over which links per event moves from crumpled to branched shrinking as a power of volume; E3-4 no threshold gives a flat middle, i.e. no setting reads slice d_H in [2.6, 3.4] with d_s in [2.4, 3.6] at the two largest sizes; E3-5 spacetime d_H stays above 4.7. Flat targets fixed now: slice d_H 3.0, d_s 3.0, links per event 6.70, mean degree 13.4, diameter 0.75n, spacetime d_H 4.0; branched is d_s at or below 2; crumpled is links per event at or above 13.4 or a diameter that stops growing with size. Exit rule: tier 1 or 2 failing is a port bug with no run; tier 3 failing means the port is a different model, said plainly and not used to reinterpret A7; E3-3 and E3-4 both holding records a first-order-like crossing with no smooth middle up to 140,608 events and closes road E with wall 3 standing; E3-3 failing (the crossing broadens) records a continuous crossing where the tuned version is first order and road E continues into exponents; E3-4 failing records a three-dimensional reading at the stated size and threshold, consistent not derived, to be repeated at the next size up before anything else is said; E3-2 failing records 'too small to measure, again' and closes road E as a failed instrument, with the readings rather than the sizes named as what needs replacing. E2 ends with a measured per-tick time at n = 52 and E3 is planned only after that number exists; under about 8x speedup the campaign is cut to three sizes, recorded before any run. | CONDITIONAL (proposed) | `E1_Resolution.md` sections 5-8. | C3, C4, C5 | — |
| C7 | Specification and build (E2, D3): the port is `model/p3_core.py`, `p3_ops.py` and `p3.py` (about 1,550 lines), attempt 7's C3c-C3f slice on flat integer arrays with the inner loop compiled by Numba. Containers: tetrahedra as rows of `tv` with an index-swap pool; per-vertex tetrahedron and neighbour lists in a power-of-two arena with free lists; an open-addressing edge hash with `e_a`, `e_b`, `e_val` and an index-swap pool of valence-3 edges; scratch open-addressing tables cleared by a stamp counter for the per-move edge delta; the tetrahedron-key hash of attempt 7 dropped, because a duplicate tetrahedron is already caught by the face check. Vertex slots are reused from a free list, where attempt 7's ids were monotone, because 150 ticks at n = 52 would otherwise allocate about 10 million slots; this changes iteration order, which C5 already covers. The port carries its own generator (splitmix64 seeding xoshiro256**) rather than numba's, so a run is reproducible from a seed within the port and independent of numba's version. Environment: numba 0.67.0 and llvmlite 0.49.0 installed 2026-09-19 with numpy unchanged at 2.4.4, so attempt 7's code still runs in the same interpreter; readings (`readings.py`, `readings_v2.py`, `c3a.py`, `c3b.py`) copied from attempt 7 with matching SHA-256 prefixes 3d5320d3adb6e407, e25b73ab4fcff02a, c76fc789ee509235, 86d76dfdb28174b5. Kept identical on purpose: the move set including paired flips, the three gates with the units fix, the cost alpha*E + lam*sum(valence - 5.104)^2, the acceptance rule, every constant, and `check()` every tick. | CHECKED (built) | `model/p3*.py`, `model/IMPLEMENTATION_NOTES.md` (written before the code ran). | A7-ledger C92, C98 | — |
| C8 | Verification (E2, D3): tiers 1 and 2 of note 2's ladder (C5) **pass**, over **61,468 comparisons** against attempt 7's `c3c`/`c3f` run in the same interpreter (`model/p3_verify.py`, `p3_verify.txt`). Tier 1, 52,744 comparisons at n = 6 and n = 8 on randomised complexes: the link condition, all four plan kinds (flip23, flip32, merge, split; 11,694 plans built), `n3_after_exact`, `delta_S` at three (alpha, lam) settings, and `allowed` over four pool values, four thresholds and two ceilings (3,840 gate outcomes per size, split budget 2,400 / ceiling 1,664 / sync 656 / passed 2,960) all agree; the largest absolute `delta_S` difference is **8.7e-14** against a 1e-12 tolerance, the two codes summing the per-edge terms in different orders. Tier 2, 8,724 comparisons: 440 scripted moves across both sizes, all four kinds, with V, E, T, the sorted degree sequence, the valence multiset, the valence-3 count, the full tetrahedron list and a clean `check()` matching after every move. **One real defect was found and fixed:** the port encoded 'no threshold' as -1.0 but gated on `s_max > 0.0`, so a threshold of exactly 0.0 was treated as off where attempt 7 treats it as on; a run's threshold is always positive, so it would not have bitten a normal run, but a threshold scan reaching zero would. Recorded as Claude's tenth specification or code error, caught by a test before any run. | CHECKED | `model/p3_verify.txt`; 2026-09-19. | C5, C7 | — |
| C9 | Timing, measured (E2, D3, `model/p3_timing.txt`, `p3_timing_big.log`): the port takes **2.29 s** per tick at n = 20 (V = 8,000) against attempt 7's measured 16.6 s, a speedup of **7.2x**; 10.5 s at n = 32 (V = 32,768); and **38.3 s at n = 52 (V = 140,608)** for setting A and 47.7 s for setting B, each including the structure check, which is 3 per cent of a tick. Peak memory at n = 52 is **755 MB**, against attempt 7 exhausting 15 GB of Python objects at a tenth the size. Scaling is slightly superlinear in volume (cache effects): n = 20 to n = 32 is 4.1x the volume and 4.6x the time. Against attempt 7's volume-scaled 291 s per tick at n = 52, the speedup there is about 7.6x. One 150-tick run at n = 52 is therefore **1.6 h** (setting A) or 2.0 h (setting B), against attempt 7's 12.1 h. Structure, the event budget, the link budget (E + pool = BL exactly) and the ceiling held at every size measured, including n = 52. **The measured speedup is below the 8x that note 2's cost plan (C6) fixed in advance as the point at which the campaign is cut from four sizes to three**; recorded here, before any E3 run, as that rule requires. | CHECKED | `model/p3_timing.txt`, `model/p3_timing_big.log`; 2026-09-19. | C6, C7, C8 | — |
| C10 | Three defects in the port, all found before any result, each by a test or a guard rather than by a run's output (E2, D3). (1) **The sync sentinel:** the port encoded 'no threshold' as -1.0 but gated on `s_max > 0.0`, so a threshold of exactly zero was off where attempt 7 has it on; found by tier 1, which reported mismatches at that one threshold and nowhere else. A run's threshold is positive, so this would not have bitten a normal run, but road E turns exactly this knob. (2) **The plan buffer:** `merge_plan` and `split_plan` wrote a star of unbounded size into a fixed 1,024-entry scratch region, so at n = 20 a large star silently overran into the next region; found by a crash in the first timing run at that size, then bounded explicitly with its own overflow code. (3) **The arena leak:** `vert_free` did not return a freed vertex's two per-vertex list blocks to their size-class free lists, so every split leaked two blocks and the arena's high-water mark grew without limit; found by the capacity guard aborting tier 3 at n = 24, and confirmed fixed by the high-water mark going flat (65,984 at tick 10, 67,296 at tick 40, 67,472 at tick 80). Separately, and not a correctness defect: the first version walked each move's edges twice, once for the gates and once for the cost, which is the same double pass Claude had to fix in A7's C3f; merged into one pass, with the separate entry points kept because tier 1 compares them one at a time. Tiers 1 and 2 were rerun after every fix and still pass. **Claude's recorded specification and code errors now stand at twelve**, nine from attempt 7 and three here. | CHECKED | `model/p3_verify.txt`; `model/p3.py`, `p3_ops.py`; 2026-09-19. | C7, C8 | — |
| C11 | Tier 3 of note 2's ladder (C5), run (E2, D3): the port at n = 24, eight seeds, settings A and B, T = 150, against attempt 7's own two seeds per setting at the same size (`model/p3_tier3_runs/`, `p3_tier3_report.txt`). The sync threshold came out at **10.956**, identical to attempt 7's, an independent check since it is computed from the flat calibration through the copied readings. All sixteen runs survived with structure, the event budget, the link budget and the ceiling clean, and flips link-neutral, every tick. **Seven of eight compared quantities pass** in both settings: links per event (port 5.14-5.17 against attempt 7's 5.14-5.16 at A; 5.10-5.13 against 5.12 at B), tetrahedra per event, slice diameter (12-22 against 12-13 at A; 12-19 against 15 at B), the grown spacetime's mass dimension (6.16-6.57 against 6.41-6.47 at A; 6.15-6.51 against 6.31-6.34 at B), budget refusals (zero everywhere), **ceiling refusals (2,456-2,760 against 2,531-2,696 at B, so the same number of candidates reach the sync gate)**, and merges and splits per tick at setting A. **One quantity does not: the sync refusal count at setting B**, port 1,033-1,289 (mean 1,132) against attempt 7's 1,318 and 1,489, about 19 per cent lower with no overlap; splits and merges per tick and abandoned splits at setting B are marginally outside on the same criterion, which requires both attempt 7 values inside the port's range, though their ranges overlap heavily. Setting A, where sync is off, agrees on everything. | CHECKED (tier 3 not passed on one quantity) | `model/p3_tier3_report.txt`, `model/p3_tier3_runs/*.json`; 2026-09-19. | C5, C7, C8 | — |
| C12 | Follow-up to C11 (diagnostic, not pre-registered, `model/p3_sync_diagnostic.py`): the cause of the sync refusal gap was narrowed and is **not** located. Excluded: (i) the gate itself, which tier 1 compares exactly, including 656 sync outcomes; (ii) the number of candidates reaching it, since ceiling refusals match; (iii) the tick-field update, extracted as `sync_step` and compared against attempt 7's numpy form on the same graph, agreeing to **2.2e-16**; (iv) the threshold, identical at 10.956. What the diagnostic shows, running both codes at n = 12 for 40 ticks on three seeds: the strain distributions nearly agree - the 99th percentile of link strain is 5.15 in attempt 7 against 4.93 in the port, and the largest strain 8.50 against 7.99 - while the refusal count comes out 16 per cent lower in the port (115.9 against 96.9 per tick), the same direction and size as at n = 24. **The refusal count is a tail statistic with very large seed-to-seed scatter:** attempt 7's own three seeds at n = 12 give 88.8, 97.9 and 161.1 per tick, a factor of 1.8, and the port's three give 67.8, 125.9 and 97.0, so the two sets overlap at that size. Attempt 7 has only two seeds at n = 24, which is why its range sits where it does; whether the gap is a real difference between the codes or a two-seed artefact is **not settled by the evidence in hand**, and is recorded as open rather than resolved either way. | CHECKED (inconclusive) | `model/p3_sync_diagnostic.txt`; 2026-09-19. | C11 | — |
| C13 | The cause of C11's one tier 3 failure, found and fixed (E2, D4): **Claude's tier 3 runner used the wrong calibration seed**, not the port. C3g set the sync threshold to 1.5 times the flat calibration's neck strain, and that calibration's reading seed was `SIZES.index(n)`, which is 0 at n = 20 and **1 at n = 24**; the tier 3 runner passed 0 at n = 24. The flat slice's neck strain is 7.3039 at reading seed 0 and 6.6569 at reading seed 1, so the port's runs used a threshold of **10.956 where attempt 7 used 9.985**, 9.7 per cent higher. A higher threshold refuses fewer moves, which is exactly and only the quantity that failed, and explains why every threshold-insensitive quantity passed. Checked directly: the port's own calibration at reading seed 1 returns **6.6569**, matching attempt 7's stored calibration to four decimals, so the port's calibration was never wrong. The runner now reads the threshold from attempt 7's own calibration file so the two cannot drift apart again, and the eight wrong-threshold runs are kept as `model/p3_tier3_runs_wrongthreshold/` rather than deleted, since they are the evidence for this diagnosis. Setting A is unaffected, sync being off there, and those eight runs stand. **Claude's recorded specification and code errors now stand at thirteen**, nine from attempt 7 and four here; this one is in a test harness rather than in the model, and it produced a false negative, not a false pass. | CHECKED | `model/p3_tier3.py`, `model/p3_tier3_runs_wrongthreshold/`; 2026-09-19. | C10, C11, C12 | — |
| C14 | The tier 3 criterion, replaced before the data that tests it existed (E2, D4). With the threshold corrected (C13), the port's setting B at n = 24 over eight seeds gives sync refusals **998-1,672, mean 1,402**, against attempt 7's 1,318 and 1,489, mean 1,403 - inside, and the means agree to one part in fourteen hundred, which settles C11 and C12: the gap was the wrong calibration seed and nothing else. Three quantities still failed the criterion as written, and the criterion is what is wrong: 'both of attempt 7's two values inside the port's eight-seed min-max' is too strict, because the min-max of eight samples does not cover a population and a fresh draw falls outside it about a fifth of the time. Merges and splits per tick missed by **0.19 and 0.12 in about 8,065**, or 0.002 per cent, with the two ranges overlapping 94 and 99 per cent; abandoned splits missed by 4.8 per cent on numbers of order 0.3 per tick. **Replaced by a symmetric criterion, written down while attempt 7's six extra seeds were still running and before any of their values had been seen (zero of six finished):** with eight seeds on each side, a quantity passes when the two eight-seed ranges overlap AND the two means differ by less than the pooled seed-to-seed standard deviation; a Mann-Whitney U p-value is reported for the record and is not part of the gate. Recorded this way, and at this moment, so that the change cannot be a choice made after seeing whether it helps. | CONDITIONAL (criterion) | `model/p3_tier3_report.py` docstring; `model/p3_tier3_runs/`; 2026-09-19. | C11, C12, C13 | — |
| C15 | **Tier 3 passes** (E2, D4, `model/p3_tier3_report.txt`). Eight seeds of the port against eight seeds of attempt 7's own code at n = 24, setting B, T = 150, both at attempt 7's threshold of 9.985, judged by the symmetric criterion of C14 that was fixed before attempt 7's six extra seeds finished. All sixteen port runs and all eight attempt 7 runs survived with structure, the event budget, the link budget and the ceiling clean and flips link-neutral, every tick. All eight quantities pass, with the two ranges overlapping and the means differing by far less than the pooled seed-to-seed standard deviation: links per event 5.11 against 5.11 (|dmean| 0.002, sd 0.013); tetrahedra per event 4.11 against 4.11; slice diameter 15.88 against 15.25 (0.63 against 2.46); the grown spacetime's mass dimension **6.30 against 6.34** (0.033 against 0.157); **sync refusals 1,402 against 1,391** (11 against 167); merges 8,064.15 against 8,063.47; splits 8,065.15 against 8,066.20; abandoned splits 0.28 against 0.27. Mann-Whitney U p-values, reported for the record and not part of the gate, run from 0.645 to 0.878, so there is no sign of a difference in any of them. Setting A, with sync off, passes on attempt 7's two seeds. Two further points for the record: attempt 7's own eight-seed range for sync refusals is 1,265-1,499, so its original two seeds sat near the middle of its own distribution and were not unrepresentative; and the port's eight runs at the **wrong** threshold gave 1,033-1,289, below that range, so tier 3 detected a real 9.7 per cent change in a knob and then cleared the model once the knob matched - it discriminated in both directions rather than merely agreeing. **The three-tier ladder of note 2's C5 is complete: the port is attempt 7's model.** | CHECKED | `model/p3_tier3_report.txt`, `model/p3_tier3_runs/`, `model/a7_extra_runs/`; 2026-09-19. | C5, C8, C11, C13, C14 | — |
| C16 | Specification (note 3, E3, proposed as E3-Q1): the sync threshold's definition is changed from attempt 7's. Attempt 7 set s_max = 1.5 x the flat calibration's largest link strain, and that calibration draws random rates, so its value depends on a reading seed: at n = 24 the flat slice's neck strain is 6.657 at reading seed 1 and 7.304 at reading seed 0, a 10 per cent spread in what should be a fixed property of flat space, and that is precisely what made tier 3 fail (C13). E3 uses s_max(f, n) = f x the median over eight reading seeds of the flat slice's largest link strain at n, with f the scanned control. This costs eight sparse solves per size, once. It matters doubly because E3's measurement is the width of a crossing in f, so a 10 per cent jitter in the abscissa would swamp the quantity being measured. Recorded consequence: E3's f = 1.5 is not exactly attempt 7's threshold, nothing in E3 is compared to attempt 7, and the two must not be conflated later. | CONDITIONAL (proposed) | `E3_Model_Spec.md` section 1. | C13, A7-ledger C92 | — |
| C17 | Reasoning (note 3, E3, proposed as E3-Q2): which quantity is the order parameter for ED's crumpled-to-branched crossing is **not known**. Attempt 7's threshold scan (A7 C101) was one seed, one size and four thresholds, and did not record several quantities; it shows that the pattern moves with the threshold and reads branched at tight thresholds (spectral dimension 1.35 and 1.59), and that the tightest setting strangles growth (719 abandoned splits per tick), which is a third outcome and not the branched phase. Diameter is not monotone across the scan, rising from 15 to 25 and falling back to 16, because strangling sets in. E3 therefore runs in two parts and only the second is pre-registered: **E3a, a pilot** at n = 24, one seed, nine values of f, exploratory and labelled, whose job is to locate the crossing and identify which quantity moves cleanly through it, and whose output is not a result; then **E3b, the campaign**, with the order parameter, the crossing's location and the ladder spacing fixed from the pilot and written down as a decision before any campaign run. Candidates judged by the pilot, with their flat, crumpled and branched values: stringiness R = diameter / V^(1/3) (flat 0.75, favoured going in because it is free and size-aware), links per event (flat 6.70, crumpled flag 13.4), spectral dimension (flat 3, branched at or below 2), the grown spacetime's mass dimension (flat 4) and events against the start. **The pilot is allowed to stop the campaign:** if no quantity moves monotonically across the crossing, E3b does not run and the result recorded is that ED's crossing has no order parameter this design can see. | CONDITIONAL (proposed) | `E3_Model_Spec.md` section 2. | A7-ledger C101, C103 | — |
| C18 | Specification (note 3, E3, proposed as E3-Q3 and E3-Q5): with the order parameter X(f) chosen, the crossing width is W = f(X = X_hi - 0.75*dX) - f(X = X_hi - 0.25*dX) by linear interpolation on the ladder, where dX is X's range across the ladder; W shrinking as a power of volume beyond its error bars records that the crossing **sharpens** (first-order-like, as in dynamical triangulations), W not shrinking or shrinking with an exponent consistent with zero records that it **broadens or holds**, making a continuous crossing live and continuing road E into exponents, and W unmeasurable at the largest size records 'not resolved at these sizes'. The seed-to-seed spread of X is recorded at every f, since it peaks at the crossing and becomes double-peaked for a first-order one; **three seeds at n = 52 cannot see a double peak, which is a stated limitation of this design rather than something it avoids**. Three expected results are added to note 2's C6: E3-6, a run with more than 10 abandoned splits per tick or an event count more than 20 per cent above the start is recorded as **strangled** and given no shape; E3-7, the pilot's ladder must bracket the crossing, X at the loosest and tightest f differing by more than three times the seed-to-seed spread, or the ladder is redrawn before the campaign; E3-8, the no-sync control must sit within the spread of the loosest f, or f is not the only thing acting. | CONDITIONAL (proposed) | `E3_Model_Spec.md` sections 3 and 5. | C6, C17 | — |
| C19 | Cost plan (note 3, E3), from measured per-tick times (C9) rather than an assumption: the pilot is 9 runs at n = 24, 15.5 minutes each, about 2.3 h single process or 35 minutes on four workers. The campaign is three sizes with six values of f bracketing the crossing, four seeds at n = 24 and n = 32 and three at n = 52, plus one control per size per seed: 28 runs at n = 24 (7.2 h), 28 at n = 32 (12.1 h) and 21 at n = 52 (42 h), about **61 h single process or 15 h on four workers**, with one T = 300 check at n = 24 for whether the outcome is tick-limited. The one soft number is the spacetime reading at n = 52, about 1.5 million events against 292,000 at n = 24 where ball growth took 16 s; it is not yet measured, so **E3b is not planned until a single n = 52 spacetime reading has been timed**, the same rule that governed E2. | CONDITIONAL (plan) | `E3_Model_Spec.md` sections 4 and 6. | C9, C17 | — |
| C20 | Correction to C16's figure, measured before any pilot run finished and before any pilot value was seen (D6): the flat slice's largest link strain at n = 24 over the eight reading seeds the new threshold uses is 7.304, 6.657, 7.051, 7.343, 7.404, 6.808, 7.556 and 8.162, a range of **6.657 to 8.162, a 23 per cent spread**, with median **7.3232**. C16 and note 3 section 1 quoted 10 per cent, which was the spread of the only two seeds then known. The larger figure makes the change to a median over eight seeds more necessary rather than less: a single reading seed lands anywhere in a 23 per cent band, and the crossing width E3 sets out to measure may be narrower than that band, so attempt 7's single-seed threshold would have put more jitter in the abscissa than there is signal in it. The design is unchanged; only the figure justifying it is corrected. | CHECKED | `model/e3_pilot_runs/cal_n24.json`; `E3_Model_Spec.md` addendum; 2026-09-19. | C16 | — |
| C21 | E3a pilot, exploratory and labelled (D6, note 4, `model/e3_pilot.txt`): ten runs at n = 24, one seed, T = 150, 0.79 h on four workers, with the threshold s_max = f x 7.3232 (C16, C20). Of the five candidate order parameters, one is usable. **Links per event** moves monotonically from the control's 5.141 to 5.017 at f = 0.45, a change of 0.124 against the seed-to-seed standard deviation of 0.013 known from tier 3, so about ten times the noise. **Stringiness R = diameter / V^(1/3) is unusable** and was the favourite going in: diameter is an integer and tier 3 measured it scattering 14 to 22 across seeds at fixed settings, which alone spans R from 0.58 to 0.92, wider than the whole range R covers across the ladder. **Spectral dimension exists only on one side**, undefined for f >= 0.55 where slices are too small to measure and 1.29 to 1.47 where it is defined, so it is a phase flag and not an order parameter. The grown spacetime's mass dimension falls 6.45 to 5.86 but not monotonically, on a seed spread of 0.157. Events against the start rises monotonically 1.008 to 1.125 but measures growth, not shape. Growth strangles between f = 0.45 and f = 0.30: at f = 0.30 the event count is 34 per cent above the start, at f = 0.22 splits are abandoned 548 times per tick, and both are recorded as strangled with no shape (E3-6). Both pre-registered ladder checks pass: E3-7, 0.124 against three times 0.013; E3-8, control 5.141 against 5.123 at f = 2.0, about 1.4 standard deviations. | CHECKED (exploratory) | `model/e3_pilot.txt`, `model/e3_pilot_runs/*.json`; 2026-09-19. | C16, C17, C20 | — |
| C22 | **ED's growth cannot reach the crumpled phase, and this follows from ED's own rules rather than from the settings** (note 4 section 3; arithmetic plus recorded run values, not exploratory). Links are conserved, so E <= BL = 6.699 x V0 and therefore mean degree = 2E/V <= 13.40 x (V0/V): with a slice holding its size, mean degree cannot exceed the flat value of 13.40, and reaching the crumpled flag of 26.8 would need the slice to fall to half its starting size, while the runs grow. Every pilot run sits at mean degree about 10.2. The cap is real and not slack: attempt 7's unpressured settings C and D at n = 24 sit at mean degree 14.3 having spent 98 to 100 per cent of the link budget, pressed flat against it, where the pressured settings spend 77 to 84 per cent. Separately, **the no-infinities ceiling blocks the crumpled phase's mechanism**: max degree is exactly 60, the ceiling, in every one of the pilot's ten runs and in every attempt 7 run at n = 24, while dynamical triangulations' crumpled phase is built on singular vertices whose local volume grows in proportion to the whole manifold (C2), which a 60-neighbour bound forbids outright. So ED's conserved link budget and its finite-neighbour bound each independently exclude the crumpled phase. Stated against ED's own crumpled flag (mean degree at or above twice flat), which is A7 D40's operational definition and not the literature's. | CHECKED | `model/e3_pilot_runs/*.json`; `../ED_Attempt_07/model/c3g_runs/*.json`; arithmetic in note 4 section 3; 2026-09-19. | C2, C21, A7-ledger C92, D40 | — |
| C23 | Correction to the wording of attempt 7's wall, and a reframing of E3's question (note 4 section 4). A7 C103 said ED's rules 'move the pattern along a line from crumpled to branched'. **By ED's own crumpled flag, no budgeted growth run was ever crumpled** (C22): attempt 7's crumpled observations came from C3c's density runaway, which happened before the link budget existed, and from C3b's stand-in slices, which were built crumpled by hand to test the readings. The measured evidence stands; the description does not. Consequently **E3's question - does the crumpled-to-branched crossing sharpen or broaden with size - has no crossing to ask about**, because one of its two phases does not exist in ED's growth; the ladder runs from 'too small to measure' to 'branched' and then to 'strangled'. The finding underneath is stronger than the question it replaces: in dynamical triangulations the crumpled and branched phases meet at a first-order transition and that transition is the obstacle to a continuum limit (C2), while ED's conserved budget and finite-neighbour bound remove one of the two phases by construction. This is the first concrete structural consequence of 'what CDT tunes, ED conserves' rather than a numerical one. **What it does not say:** it does not say ED has a continuum limit, or that the remaining side is flat - every measurable slice in the pilot reads branched, d_s 1.29 to 1.47 against flat's 3. Attempt 7's wall stands; only its description changes. | CONDITIONAL | note 4 section 4. | C2, C21, C22, A7-ledger C103 | — |
| C24 | E3b as specified should not run (note 4 section 5, put to Allen). The campaign was about 15 hours to measure the width of a crossing (C18, C19); there is no crossing to measure (C23), so it would produce a width for a drift within one phase and a finite-size scaling of that number would mean nothing. Note 3's E3-Q6 let the pilot stop the campaign if nothing moved monotonically; something does move monotonically, links per event, but the premise the campaign rests on fails, so the rule is invoked in its spirit. The pilot cost 47 minutes and saved about 15 hours, which is what it was for. Recorded alongside: **both pre-registered ladder checks passed while the premise failed**, because E3-7 and E3-8 tested the ladder's magnitude and what was wrong with it was its location. Options put to Allen: (a) record the finding and re-pose road E around whether the branched reading survives at n = 52, where slices are 39 across instead of 19, about 6 hours; (b) test the structural claim by raising the ceiling and the budget, which is a meanings question and Allen's call, not a knob-turn; (c) run E3b anyway, not recommended; (d) stop and take stock on paper. Proposal (a). | CONDITIONAL (proposed) | note 4 section 5. | C18, C19, C21, C23 | — |
| C25 | The n = 52 spacetime reading, measured (D6, C19's one soft number, `model/e3_spacetime_timing.txt`): from 22 kept slices the pattern is **3,091,954 events and 21,895,345 links** - twice the 1.5 million note 3 estimated, because events accumulate over the kept slices - and ball growth on it takes **459 s**, against 16 s on the 292,000-event pattern at n = 24. The reading resolves **five usable radii, 4 to 8**, against three (3 to 5) at n = 24, so the grown spacetime does get better with size as note 2's arithmetic said it would. Two corrections to earlier figures fall out. The per-tick time at n = 52 is **34.1 s run alone**, not the 47.7 s recorded in C9, which was measured while three other workers competed; C9's 7.2x speedup at n = 20 is unaffected, but the n = 52 per-run cost falls from 2.0 h to about **1.4 h plus 7.7 min of reading**. And memory is now the binding constraint rather than time: the machine has 8.4 GB, a single n = 52 slice takes 755 MB, and the spacetime matrix adds roughly 1.5 GB while a reading runs, so **n = 52 supports two or three workers, not four**, which is what note 3's 15-hour campaign estimate assumed. | CHECKED | `model/e3_spacetime_timing.txt`; 2026-09-19. | C9, C19 | — |
| C26 | E4 calibrations, run before any growth run (D7, note 5, `model/e4_gate.txt`). **The flat calibration reads flat, and better with size:** d_H 2.657 at n = 24 (reproducing attempt 7 exactly), 2.740 at n = 32, **2.850 at n = 52**, converging toward 3 as the literature's finite-size warning says it should (C2); d_s 3.050, 3.024, 3.007; mean degree 14.00 throughout; diameter 18, 24, **39**; and usable radii 3-6, 4-8, **7-14**. So note 2's premise is confirmed directly: **bigger slices buy measurement**, and E4-2's requirement of six usable radii at n = 52 is already met by the calibration alone. | CHECKED | `model/e4_runs/cal_FC_n*.json`, `model/e4_gate.txt`; 2026-09-19. | C2, C3 | — |
| C27 | Two defects found by the E4 gate before any growth run, the first Claude's and the second in the gate itself (D7). **(1) The randomised calibration silently overflowed.** RC ignores the link budget by design, so it needs about 14.5 links and 13.5 tetrahedra per starting event; the port's default caps give 10 and 12, and `flip_randomize` did not check the overflow flag that the rest of the port sets. All three RC runs read mean degree **exactly 20.00 = 2 x (10 V0)/V**, the edge array's edge. Caps raised to 24x and the wrapper now raises; RC then reads mean degree 29.2-29.3 at every size, matching attempt 7's 29.3. **Claude's recorded errors now stand at fourteen.** Had the gate not been pre-registered, thirteen growth runs and five hours would have been judged against a truncated calibration. **(2) The gate's own criterion was mis-specified.** It required the randomised calibration to read 'd_H undefined', which was an artefact of n = 24 being too small to fit a dimension at all, not a property of the readings; at n = 32 and n = 52 RC does fit, at 3.251 and 3.286, so the gate fails on a test that encoded the smallest size's limitation. | CHECKED | `model/e4_cal.log`, `model/e4_cal2.log`; 2026-09-19. | C26 | — |
| C28 | A limitation of the readings, exposed by the E4 gate and worth recording on its own (D7). **At n = 52 the shape readings barely separate flat from crumpled.** Flat reads d_H 2.850 and d_s 3.007; the randomised, crumpled slice at mean degree 29.27 reads d_H 3.286 and d_s 3.072, and **both read small-world False**. The two differ by 0.44 in d_H, about twice flat's own drift across the three sizes, and by 0.065 in d_s, which is nothing. What does separate them cleanly is counting, not shape: mean degree 14.00 against 29.27, and diameter 39 against 18. The comparison is also not like for like - flat's fit uses eight radii (7-14) and the randomised slice's four (4-7) - and both calibrations are single-seed, so neither has a spread. **This does not block E4-3**, which asks whether grown slices read branched at d_s near 1.3, far from both 3.01 and 3.07; it does mean the readings cannot be relied on to tell flat from crumpled at this size, and any future claim that rests on that distinction needs a different instrument. | CHECKED | `model/e4_runs/cal_*_n52.json`; 2026-09-19. | C26, C2 | — |
| C29 | The E4 gate replaced, declared before the calibrations that judge it had been run (D8, note 5 addendum). The old E4-0 required the randomised calibration to read d_H undefined, which encoded n = 24's inability to fit a dimension at all rather than any property of the readings (C27). The calibrations now run **five seeds each** where they ran one: flat is a deterministic slice so its seeds vary the reading, and the randomised slice varies both its randomisation and its reading seed, which closes the gap C28 exposed, that neither calibration had a spread. The replacement: **E4-0a, the gate** - at every size and across all five seeds, flat reads d_s within 0.4 of 3.0, d_H defined, at least six usable radii at n = 52, and flat's own seed spread in d_s below 0.5; failing it closes road E as a failed instrument. **E4-0b, recorded and not gating** - flat and randomised have non-overlapping five-seed ranges in at least one shape reading. **E4-0c** - which readings separate the two at each size, with spreads. E4-0b does not gate because E4-3 does not rest on flat-versus-crumpled: it asks whether grown slices read branched at d_s near 1.3, about 1.7 from flat's 3.0, far beyond any spread these calibrations have shown; if E4-0b fails, E4 runs and every claim it makes carries the limitation that the readings cannot tell flat from crumpled at that size. The condition that would stop E4 is flat's own d_s spread exceeding 0.5, since d_s could then not separate 1.3 from 3.0 either - and that spread has not been measured. Recorded at this moment so the replacement cannot be a choice made after seeing whether it helps. | CONDITIONAL (criterion) | `E4_Model_Spec.md` addendum; 2026-09-20. | C26, C27, C28 | — |
| C30 | **The E4 gate passes** on the replaced criterion (D8, C29, `model/e4_gate.txt`), with five seeds per calibration. E4-0a, the gating test, holds at every size: flat reads d_s **3.050, 3.024, 3.007** at n = 24, 32, 52, all within 0.4 of 3.0 and converging toward it; d_H is defined everywhere at **2.657, 2.740, 2.850**; and the fit at n = 52 uses **eight usable radii** against the six required. **Flat's five-seed spread is exactly zero**, bit-identical across reps, and this is not a bug and not a vacuous test but a property of the object: the flat cube-grid torus is vertex-transitive, so ball volumes and return probabilities are the same from every vertex and no choice of 200 centres can change the reading. It does mean that particular sub-test carries no information about grown slices, which are not homogeneous and will have their own spread; that spread comes from E4's three seeds, not from the calibration. The randomised calibration does vary, since its slices differ: d_H 3.286-3.295 and d_s 3.072-3.082 at n = 52, a spread of about 0.01. E4-0b, recorded and not gating, separates flat from randomised on d_H and d_s at n = 32 and n = 52 and on nothing at n = 24 where the randomised readings are undefined - **but the separation in d_s at n = 52 is only 0.065**, so C28's limitation stands in substance even though the formal test passes, and any grown slice whose own seed spread exceeds 0.065 cannot be told from crumpled by d_s. The growth runs are launched on this basis, at s_max = 0.45 x 7.3232 = 3.2954 at n = 24. | CHECKED | `model/e4_gate.txt`, `model/e4_runs/cal_*_r*.json`; 2026-09-20. | C26, C28, C29 | — |
| C31 | E4 cut short at n = 52 by memory, recorded as a change to what E4 measures (D9). The n = 24 and n = 32 runs finished (eight runs, 0.95 h); the n = 52 runs then failed in the **spacetime** reading, not in growth: one reading over 3,258,054 events needs 200 centres x 3.26 million x 8 bytes = **5.2 GB for the distance array alone**, plus about 1.25 GB of slice and sparse matrix, so about **6.5 GB on a machine with 8.4 GB**. Two workers cannot both do it and one is marginal - the standalone timing run of C25 succeeded at 3.09 million events only because nothing else was running. **What changed:** at n = 52 the growth runs now take the slice readings only, which need 200 x 140,608 x 8 = 225 MB and are what E4-3 actually asks for; the grown spacetime's mass dimension, E4-5, is reduced from three seeds to **one**, whose slices are saved and read afterwards alone by `model/e4_spacetime52.py`. If that one reading also runs out of memory, **E4-5 is recorded as not measurable at n = 52 on this machine** and the slice readings stand on their own. The change touches only the context reading, not the question, and is recorded here rather than folded in silently. Note 5's cost plan had memory as the binding constraint at n = 52 (C25) and still underestimated it, because it costed the slice and not the reading. | CHECKED | `model/e4_grow.log`; 2026-09-20. | C25, C26 | — |
| C32 | **Sync means a whole slice agreeing, not a move-by-move veto** (D10, Allen's decision, recorded 2026-09-20 **before the n = 52 scale curve finished**, so it cannot be read as a response to that result). Allen: 'sync was always about a whole slice agreeing, not one move - is what my instinct is.' The record shows where the meaning drifted, in three steps none of which was a decision: in attempt 6 sync was that rates **can match** across a region (A6 D6, D7); in A7 C2b, Synced Now, it was that the **wobble across a region of size L** shrinks relative to how fast influence travels, so a common now survives for d >= 3 - a statement about an aggregate over a region, compatible with neighbouring clocks differing substantially; in A7 C3e it became a **sum over links** of squared clock difference added to the cost; and in A7 C3f (D39-D41) it became a **refusal of any move creating one link above a strain threshold**. Each step was taken for a modelling reason - a cost function needs local terms, a condition needs to be checkable per move - and the last two impose a strictly stronger requirement than Synced Now derived, forbidding local roughness that Synced Now never forbade. **Consequence, recorded plainly: every three-dimensional growth run in attempts 7 and 8 tested a local veto, not sync as ED means it.** Those runs are not discarded; they are reclassified as tests of a local veto. Claude's reading, offered and labelled as such: in Synced Now sync was a **diagnostic** - given a slice of dimension d, can a common now survive - and in the growth model it became a **driver** that builds the pattern, which is a change of job that was never decided; a filter on finished slices is not obviously a rule for choosing the next move. | CONDITIONAL (meaning, Allen's decision) | `E4a_Sync_Meaning.md`; A6 D6, D7; A7 C2b, C3e, C3f, D39, D41. | A6-ledger D6, D7; A7-ledger C2b, D39, D41 | — |
| C33 | A mechanism for why the local veto would make slices stringy, offered with the evidence that prompted it (note 6, alongside C32). If a move is refused whenever it would create a link whose two clocks disagree, **growth can only proceed where clocks already agree**, so the pattern follows narrow corridors of agreement and produces filaments. The E4 runs are consistent with this: at n = 52 the three runs with the condition on read spectral dimension **1.635, 1.646, 1.685**, and the two with it off read **2.319 and 2.320** - the highest, most space-like values this project has produced, and identical to three figures across seeds. So switching the condition off moves the slice toward three dimensions rather than away, which is the opposite of what sync was introduced to do. **What this does not say:** removing the condition does not rescue the growth rule. The controls read 2.32, not 3, with commitment and curvature still acting, so sync's form is part of the wall and not all of it; the local costs remain and still lose to entropy, which is A7's C3a lesson that local costs do not act at large scales. | CHECKED (runs) / CONDITIONAL (mechanism) | `model/e4_runs/grow_n52_*.json`; 2026-09-20. | C32, A7-ledger C89 | — |
| C34 | E4 growth runs (D7, note 7, `model/e4_run.txt`, `model/e4_runs/`): thirteen runs at f = 0.45 over n = 24, 32 and 52, three seeds each with no-sync controls at the two larger sizes, T = 150, about 6 h. **All survived, with structure, the event budget, the link budget, the ceiling and flip neutrality exact at every tick of every run**, including at 156,172 events. Fitted spectral dimension with the sync condition on: 1.393-1.422 at n = 24, 1.392-1.498 at n = 32, 1.635-1.685 at n = 52; with it off, 1.728-1.784 at n = 32 and **2.318-2.319** at n = 52. Fitted mass dimension 4.74-6.21 against flat's 2.657-2.850. Flat reads d_s 3.050, 3.024, 3.007 at the same sizes. **E4-4 held at ten times the size**: mean degree 9.98-10.31 against the budget's bound of 11.73-13.44, and max degree pinned at the ceiling of 60, in all thirteen runs, so C22's structural claim stands. | CHECKED | `model/e4_runs/grow_*.json`; 2026-09-20. | C22, C29, C30 | — |
| C35 | The fitting window never widened with size, which changes what every E4 number means (note 7 section 2). Flat's ball-growth window grows 3-6, 4-8, **7-14** at n = 24, 32, 52; the grown slices' window is **2-4, 2-4, 3-5**. So every fitted spectral and mass dimension in C34 describes a **two-hop span at short range**, and the apparent upward drift with size, 1.41 to 1.44 to 1.66, is the window sliding rather than the geometry changing. **Recorded as Claude's error of interpretation:** the branched reading was reported to Allen as a finding, and described as 'clear and negative', before the fitting windows had been looked at. They should have been the first thing checked. | CHECKED | `model/e4_runs/*.json`; 2026-09-20. | C34 | — |
| C36 | **ED's spectral dimension runs with scale** (note 7 section 3; `model/e4_dscurve.py`, `model/p3_dscurve.py`, diagnostic, not pre-registered). Each slice was re-grown exactly - the port's generator is seeded, and all three re-grown slices matched their runs to the event and to the link - and then measured at every walk radius instead of over one window. At all three sizes the curve has the same shape: about 2.35 at radius 1.4, a **minimum of 1.36, 1.43 and 1.48 at radius 2.6-3.0**, then a monotonic rise through 2 and 3, a peak, and a collapse at saturation. **The single fitted number was always the bottom of the dip**: the n = 52 run fitted 1.685 over radii 3-5 where the curve's minimum is 1.48 at radius 2.8. The flat slice's curve is nearly constant at 3.0-3.3 across the same radii before collapsing, so the dip is a real feature of grown slices and not an artefact of the method. Running dimension is the shape causal dynamical triangulations reports, and it had never been looked for in ED. | CHECKED (diagnostic) | `model/e4_runs/dscurve_n*.json`; 2026-09-20. | C34, C35 | — |
| C37 | **The rise is a finite-size effect, and bigger slices are ruled out as a route** (note 7 section 4). Written down **before the n = 52 curve ran**: the walk's reach should come in near 11.6 if the slices are three-dimensional at large scales, or near 8.5 if the rise is a finite-size effect. **It came in at 8.45.** Across the three sizes the reach is 6.68, 7.37, 8.45 for 15,557, 36,980 and 156,172 events, so **ten times the volume bought 26 per cent more reach**, and fitting gives reach ~ V^0.101, an effective dimension of **9.9**; flat slices measured the same way give V^0.333, exactly three. The peak of the curve also **rises** with size, 3.88, 4.00, 4.94, where real three-dimensional behaviour would settle near 3, which marks the peak as the walk reaching equilibrium rather than a dimension. **The consequence is structural, not computational:** the walk saturates before the spectral dimension can settle, and at V^0.101 **doubling the reach takes about 943 times the volume**, against an n = 52 run costing 1.4 h. **Road E's question is answered and the route it was built on is closed: bigger slices cannot settle the shape of ED's grown space, with any amount of time or any faster model, because the geometry is too compact for the measurement to converge.** By note 5's exit rule: ED's growth, with its conserved budgets and its three meanings, gives a branched slice at every size reachable here, up to 156,172 events, and attempt 7's wall stands in its corrected form. | CHECKED | `model/e4_runs/dscurve_n52_s0.json`; 2026-09-20. | C36, A7-ledger C103 | — |
| C38 | Road E closed with its verdict recorded (note 8 `Road_E_Verdict.md`, written in plain language). **ED's growth rule makes stringy, extremely compact patterns rather than space, at every size reachable, and bigger sizes are ruled out as a way of changing that answer; attempt 7's wall stands and the route road E was built to try is closed** (C37). What road E found: (i) ED cannot reach the crumpled phase, forbidden independently by the conserved link budget and the no-infinities ceiling, which removes one of the two phases whose meeting blocks a continuum limit in dynamical triangulations (C22); (ii) ED's spectral dimension runs with scale, dipping to 1.36-1.48 and rising, and every number attempts 7 and 8 had reported was the bottom of that dip (C36); (iii) the rise is finite-size, since ten times the volume bought 26 per cent more reach, giving reach ~ V^0.101 against flat's V^0.333, so doubling the reach would take about 943 times the volume (C37); (iv) the sync condition pushes the geometry away from space, its controls reading 2.32 against 1.64 with it on, and Allen decided sync means a whole slice agreeing rather than a per-move veto, which reclassifies every 3D growth run in attempts 7 and 8 (C32, C33). Cost: about 28 h of recorded run time and roughly 35 h in total; 37 claims, 10 decisions, 13 recorded steps, 8 notes; fourteen recorded Claude errors in code or specification, all caught before reaching a result, plus one error of interpretation (C35). Inputs supplied unchanged at 3; no new number and nothing measurable. Untouched by this: Budgeted Causality and Synced Now, neither of which is about growing the pattern. Three roads open: whole-slice sync as Allen has defined it; the local costs, which arrived the same way sync did; and whether a single grown history can give smooth geometry at all, which is a conversation for Allen and may decide whether the other two are worth running. | CONDITIONAL (road closed) | `Road_E_Verdict.md`. | C22, C32, C33, C35, C36, C37 | — |
| C39 | **Allen's decision on road 3 (D11): 'once something has happened it's fixed, but many could have happened.'** So ED's meaning 'what happened stays happened' **does not bar ED from being an ensemble theory**, and neither roads 1 and 2 nor a sum over histories is ruled out on principle. Recorded with the survey that prompted it (note 9): the sum over histories is a calculational device that most readings can use while holding that exactly one thing happened; many-worlds takes all branches as real; collapse readings take one as happening with the sum as bookkeeping; **consistent histories (Gell-Mann, Hartle, Griffiths) is a framework about sets of histories and when probabilities may be assigned to them, and is the closest to ED's picture**; CDT uses the sum to compute geometry and takes no position on what happened. Allen's reading that it 'seems like a math trick' is half right: it is a device in most readings, and one that keeps being correct. | CONDITIONAL (meaning, Allen's decision) | `Road_3_One_History.md`. | A5-ledger D10 | — |
| C40 | **Allen's clarification, which corrects the record (D12):** 'a node doesn't stay committed, it passes it on; the history isn't recorded anywhere or known to the present; to the present, any set of events that led to the present is possible; you can undo history, but you can't really know what it was.' **If the present does not encode a unique past, ED already holds that the past is a set of possibilities rather than a recorded thread, so ED is an ensemble theory and has been all along** - the growth model simply never treated it as one. **Two corrections Claude owed, recorded here.** (1) **'Stringy' gave the wrong picture:** a slice reading spectral dimension 1.4 describes the shape of space **at one tick**, a tangle of thin filaments filling a region, with nothing persisting, since the pattern is remade every tick; Allen read it as worldlines running through time, reasonably, given how Claude wrote it. (2) **Forbidding, catching and cancelling are three different things** that Claude's wording blurred: forbidding removes shapes from the pool (the budget and the ceiling, which is why ED cannot crumple, C22); catching flags a bad shape after the fact (A7 C3b, which is what Allen was recalling); cancelling lets shapes in and has them destroy each other, **which ED does not do**. Only the third produces smoothness. | CONDITIONAL (meaning, Allen's clarification) | `Road_3_One_History.md`. | C22, A7-ledger C3b | — |
| C41 | Claude's reading of the gap, offered and labelled as such, not proposed as a decision (note 9). **Being an ensemble does not rescue the growth rule, because ED's ensemble is classical: every weight is positive.** A classical ensemble is dominated by whatever is most numerous, and stringy shapes are overwhelmingly more numerous than smooth ones, so no local preference outvotes the count - which is what five runs in attempt 7 and thirteen in attempt 8 measured. **Cancellation is what beats 'more numerous', and adding positive numbers can never cancel; weights that can point in opposite directions are needed.** ED already supplies the Born rule as one of its three inputs (A2 C11), which is the machinery of amplitudes, and **the growth model uses none of it** - the Born rule is assumed at the foundations while the universe is grown with dice. Allen reached for this unprompted, spitballing that the wobble or psi\* played a role in cancellation. A precise and unexotic version exists: **a quantum walk in place of a classical random walk**, same structure, amplitudes instead of probabilities, interference instead of averaging, and ED already uses random walks throughout its readings. Recorded with Allen's Deutsch analogy, which holds: in Deutsch's problem a global property of a function is learned in one query by asking in superposition, and checking inputs one at a time cannot do it - which is the exact shape of road E's failure, smoothness being a global property of a slice while the growth model checks one move at a time. **Whether ED's growth should carry amplitudes is not decided, is not proposed, and is Allen's alone.** | CONDITIONAL (Claude's reading) | `Road_3_One_History.md`. | C39, C40, A2-ledger C11 | — |
| C42 | Road F opened on paper (D13, note 10 `F1_Whole_Slice_Sync.md`): sync as a whole slice agreeing, which D10 decided and which **has never been tested in growth**, since every three-dimensional run in attempts 7 and 8 used the per-move veto. The reading is Synced Now's own statement put back where it started (A7 C2b): across a region of size L the clocks' spread is W(L) and the tilt of now is W(L)/L, which grows at d = 1, holds at d = 2 and **shrinks at d = 3**, so **a slice supports a common now exactly when its tilt shrinks with scale** - a property of a finished slice, measurable with no moves at all, from the steady tick field the neck-strain reading already solves and the ball distances already computed. Recorded with the literature's threshold: **Hong, Park and Choi give the lower critical dimension for frequency synchronisation as 2** (and 4 for phase), so below spectral dimension two coupled clocks with persistent rate differences cannot hold a common rhythm at large scales however strong the coupling. **E4 measured exactly that quantity:** with the veto on, spectral dimension 1.635-1.685 at n = 52; with it off, 2.318-2.319; flat 3.007. **So the per-move veto drives the slice below the dimension at which a common now is possible, destroying the condition it was imposed to enforce.** Stated as a claim to be tested, not as a measurement: the tilt has never been measured on a grown slice. | CONDITIONAL (proposed) | `F1_Whole_Slice_Sync.md` sections 1-3; A7-ledger C2b; Hong-Park-Choi. | C32, C34, A7-ledger C2b | — |
| C43 | Road F's design, least structure first (note 10 section 4, proposed as F-Q1-F-Q4): **sync as a filter, not a driver**. Grow with commitment and curvature only, with no sync anywhere in the move loop, then ask of the finished slice whether it supports a common now - which is how Synced Now used sync, as a diagnostic applied to a given slice. Nothing is added to growth; a property is measured. The reading is calibrated first on flat slices, which should show shrinking tilt, and randomised ones, which should not, and **if it cannot tell those two apart the road stops there**, the same discipline that caught two defects in E4's gate before any result. It is then measured on the E4 control slices (commitment and curvature, no veto) at n = 24, 32 and 52 and contrasted with the E4 veto slices at the same sizes; the slices re-grow deterministically, so the cost is about **2.5 hours**, almost all of it re-growing n = 52, against road E's thirty-five. | CONDITIONAL (proposed) | `F1_Whole_Slice_Sync.md` section 4. | C42, C34 | — |
| C44 | Road F's three outcomes, fixed before any run (note 10 section 5). **If grown slices support a common now** (tilt shrinks): sync does not belong in the growth loop at all, ED's growth already produces slices that can hold a common now, the veto was both unnecessary and harmful, and the wall is entirely about the local costs, making road 2 the whole question. **If they do not** (tilt flat or growing): sync as a condition was doomed from the start, since the slices cannot satisfy it and forcing it move by move mangled the geometry instead, and the question becomes what growth rule could make a slice that supports a common now - a sharper question than 'why isn't it flat'. **If the veto slices fail and the controls pass:** the strongest result road F could give, with the veto shown self-defeating by measurement rather than argument and D10's correction vindicated in the model as well as in the meanings. **In every case road F says something**, which is why it is worth its cost. | CONDITIONAL (exit rule) | `F1_Whole_Slice_Sync.md` section 5. | C42, C43 | — |
| C45 | What road F does not do, recorded with its proposal (note 10 section 6). **It does not make space:** the E4 controls read spectral dimension 2.32, not 3, so removing the veto helped and did not fix it, and commitment and curvature remain, which is road 2. **It does not test sync as a driver:** if a whole-slice condition should shape growth rather than judge it, that is a further road needing its own design - a condition on a tick rather than a move, costing a whole tick per rejection. **It does not touch the classical-quantum gap of C41:** whatever road F finds, ED's growth still uses positive weights and cannot cancel. | CONDITIONAL | `F1_Whole_Slice_Sync.md` section 6. | C41, C42 | — |
| C46 | Road F's calibration changed before any of it was run (D14, note 10 addendum). C43 proposed calibrating the tilt reading on flat against randomised; **that contrast tests nothing, because both pass** - a randomised slice is compact and effectively high-dimensional, so a common now survives on it as easily as on flat. The right contrast is Synced Now's own: objects where a common now survives against objects where it does not. The reading must reproduce the predicted tilt exponent on three trivial graphs needing no growth - **a ring (d = 1, tilt exponent +0.5, growing), a square torus (d = 2, exponent 0, holding) and a cube torus (d = 3, exponent -0.5, shrinking)** - with ED's triangulated flat 3-torus and a randomised slice measured alongside for reference. If it cannot reproduce those three, it is not measuring what Synced Now measured and the road stops. The measurement itself is A7 C2b's, reused exactly rather than reinvented: the structure function G(r) = mean of (phi_a - phi_b)^2 over pairs at hop distance exactly r, on a doubling ladder r = 1, 2, 4, 8..., from about 20 sources with at least 100 pairs a rung, with the tilt as sqrt(G(r))/r - applied to a slice's steady tick field instead of a simulated one. Recorded before any calibration ran, so the change cannot be a response to what the first contrast showed. | CONDITIONAL (criterion) | `F1_Whole_Slice_Sync.md` addendum; A7 `model/c2b.py` tilt_pairs and simulate. | C43, A7-ledger C2b | — |
| C47 | **The tilt calibration failed, and what it caught was a physics error of Claude's, not a coding one** (D14). Note 10 predicted tilt exponents of +0.5, 0 and -0.5 for dimensions 1, 2 and 3, taken from Synced Now's W proportional to L^((4-d)/2). **Those are SYSTEM-SIZE exponents - how the wobble grows as the whole system gets bigger - while the reading measures a SEPARATION exponent, how the wobble grows with distance inside one fixed system. They are not the same quantity, and Claude conflated them.** Worked out properly, with the tick field's spectrum going as k^-4 against a white source: in **d = 3** the separation integral is dominated by distances of order r, giving G(r) ~ r, W ~ sqrt(r) and a tilt exponent of **-0.5**, which does coincide with the system-size value; in **d = 1** it is dominated by the infrared cutoff, giving G(r) ~ r^2 N, so W ~ r and the **tilt is flat in r**, exponent 0, not +0.5; in **d = 2** it carries a logarithm, G ~ r^2 ln(n/r), so the tilt falls slowly as sqrt(ln(n/r)), about **-0.23** over the measured range, and is not a power law at all. Measured over rungs below a quarter of the largest, to keep finite-size saturation out of the fit: ring **-0.015**, square torus **-0.311**, cube torus **-0.662**, each stable across five seeds. So the prediction held only where the two quantities coincide. **A second defect the same run exposed, in the reading rather than the physics:** A7 C2b used 20 sources for random slices and a separate branch for grids that took every vertex, and porting only the random-slice branch left too few pairs in low dimensions - a ring gives two vertices at each distance from a source, so 20 sources give 40 pairs and the ladder was empty at r = 1. Raised to 200 sources. **Both were caught before any grown slice was touched**, which is what the calibration was for. | CHECKED | `model/f1_calibrate.txt`, `model/f1_tilt.py`; 2026-09-20. | C42, C46 | — |
| C48 | **The tilt reading calibrated and road F measured** (D15, note 11, `model/f1_calibrate.txt`, `model/f1_measure.txt`). The calibration passed: the ring, square torus and cube torus came out **ordered and separated by 0.27 and 0.35 on every one of five seeds**, at tilt exponents **-0.013, -0.280 and -0.627**, with ED's flat triangulated slice at -0.671 alongside the cube torus, and the steady-state solve matching `c3b.sync_steady_state` to machine precision. Allen's decision (D15) made those objects the scale rather than a theoretical exponent, after C47. **The threshold was fixed before any grown slice was touched: a slice supports a common now if its exponent is at or below -0.280**, two being the literature's lower critical dimension for frequency synchronisation. Measured on E4's slices, re-grown exactly: **the veto slices fail at every size and fail identically - -0.130 at n = 24, -0.134 at n = 32 and -0.132 at n = 52**, across a tenfold range in volume, sitting between one and two dimensions and nearer the ring than the square torus. **A slice grown under the per-move veto cannot hold the thing the veto was imposed to enforce.** The controls are **not measurable** under the rule fixed in advance: being more compact than the veto slices, diameters 13 and 17 against 19 to 25, their ladders stop at r = 8 with four rungs, and dropping the two saturated top rungs leaves two, below the three a fit needs - which is the possibility F1-4 named before the run. | CHECKED | `model/f1_calibrate.txt`, `model/f1_measure.txt`, `model/f1_runs/`; 2026-09-20. | C42, C46, C47 | — |
| C49 | What the controls would have said, **post-hoc and labelled, not a result** (note 11). These fits were not fixed in advance; they are recorded because 'not measurable' could be hiding a clear pass and the honest thing is to look. Fitting all four rungs: n = 32 control **-0.185**, n = 52 control **-0.325**. Dropping one rung: **-0.122** and **-0.221**. **Three of the four sit above the -0.280 threshold**, and the one below it clears by 0.045 while using the rung most likely to be saturated. **There is no hidden clear pass.** The reading this supports, stated as a reading: **ED's grown slices do not support a common now, with the sync condition or without it; the veto makes it worse and removing it does not restore it.** | CHECKED (post-hoc, labelled) | `model/f1_runs/*ctrl.json`; 2026-09-20. | C48 | — |
| C50 | What road F does to C42's claim, and where it lands (note 11). C42 claimed the per-move veto drives the slice below the dimension at which a common now is possible, destroying the condition it enforces. **Half is confirmed and half is not.** Confirmed: the veto slices do not support a common now, at three sizes, unambiguously. **Not confirmed: that removing the veto restores it** - the controls are better, -0.12 to -0.33 against the veto's -0.13 and spectral dimension 2.32 against 1.69, but they do not clearly cross the threshold. **So road F lands on its second outcome, not its third** (C44): sync as a condition was doomed from the start, since the slices cannot satisfy it, and the question becomes what growth rule could make a slice that supports a common now. **Allen's decision on what sync means (D10) stands on its own reasoning and is not weakened by this; what this shows is that fixing sync's form is not enough**, because ED's growth does not produce slices that can carry a common now in the first place. Recorded alongside: every quantity road F measured is **flat in size** - -0.130, -0.134, -0.132 over a tenfold volume range - which is the same size-independence road E found in the walk's reach (V^0.101), in the fitting window (2-4 hops at every size) and in the spectral dimension curve. **ED's grown slices are the same object at every scale that can be reached.** A finer ladder, every integer r rather than doubling, would probably make the controls measurable and was deliberately not done, because the post-hoc fits already show which way they point and changing the measurement after seeing that would be the forking path this project keeps guarding against; whether to re-measure that way is Allen's call. | CONDITIONAL | note 11. | C42, C44, C48, C49, C37 | — |
| C51 | Stock-take of attempt 8 (D16, note 12 `Taking_Stock.md`, written in plain language), reasoning only. Attempt 8 set out to find whether attempt 7's wall was the growth rule or the patch size, and **the answer is the rule**. Findings: ED cannot collapse, the link budget and the no-infinities ceiling each forbidding the crumpled state (C22); ED's spectral dimension runs with scale and every earlier number was the narrowest point of that curve (C36); bigger patches cannot settle the shape, reach growing as V^0.101 so that doubling it would take about 943 times the volume (C37); sync had drifted into a per-move veto through three undecided steps and Allen decided it means a whole slice agreeing (D10, C32); ED's past is a set of possibilities, the present not knowing its own past, so ED has been an ensemble theory all along (D11, D12); ED's ensemble uses only positive weights and so cannot cancel, while the Born rule it supplies is unused by the growth model (C41, Claude's reading); and grown slices cannot hold a common now, failing identically at every size with the veto and showing no pass without it (C48-C50). Untouched: Budgeted Causality and Synced Now. Census: inputs supplied 3, nothing derived, no new number; one knob redefined, the sync threshold now a median over eight reading seeds after a 23 per cent single-seed wobble (C16, C20). Walls: 1, 2, 4 and 5 unchanged; **wall 3 sharper and narrower - ED cannot crumple, the rule makes compact stringy patterns at every reachable size, bigger sizes cannot change that, and the patterns cannot carry a common now, so the wall is now about the kind of rule (local, step by step, positive weights) rather than its settings.** Cost: about 40 h of compute; 50 claims, 15 decisions, 17 recorded steps, 12 notes; 15 Claude code or specification errors, all caught by a test or guard before reaching a result, and 2 reasoning errors (C35, C47). Open: road 2, the local costs; road G, amplitudes in growth; and the finer ladder for road F's controls. Options put to Allen: (a) conclude attempt 8 and open attempt 9 on the kind of rule, starting on paper with road 2 and road G [proposed]; (b) stay in attempt 8 and take road 2 on paper; (c) run the finer ladder first; (d) pause. | CONDITIONAL (stock-take) | `Taking_Stock.md`. | C22, C32, C36, C37, C41, C48-C50 | — |
| C52 | Attempt 8 concluded (D17, RD19) and attempt 9 opened. What attempt 9 inherits, summarised in A9 note 1 `Carry_Forward.md`: attempt 8's answer, that attempt 7's wall is the growth rule and not the patch size; the meanings of attempts 4-7 unchanged, plus three decided in attempt 8 - sync means a whole slice agreeing, not a per-move veto (D10); once something has happened it is fixed but many things could have happened (D11); and the present does not know its own past, so ED is an ensemble theory and has been all along (D12). Inputs supplied 3, nothing derived. Walls: 1 rest frame a risk; 2 dark sector untouched; **3 growth now about the kind of rule** - ED cannot crumple (C22), its local, step-by-step, positive-weight growth makes compact stringy slices at every reachable size (C37), and they cannot hold a common now (C48-C50); 4 numbers inherited with the sync threshold redefined; 5 handle-free rule chosen. Standing results: Budgeted Causality, Synced Now, ED cannot crumple, and ED's spectral dimension runs with scale. Tools: the validated port, the readings bridge, the scale-resolved spectral dimension and the tilt reading. Lessons: calibrate on objects with a known answer and let the calibration stop the road; fix pass criteria before the data that tests them; look at where a reading is measured and not only its number; memory binds before time at 140,000 events. Opening road for attempt 9: road H, what kind of rule - H1 the local costs, H2 weights that can cancel - both on paper first. Carried and not opened: the finer ladder for road F's controls and the spacetime reading at 140,000 events. | CONDITIONAL | `Taking_Stock.md`; `../ED_Attempt_09/Carry_Forward.md`. | C51 | — |
