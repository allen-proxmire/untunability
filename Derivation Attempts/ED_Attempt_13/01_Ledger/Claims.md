# Claims (ED_Attempt_13)

*Statuses and rules as in `../../ED_Attempt_01/01_Ledger/README.md`. References to earlier attempts are written A1- through A12-ledger.*

| ID | claim | status | evidence | inputs | outside check |
|---|---|---|---|---|---|
| C1 | Reasoning: carried into attempt 13 from A12-ledger C13, C14 and RD14 - the attempts 7-12 thread; the standing results: ED's budgets fix the three totals CDT tunes with nothing tuned and no new free numbers (A11 C5, exact algebra, the strongest claim and the only genuine reduction); A6's rate-matching floor measured on four known shapes (A11 C21); ED's patterns hold their rates on about 4 relations per event, below the 6.7 budget (A11 C22, A12 C12); patterns fragment below about 5 relations per event (A12 C7); and the finding attempt 12 established, that **ED's rules CONVERGE rather than SELECT** - a line, a lattice and a random web all reach one common end state, tightly, with nothing tuned (A12 C11, C12). Corrected and not carried: A11 C23/C24's "a shape is carried, not chosen", which was measured with sync switched off and does not survive switching it on (A12 C5). Not established: three dimensions from ED, space derived rather than supplied, where a first shape comes from. Meanings settled in A12 and carried: no shared now (D5), a relation can dissolve (D4), a relation persists while it is part of a commitment (D6). Inputs supplied: 3, plus the starting shape in every model so far; nothing derived. Tools: `l_local2.py` (A6's condition as an exact max-flow feasibility with the commitment rule), `l_local.py`, `p_persist.py`, `b_balance.py`, `n1_sync.py`, `n2_grow.py`, `st3f.py`. Opening road E: describe the end state ED's rules actually make. | CONDITIONAL | `Carry_Forward.md`; A12-ledger C13, C14. | A11 C5, C21, C22; A12 C5, C7, C11, C12, C13 | — |
| C2 | Specification (paper), road E (note 2 `E_End_State.md`): describe the object ED's rules actually make, rather than testing whether it is the thing we wanted. Five questions - E-1 is it one object or only one set of readings (neighbourhood statistics, relation-count spread, clustering, spectrum, not just the two numbers that agreed); E-2 how distances really grow, logarithm or power, needing sizes to about 32,000; E-3 the main one, is it sitting exactly on the edge of holding together - the cheapest pattern that holds is one that only just holds, so its patches should sit near equality in A6's inequality, which would make it a CRITICAL pattern sitting on the floor A6 derived; E-4 does it have structure at some scale even without a dimension (A8's spectral reading); E-5 is it the same object at every size. Expectations fixed: E1 same object from every start (60%); E2 distances grow logarithmically (70%); E3 patches sit near equality (45%); E4 a definite value at some scale (30%); E5 reported. Discipline: every statement measured against references at the same size and relation count (random regular web, 3D grid, small-world rewiring, tree, preferential attachment), and a literature step in the middle to check whether the object is a known class. Honest points: this is description, not a test of ED; no reduction is available on this road; the starts are supplied but matter less because the object is the same whatever you start from. **SUPERSEDED IN PART by C3 before anything was run.** | CONDITIONAL (superseded in part) | `E_End_State.md`. | C1; A12 C11, C12 | - |
| C3 | **Reading of Allen's first paper, Event Density and the Architecture of the Universe (January 2026), whole (note 3 `E_The_First_Paper.md`); reasoning only.** Three findings. (i) **A correction of Claude's:** earlier today Claude checked ED's inflation language against the old repo and told Allen his recall had the terms swapped; Allen's recall was right and it is this paper - 'It is the moment when ED diffusion outpaced ED production' (S13). The phrasing Claude found ('complexity outpacing event-production capacity') is a different passage about the quantum-classical boundary. Claude corrected Allen against the wrong document. (ii) **The gap, and it is in every model built in this project:** S4 says 'Space is the pattern of stable relations formed by persistent ED gradients' and 'Distances correspond to differences in ED; directions correspond to pathways along which ED remains coherent; regions correspond to clusters of stable ED relations.' **Every model from road N onwards draws each event's rate independently from a bell curve - noise, not a gradient - and the generation models REDRAW the rates every generation, so nothing about them can persist.** Same class of miss as the audit (clocks absent) and D5 (a shared now built in), all three Claude's and all three invisible from inside the numbers. Consequences: **A6's floor rests on the sqrt(n) surplus of INDEPENDENT random rates; with a coherent gradient a patch's surplus grows like n, so the floor as measured (A11 C21) may be an argument about noise rather than about ED**; and the paper treats a surplus not as something to be carried through an edge but as what DRIVES diffusion - 'The natural flow of becoming is from concentrated ED toward diffuse ED' (S26), with the flattening of gradients being the arrow of time - while nothing in our models diffuses at all. (iii) **The paper does not claim to derive three dimensions:** 'ED does not predict every structure in the universe. It explains why structure is possible' (S32); 'ED does not specify the exact distribution of ED at any moment, the particular gradients that form galaxies or stars' (S34); 'ED provides the conditions of possibility, not the full catalogue of outcomes'; 'a theory of why anything can have a structure at all.' Twelve attempts have been chasing something ED's founding statement does not promise, which does not make the failures less real but means the target was set by us. It also recasts A11 C5 (the budgets fixing what CDT tunes) as exactly a conditions-of-possibility claim - a constraint on what structures are available rather than a prediction of one - which is the kind of claim the paper says ED makes. Two correspondences recorded carefully and NOT claimed: S6's ED thresholds ('every structure exists only when the local rate of becoming exceeds the threshold needed to sustain it') against A12 C7's fragmentation threshold near 5 relations per event; and S29's ontological flatness (the Penrose CCC parallel) against our featureless end state, which is reached by adding relations rather than by gradients flattening. **Effect on road E: note 2 is premature as written** - the object it would describe is produced by a model with no gradients, no persistence and no diffusion, so describing it carefully would be describing an artefact of noise. Questions for Allen: **E-Q1 do rates pass on** (default: a child's rate is its parent's plus a small variation, the least-structure way for a gradient to exist at all); **E-Q2 do rates diffuse** (default: yes, each tick an event's rate moves a little toward its neighbours', with the rate of movement a number that would be Claude's unless ED supplies one). Warning recorded before any build: with diffusing rates a pattern flattens toward uniform rates, and a uniform pattern trivially satisfies every sync condition, so **a model that flattens its own gradients will 'succeed' at sync while containing nothing** - which is what the paper's S16 and S29 say a universe ends up as. Options: (a) answer E-Q1 and E-Q2, then rewrite road E's model with gradients that persist and diffuse [proposed, with (c) folded in]; (b) describe the end state as it stands first, then add gradients; (c) redo A6's floor with gradient rates rather than noise, since that argument underpins everything since. | CONDITIONAL (reading) | `E_The_First_Paper.md`; Allen's paper on the Desktop. | C2; A6 C28, C29; A11 C5, C21; A12 C7, C11, C12 | Proxmire, Event Density and the Architecture of the Universe (Jan 2026), SS4, 6, 13, 16, 26, 29, 32, 34 |
| C4 | Specification (paper), road E with gradients (D2, note 4 `E_Gradient_Model.md`). The local-sync model (C9, C11, C12) unchanged in every respect - pass on once and be spent, neighbourhoods inherited, every patch able to shed its surplus through its own edge checked exactly with no shared now, additions across the min cut, a relation persisting once it has carried anything - **with exactly two changes, both from the January 2026 paper: a child's rate is its parent's plus a small variation (S4, persistent gradients), and each tick an event's rate moves a little toward its neighbours' (S26, becoming flows from concentrated to diffuse and gradients flatten).** **Pre-registered VOID condition, not a result: if the spread of rates falls below a tenth of its starting value the run is void and is reported as 'the pattern flattened', never as 'the pattern held together'** - because a flat pattern satisfies every sync condition trivially, and the paper's S16 and S29 say a universe does end that way, so flattening is a real outcome but not a success. Expectations fixed before code: **G0 the void check, gradients survive long enough to matter (about 55%; if it fails nothing else counts)**; G1 patterns hold on FEWER relations than with noise, under 4.15 per event, because neighbours now tick alike (about 70%); **G2 the main one, the end state is no longer the same from every start, because gradients give a pattern something to remember (about 40%)**; **G3 the one that would matter most, readings lower than the noise model's 3.9 with the small-world flag clearing on at least one start (about 20%, written low on purpose)**; G4 reported - whether the paper's own distance (accumulated rate difference along a path) agrees with hop-count distance, the size of the gradients at the end, and what the rate spread does over the run. What each outcome means: G0 failing means ED's own diffusion erases its own gradients and space in the paper's sense cannot persist under these rules - a real negative about the mechanism, not the instruments; G2 holding would revise A12 C11 and C12, making attempt 12's convergence an artefact of rates being noise; G2 failing leaves that convergence standing as ED's own; G3 holding would be the first positive movement in the project on this question and would need repeating before being said aloud. Claude's, labelled: the size of the variation when a rate passes on and the speed of diffusion (both set to the smallest values that let a gradient exist, reported plainly, and varied by about a factor of three afterwards as the pull was in C11), plus sizes, seeds, the first generation's bell curve, and the tenth-of-start threshold. Honest points: starts are still supplied; **two new numbers of Claude's go in, so under the census guard the input list gets LONGER and the write-up will say so**; the most likely single outcome is G0 failing; and this is the fourth correction of the same kind (clocks, shared now, dissolution and commitment, now gradients) - if it too improves the character without changing the answer, the honest reading is that ED's rules as written do not make space, and that becomes the result rather than a staging post. | CONDITIONAL | `E_Gradient_Model.md`. | C3; D2; A6 C28, C29; A12 C11, C12 | Proxmire, Event Density and the Architecture of the Universe (Jan 2026), SS4, 16, 26, 29 |
| C5 | **Road E with gradients: the end state is no longer the same from every start, but the readings still show no dimension (G0 passed, G1 passed, G2 passed by its rule but marginally, G3 FAILED, G4 reported)** (`model/g_grad.py`, expectations G0-G4 in the file header before the run; output `model/g_grad.txt`, data `model/e_runs/grad.json`, note 5 `E_Gradient_Results.md`). Three starts, three seeds, about 8,500-9,300 events. **G0, the gate: gradients survive** - the rate spread settles near half its starting value in every run (0.46-0.53), nowhere near the void line at a tenth, so passing-on and diffusion reach a balance rather than flattening; the run counts. **G1 passed:** relations per event 3.39 (ring), 3.19 (cubic torus), 3.16 (web), against the noise model's 4.15 - neighbours tick alike, so less has to be carried. **G2 passed by its pre-registered rule but marginally:** readings ring 3.83/3.98/4.14 (mean 3.99), cubic torus 4.57/4.83/4.41 (4.60), web 4.41/4.71/4.58 (4.57) - a gap of 0.6 between starts where the rule asked 0.3, **but the seed spread within a start is 0.3-0.4, so the effect is only about twice the noise and is NOT called established; A12 C11 and C12 are recorded as UNREVISED pending more seeds.** What can be said: with noise rates the three starts agreed to within 0.1; with gradients they no longer do, and the ring is consistently lowest across every seed. **G3 FAILED:** readings are 4.0-4.8 and went UP, not down, against the noise model's 3.9. G4: the paper's own distance (shortest path weighted by rate difference) agrees with hop distance only moderately (correlation 0.35-0.65). **Clean side-finding: the 3D-grid start is no longer a small world at any seed** (every earlier model made it one), with mean distance 8.4-9.2 at 9,200 events against about 10.6 for a real 3D grid - yet its ball-growth reading rose to 4.6, so the two readings now disagree; recorded as an oddity, not a result. **Robustness:** both of Claude's new numbers varied by a factor of three (variation on passing on 0.03/0.30, diffusion speed 0.03/0.30) - relations per event moves (3.05-3.93), the rate spread moves (0.26-0.82), **the reading does not (4.0-4.5)**, so G3's failure is not an artefact of those settings. **Standing reading:** gradients are not self-erasing, they make the model cheaper, and they give a pattern some memory - but they do not produce space. **This is the fourth correction of the same kind (clocks, no shared now, dissolution and commitment, gradients), each taken from ED's own words, and each has improved the model's character without changing the answer. Note 4 said before the run that if gradients did not change the answer either, the honest reading is that ED's rules as written do not make space, and that this becomes the result rather than a staging post. That is where it stands.** Options: (a) settle G2 with ten seeds per start, cheap, and either revise A12's convergence finding or drop the claim [proposed, then (c)]; (b) chase the oddity that the 3D-grid start is not a small world yet reads 4.6, since one of the two readings may be the wrong instrument for this object; (c) take stock of attempt 13 and of the project. | CHECKED (result) / CONDITIONAL (reading) | `model/g_grad.txt`, `model/e_runs/grad.json`, `E_Gradient_Results.md`. | C3, C4; D2; A12 C11, C12 | Proxmire, Event Density and the Architecture of the Universe (Jan 2026), SS4, 26 |
| C6 | **Reading of Allen's paper 11, Event Density and the Architecture of Physical Law (February 2026), SS1-7 (note 6 `E_Paper_11_Dimension.md`); reasoning only.** **The decisive passage: S1.2 lists what does NOT exist at the micro-scale - no manifold, no coordinates, no metric, NO DIMENSIONALITY, no global symmetries, no conserved quantities - and says 'Micro-events obey constraints, not laws'; S3.1 lists stable geometry, stable adjacency and STABLE DIMENSIONALITY as 'classical-regime features, not micro-event primitives'; S3.5 and S6.7 say that when participation is thin, 'dimensionality fluctuates' and 'becomes unstable'.** So in ED, dimension is not a property of the substrate: it appears only when participation is THICK. **Consequence for thirteen attempts: every model run in this project has been in the thinnest regime the rules allow** - the minimality rule (A11 D13, Claude's proposed default which Allen accepted) drives every pattern to about 3.2 relations per event - **and what we measure there is exactly what paper 11 says a thin regime looks like: no stable dimension, readings wandering between 3.8 and 4.8, a small-world flag flickering between seeds. The negatives are a measurement of the regime ED says has no dimension, not evidence against ED.** Stated carefully: this does NOT mean ED is right; it means the test was aimed at the wrong regime, and ED said so in writing in February. **The tension inside ED, recorded:** minimality (fewest relations that still hold) drives toward the thinnest working pattern, while paper 11 requires thickness for laws, symmetry, conservation and dimension to appear - one is a default Claude offered and Allen accepted, the other is in Allen's own paper, and they cannot both govern the regime where space is supposed to appear. **The target changes, downward, which is the honest direction: the paper never says three** (S5.7 lists 'dimensionality varies' among regime differences), so the question becomes 'does a stable dimension exist at all once participation is thick, and what is it?' - a smaller, testable, ED-own claim. **Also recorded for Allen's earlier question about a theory of possibilities versus a GUT: his corpus contains both ambitions, one month apart** - January's S32, 'ED does not predict every structure in the universe. It explains why structure is possible', against February's S1.6, 'The goal is not to reinterpret physical law. The goal is to derive it.' The tension is in the papers, not in his memory, and which one ED is held to decides what counts as success. **Also recorded, not resolved: paper 11 S4.1 says 'At the micro-event level: nothing is conserved' and calls conservation 'commitment accounting in a stable participation regime' that fails near horizons and in the early universe - while this whole program rests on Budgeted Causality as a primitive, and the project's strongest result (A11 C5) is built on it. That does not break the result; it re-labels what kind of claim it is.** **Risk on the record: this is the FIFTH time a model has turned out to be missing something ED already said (clocks, no shared now, dissolution and commitment, gradients, now thickness). Each correction was real, came from ED's own words, and improved the model; each time the answer stayed the same. At some point the sequence itself is the finding - if a thick-regime test also returns no stable dimension, the honest conclusion is not 'one more thing was missing' but that ED's rules as written across the papers do not produce a dimension, and the project should say so.** Options: (a) road T, thickness - run the gradient model with minimality relaxed at a ladder of thicknesses (6, 12, 25, 50 relations per event) and ask whether the reading becomes STABLE ACROSS SIZES and what value it takes, with 'stable' pre-registered [proposed]; (b) settle which ambition ED is held to before running anything, since it decides what counts as success [put to Allen as a question, not a road]; (c) finish the ten-seed run, take stock of attempt 13 and stop. | CONDITIONAL (reading) | `E_Paper_11_Dimension.md`; Allen's paper 11 on the Desktop. | C3, C5; A11 D13, C5; A12 C7 | Proxmire, Event Density and the Architecture of Physical Law (Feb 2026), SS1.2, 1.6, 2.3, 3.1, 3.5, 4.1, 4.5, 5.7, 6.7 |
| C7 | **G2 settled at ten seeds: the line keeps something, the lattice and the web do not keep anything from each other** (`model/g_seeds.py`, rule G2a fixed in the file before it ran; output `model/g_seeds.txt`, data `model/e_runs/seeds.json`, note 7 `E_Seeds_Result.md`). Ten seeds per start, model and settings unchanged. Readings with 95% intervals: **ring 4.096 [3.936, 4.256]; cubic torus 4.581 [4.478, 4.684]; web 4.631 [4.533, 4.729]** - every run gave a reading, none dropped (silently excluding runs that fail to produce a number would have biased the comparison, so the count is reported: 0 of 30). **G2a passes: the ring's interval sits clearly below the cubic torus's, and below the web's** (gaps 0.485 and 0.535, intervals separated). **But the cubic torus and the web are NOT distinguishable from each other** (gap 0.050, intervals overlapping). Second reading pointing the same way, not pre-registered: the small-world flag is set in 9 of 10 ring runs, 4 of 10 cubic torus runs and 2 of 10 web runs. Relations per event 3.41, 3.22, 3.18. **Effect on A12 C11 and C12: their convergence finding is REVISED and is now half right - ED's rules erase the difference between a lattice and a random web completely, but they do NOT erase the difference between a line and either of them; a one-dimensional start keeps something that survives four-fold growth.** With noise rates all three agreed to within 0.1, so the line's difference appears only once gradients are in (C5). **What it does not show: none of these are dimensions** - 4.10, 4.58 and 4.63 with small-world flags set in most runs are an instrument reporting that there is no stable dimension to read; the starts differ from each other, but none is three-dimensional and the cubic-torus start is no more three-dimensional than the random web it converges with. **By C6 (paper 11) that is what this regime should look like:** every run sits at about 3.2 relations per event, the thinnest participation the rules allow, which is where ED says dimensionality is not defined. C5's caution on G2 is discharged. | CHECKED (result) | `model/g_seeds.txt`, `model/e_runs/seeds.json`, `E_Seeds_Result.md`. | C5, C6; A12 C11, C12 | - |
| C8 | **Road T, thickness: the readings stabilise once participation is thick, and they track the start** (`model/t_thick.py`, rule T1 and expectations T0-T3 fixed in the file before the run; output `model/t_thick.txt`, data `model/e_runs/thick.json`). Minimality relaxed and thickness imposed at 6, 12, 25, 50 relations per event, two starts, two seeds, readings at about 4,700 and about 18,000 events (a four-fold span), with the sync condition checked rather than repaired. **Instrument fix made at the smoke test and recorded before the run: the reference is matched in THICKNESS as well as dimension (a cubic torus widened to the same relations per event), because a real 3D lattice carrying 25 relations per event trips the small-world flag too, so judging thick patterns against a 6-regular reference would have penalised thickness itself.** Results, ball growth at the end: cubic torus start 3.60 (thickness 6), 3.02 (12), 2.54 (25), 2.33 (50), with drift falling from 0.68 to -0.02; ring start 1.00 at every thickness with drift under 0.03. T0 fails (the condition does not hold unaided at the thinner settings); T1 passes (stable readings exist); T2 as written fails, but only because the rule demanded every stable value lie in 2.5-3.5 including the ring's 1.0 - the cubic-torus start does read 2.54 and 2.58 at thickness 25. **Finding: thickness makes the reading stop drifting, and what it settles on tracks where the pattern started** - which is paper 11's account of dimensionality as a thick-regime phenomenon, and preservation rather than selection. | CHECKED (result) | `model/t_thick.txt`. | C6; A12 C7 | Proxmire, paper 11, SS1.2, 3.1 |
| C9 | **ED's own readings built and gated (paper 10): the dimension reading passes, the distance reading fails** (`model/ed_readings.py`, gate I0 fixed in the file before it ran). Paper 10 defines both quantities in ED's own terms - S3.5 'the number of effective dimensions is the number of INDEPENDENT PARTICIPATION DIRECTIONS available at scale', and S3.2 distance as PARTICIPATION RESISTANCE - and neither is what this project has been measuring; every reading in thirteen attempts used borrowed ball growth, hop distance and a small-world flag. Implemented: directions as the size of the group of lowest modes, halved (two modes per independent direction); resistance as effective resistance against separation. **Gate on known shapes: directions gives ring 1.0 (group of 2), flat torus 2.0 (group of 4), cubic torus 3.0 (group of 6), random web nothing definite - a clean pass. Resistance FAILS: it reads 1.19, 1.87, 1.96, 1.99 for those four, saturating near 2 and unable to tell a cubic torus from a random web, so by the pre-registered gate it is NOT USED** (a limit of the log-log implementation above two dimensions, reported and not claimed as a limit of ED's definition). **Applied to patterns already run, the borrowed instrument and ED's own disagree by more than a whole dimension in both directions: thick cubic-torus-grown patterns read 2.32 by ball growth and exactly 3.0 by ED's; thin minimality-regime patterns read 4.57 by ball growth and 1.0 by ED's.** Allen's caveat recorded: these definitions are early work and were tried, not assumed - which is what the gate is for. | CHECKED (instrument) | `model/ed_readings.py`, `model/ed_readings.txt`. | C6, C8 | Proxmire, paper 10, SS3.2, 3.5 |
| C10 | **THE ANSWER: ED's rules carry a dimension; they do not make one (W1 fails, W3 passes)** (`model/w_web.py`, robustness rule and expectations W1-W4 fixed in the file before the run; output `model/w_web.txt`, data `model/e_runs/web.json`, note 8 `E_The_Answer.md`). The decisive test: grow a pattern from a RANDOM WEB - which ED's own instrument reads as having no definite number of directions - thicken it, and ask whether it acquires one. Robustness fixed first: a reading counts as definite only if the group of lowest modes is the same size at all three clustering tolerances (0.15, 0.25, 0.40), the group is even, and the gap ratio to the next mode is at least 1.5. **Result over nine runs (three thicknesses, three seeds): the web start NEVER acquires a definite number of directions - groups disagree across tolerances ([13, 25, 29] and the like) and the gap ratio is 1.006-1.42, meaning no gap at all. Controls: the ring reads exactly 1.0 in nine runs of nine with gap ratios 3.7-3.9; the cubic torus reads exactly 3.0 in five of nine, the other four refused by the tolerance-agreement rule although they read 6 modes at two of three tolerances.** So: a line stays a line, a cubic grid stays three-dimensional, and a pattern with no dimension never gains one, under every rule ED supplies - passing on, spent events, inherited neighbourhoods, gradients that persist and diffuse, patches shedding their rates through their own edges. **Why this answer outweighs the thirteen before it: those were measured with borrowed instruments in the thin regime, and both are now known to be wrong for the job; this is ED's own definition of dimension, gated on known shapes, applied in the regime ED's own papers specify. It is the first time the question has been asked in ED's own terms, and the answer is still no.** What is established: in the thick regime ED's rules PRESERVE a dimension exactly (three in, three out, at 12, 25 and 50 relations per event, stable across a four-fold growth) - a real positive result, new to this project; in the thin regime there is no dimension, exactly as paper 11 says, so thirteen attempts were measuring a regime ED had already described as dimensionless; and nothing creates a dimension. **Consequence for the project's question: in ED's own terms and regime, ED CONDITIONS space but does not PRODUCE it - which is precisely the January paper's 'conditions of possibility, not the full catalogue of outcomes', reached by measurement rather than by reading.** Honest limits: the instrument is hours old and untested outside this project; three seeds and one size range; the cubic-torus readings are noisier than the ring's (gap 1.6 against 3.8) so 'exactly 3' rests on five clean runs of nine; and the starts are still supplied. Options: (a) take stock of attempt 13 and the project; (b) more seeds and a second size range to make both halves solid [proposed, then (a)]; (c) ask what would have to be ADDED for a dimension to be selected rather than carried, and whether anything in ED supplies it. | CHECKED (result) | `model/w_web.txt`, `model/e_runs/web.json`, `E_The_Answer.md`. | C6, C8, C9; A12 C11, C12; A13 C5, C7 | Proxmire, papers 10 and 11 |
| C11 | **Eight seeds and two sizes: the negative holds, the positive is WITHDRAWN (S1 passes, S2 fails, S3 passes)** (`model/s_solid.py`, expectations S1-S4 fixed in the file before it ran; output `model/s_solid.txt`, data `model/e_runs/solid.json`, note 9 `E_Solid.md`). Eight seeds, two size ranges (about 8,000 and about 16,000 events), thickness 25, model and instrument unchanged. **Ring: definite in 8 of 8 at both sizes, always exactly 1.0, gap ratios 3.4-3.9 - the cleanest signal in the project. Cubic torus: definite in 1 of 8 at both sizes, gap ratios 1.47-1.68. Random web: 0 of 8 at both sizes, gap ratios 1.00-1.01.** S1 passes, S3 passes, **S2 fails badly**. **Why C10's positive was wrong: its 'five of nine' was three seeds at three thicknesses, so a seed that worked worked at every thickness - two seeds of three, counted nine ways. Eight independent seeds give one of eight.** What survives: **a line is carried perfectly** (16 of 16); **nothing creates a dimension** (16 of 16 from a random web, no structure at all); and **a three-dimensional start is NOT carried cleanly** - the modes still cluster into six at looser tolerances but the grouping is no longer sharp, failing a rule fixed in advance seven times in eight. **Revised statement: ED's rules carry ONE dimension perfectly, DEGRADE three, and CREATE none.** The asymmetry is new and unexplained and is recorded without an explanation: the structure ED preserves best is the one that CANNOT hold its clocks together (a line fails the sync condition), and the structure it degrades is the one that can - ED's growth is hardest on exactly the shape the project wants. C10's negative stands, now on 16 runs instead of 9; C10's positive is withdrawn. | CHECKED (result) | `model/s_solid.txt`, `model/e_runs/solid.json`, `E_Solid.md`. | C10 | - |
| C12 | **Attempt 13 stock-taken** (note 10 `Taking_Stock.md`), reasoning only. Attempt 13 opened to describe the end state, and Allen's own papers changed the question three times before anything was measured: the January paper's space as PERSISTENT ED GRADIENTS (absent from every model built here), paper 11's dimension as a THICK-regime phenomenon that does not exist at the micro-scale (a regime no model here had run in), and paper 10's DEFINITION of dimension as independent participation directions (not what any model here had measured). All three were fixed and the question asked again. **Established in attempt 13:** gradients survive and balance at about half the starting spread (C5); gradients make patterns cheaper and more spread out, 3.2 relations per event against 4.15 (C5); **with gradients a line ends measurably apart from a lattice and a web while those two converge on each other, on ten seeds with separated intervals (C7)**, revising A12 C11/C12; **ED's own dimension reading passes a gate on known shapes while ED's distance-as-resistance reading fails it (C9)**; the borrowed ball-growth instrument disagrees with ED's own by more than a dimension in both directions (C9); thickness stops the readings drifting (C8); **a line is carried perfectly, nothing creates a dimension, and three is NOT carried cleanly, on eight seeds at two sizes (C11)**. **Withdrawn: C10's positive that the thick regime preserves a dimension exactly.** Thirteen attempts in shape: 7-9 removed growth, costs and conserved quantities as sources of extended space; 10 removed counting; 11 produced the budget result and the audit; 12 removed persistence; 13 asked the question in ED's own terms and answered that **ED conditions space but does not produce it**. What ED has: the budgets fixing what CDT tunes (the only genuine reduction, and exactly the kind of claim the January paper says ED makes); the measured sync floor; nothing creating a dimension; a line carried and three degraded; a fragmentation threshold near 5 relations per event; no collapse in 2+1. Not established: three dimensions from ED, space produced rather than supplied, where a first shape comes from; inputs supplied 3 plus a starting shape, nothing derived. **The two ambitions in Allen's corpus are set side by side: January's 'conditions of possibility, not the full catalogue of outcomes' against paper 11's 'the goal is to derive it' - thirteen attempts have graded ED against the second while the evidence keeps landing on the first, and which claim ED is making is Allen's to answer.** Method recorded: five times a model was missing something ED had already written down, three of those caught by Allen from the meaning with no numbers; the instruments were wrong twice; two results were withdrawn on their own checks. The pattern worth keeping is to fix the rule before the run and build the run that can destroy the result. Options: (a) conclude attempt 13 and write the project up; (b) chase the asymmetry - why one dimension is carried perfectly and three degraded [proposed, then (a)]; (c) ask what would have to be added for a dimension to be selected; (d) conclude and pause. | CONDITIONAL (stock-take) | `Taking_Stock.md`. | C5, C7, C8, C9, C10, C11; A11 C5, C21; A12 C13 | Proxmire, papers 10 and 11, and the January paper |
| C13 | Specification (paper), the asymmetry (D3, note 11 `A_Asymmetry.md`): is C11's asymmetry ED's, or is it the shape of the signature? The reading needs a clear gap above the group of lowest modes, and how clear that gap is differs by shape before anything is done - about 4 for a clean ring, about 2 for a clean cubic torus - so three dimensions may simply have less margin and lose its reading sooner under any blurring. Three steps: A1 measure the clean gap for ring, flat torus and cubic torus; A2 blur with generic noise and find where each stops reading; A3 the decider, compare that with the blurring ED's growth introduces. Expectations: A1 the clean gap is larger for the ring (high); A2 noise tolerance falls as dimension rises (moderate-high); A3 the asymmetry is generic, not ED's (about 60%). **Written to kill a nice-sounding result of Claude's, which is why it came first.** | CONDITIONAL | `A_Asymmetry.md`. | C11; D3 | - |
| C14 | **The asymmetry is not what Claude guessed: three dimensions is the ROBUST one (A1 holds, A2 fails in the opposite direction, A3 not shown)** (`model/a_asym.py`, expectations fixed in note 11; output `model/a_asym.txt`, data `model/e_runs/asym.json`). A1 holds: the clean gap is 4.00 for a ring and 2.00 for a cubic torus. **A2 fails in the opposite direction to the expectation: under LOCAL re-pointing of relations, a flat torus and a cubic torus keep their readings (exactly 2.0 and 3.0) with 100 PER CENT of their relations moved, while the RING loses its reading at 5 per cent** - a ring's signature depends on its loop staying intact. So three dimensions is the robust shape and one is the fragile one, which is the reverse of the guess, and the reverse of what ED's growth does (ring survives 16 of 16, cubic torus fails 7 of 8). **Method error found and recorded: the first version blurred by re-pointing an end to a RANDOM event anywhere, which destroyed every shape at 2 per cent and told us nothing - ED's growth only ever makes local relations, so the blurring had to be local too.** Control added and run: **a clean cubic torus reads exactly 3.0 at 6, 12, 25 and 50 relations per event (gaps 1.91-2.00) and a clean ring reads exactly 1.0, so thickness is not the cause either.** | CHECKED (result) | `model/a_asym.txt`. | C11, C13 | - |
| C15 | **The mechanism test: random splitting is NOT the cause, and the asymmetry deflates (M1 fails, M2 fails)** (`model/m_mech.py`, expectations M1-M3 fixed in the file before the run; output `model/m_mech.txt`, data `model/e_runs/mech.json`, note 12 `A_Asymmetry_Results.md`). Hypothesis tested: that splitting a parent's neighbourhood AT RANDOM between its children decoheres the three directions, while a loop does not care how it is split. Two arms differing in one line - random pairing against children paired BY INDEX so a neighbourhood passes on consistently - grown from a cubic torus at thickness 25, read every third generation, three seeds. **M2 fails: coherent pairing does NOT rescue three dimensions, losing the reading in all three seeds and if anything slightly sooner. M1 fails too: one random seed kept 3.0 throughout.** **What the numbers actually show: in BOTH arms the gap decays smoothly with growth, 2.00 at generation 0 to about 1.55 by generation 15 - the group of six modes is still there and what widens is the spread within it, so the reading blurs rather than collapses. The ring does the same, 4.00 drifting to 3.4-3.9 over comparable growth. In relative terms both decay at similar rates (about 20 per cent for the cubic torus, about 15 for the ring); the difference is the STARTING MARGIN and where Claude's pass/fail bar sits.** Honest statement: **ED's growth gradually blurs directional structure at a similar relative rate whatever the dimension; a one-dimensional signature starts with twice the margin and survives, a three-dimensional one starts with half and does not.** **C11's sentence - that ED's growth is hardest on exactly the shape the project wants - is WITHDRAWN: the growth is corrosive to all directional structure, and a binary rule turned a smooth common decay into a dramatic-looking asymmetry.** What survives: nothing creates a dimension (16 runs, untouched by any of this); ED's growth degrades directional structure as it grows (new, two arms, three seeds, stated as a constraint per D3); ED's own instrument works and a clean lattice reads its dimension exactly at every thickness. **Claude's three errors today, all caught by tests written to catch them: the thick regime preserving a dimension exactly (withdrawn, correlated seeds); ED being hostile to three dimensions specifically (withdrawn, it is hostile to everything); and random splitting as the cause (wrong, coherent splitting does the same).** | CHECKED (result) | `model/m_mech.txt`, `A_Asymmetry_Results.md`. | C11, C13, C14; D3 | - |
| C16 | **Attempt 13 concluded (D4, RD13) and the project written up** in `../Project_Write_Up.md`, a cross-attempt document alongside `../What_ED_Needs.md`; no closed record edited. The write-up states what ED claims under D3 (conditions of possibility, in the January paper's words), what thirteen attempts established with the strength of each, what was not established, the census, the six things ED was found to be missing and who found them, what was got wrong and withdrawn, where this leaves ED, what would change the picture, and how to read the record. **Established, as written up:** the budgets fixing three numbers CDT tunes by hand (A11 C5, C8 - exact, the only genuine reduction, and a constraint-shaped claim); A6's floor measured, so that below three dimensions clocks cannot hold together at large size (A11 C21); **nothing in ED creates a dimension** (A13 C10, C11 - sixteen runs, two sizes, eight seeds, ED's own instrument calibrated on known shapes, in the thick regime ED's own papers specify), together with the correction that the borrowed ball-growth instrument used for twelve attempts disagrees with ED's own definition by more than a whole dimension in both directions; ED's growth degrading directional structure as it grows (A13 C14, C15); patterns fragmenting below about five relations per event (A12 C7); and ED never collapsing in the 2+1 runs (A11 C20). **Not established:** three dimensions from ED, space produced rather than supplied, where a first shape comes from; inputs supplied three plus a starting shape in every model; nothing derived, the budget result being a reduction in a RIVAL theory's free numbers rather than in ED's own list. **Verdict as written: on the evidence ED is a theory of conditions** - it says what a pattern must satisfy to hold together, fixes numbers another theory has to guess, carries and blurs a shape, and never collapses; it does not make space and does not pick three, and on its own account it was never claiming to. What would change the picture: something in ED that says WHERE A NEW EVENT GOES (the gap every attempt reached); a second genuine reduction of the budget result's form; a checkable difference from the standard account. | CONDITIONAL (write-up) | `../Project_Write_Up.md`. | C12, C15; D3, D4; A11 C5, C8, C20, C21; A12 C7; A13 C10, C11, C14 | - |
